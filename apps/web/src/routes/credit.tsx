// ##################
// authored (generated by gemini, Aug 30 2026 14:58 WITA)
// ##################

import { createFileRoute } from "@tanstack/react-router";
import { pageHead } from "@/utils/page-head";
import LandingNavbar from "@/components/navbar";
import Footer from "@/components/footer";
import ImageWithFallback from "@/components/image-with-fallback";
import { useQuery } from "@tanstack/react-query";
import { trpc } from "@/utils/trpc";
import { getPublicUrl } from "@/utils/url";
import {
  IconBrandLinkedin,
  IconMail,
  IconBrandInstagram,
  IconBrandTwitter,
  IconBrandGithub,
  IconWorld,
  IconUsers,
} from "@tabler/icons-react";
import { Skeleton } from "@/components/ui/skeleton";

export const Route = createFileRoute("/credit")({
  component: CreditPage,
  head: () => pageHead("Tim Kami - The People Behind Tepian K3"),
});

const SOCIAL_ICON_MAP: Record<string, any> = {
  linkedin: IconBrandLinkedin,
  email: IconMail,
  instagram: IconBrandInstagram,
  twitter: IconBrandTwitter,
  github: IconBrandGithub,
  website: IconWorld,
};

function TeamMemberCard({ member }: { member: any }) {
  const socialLinks: Array<{ platform: string; url: string; label: string }> =
    Array.isArray(member.socialLinks) ? member.socialLinks : [];

  return (
    <div className="group relative flex flex-col overflow-hidden rounded-2xl border border-slate-200/80 bg-white p-4 shadow-xs transition-all duration-300 hover:-translate-y-1.5 hover:border-blue-300 hover:shadow-xl dark:border-neutral-800 dark:bg-neutral-900">
      {/* Photo Container */}
      <div className="relative aspect-[4/5] w-full overflow-hidden rounded-xl bg-slate-100 dark:bg-neutral-800">
        {member.photoUrl ? (
          <ImageWithFallback
            src={getPublicUrl(member.photoUrl)}
            alt={member.name}
            className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105"
          />
        ) : (
          <div className="flex h-full w-full items-center justify-center bg-gradient-to-br from-slate-100 to-slate-200 text-slate-400 dark:from-neutral-800 dark:to-neutral-900 dark:text-neutral-600">
            <IconUsers className="h-20 w-20" />
          </div>
        )}
      </div>

      {/* Details */}
      <div className="mt-4 flex flex-1 flex-col justify-between">
        <div>
          <h3 className="line-clamp-1 text-base font-extrabold text-slate-800 dark:text-white">
            {member.name}
          </h3>
          <p className="line-clamp-1 text-xs font-semibold text-blue-600 dark:text-blue-400">
            {member.role}
          </p>
          {member.bio && (
            <p className="mt-2 line-clamp-2 text-xs text-slate-500 dark:text-neutral-400">
              {member.bio}
            </p>
          )}
        </div>

        {/* Social Links */}
        {socialLinks.length > 0 && (
          <div className="mt-4 flex flex-wrap items-center gap-2 border-t border-slate-100 pt-3 dark:border-neutral-800">
            {socialLinks.map((link, idx) => {
              const IconComponent =
                SOCIAL_ICON_MAP[link.platform.toLowerCase()] || IconWorld;
              const href =
                link.platform.toLowerCase() === "email" &&
                !link.url.startsWith("mailto:")
                  ? `mailto:${link.url}`
                  : link.url;

              return (
                <a
                  key={idx}
                  href={href}
                  target="_blank"
                  rel="noopener noreferrer"
                  title={link.label || link.platform}
                  className="flex h-8 w-8 items-center justify-center rounded-lg border border-slate-200 bg-slate-50 text-slate-600 transition-all hover:scale-110 hover:border-blue-500 hover:bg-blue-50 hover:text-blue-600 dark:border-neutral-700 dark:bg-neutral-800 dark:text-neutral-300 dark:hover:border-blue-400 dark:hover:bg-neutral-700 dark:hover:text-blue-400"
                >
                  <IconComponent className="h-4 w-4" />
                </a>
              );
            })}
          </div>
        )}
      </div>
    </div>
  );
}

function CreditPage() {
  const { data: teamMembers, isLoading: isTeamLoading } = useQuery(
    trpc.platform.teamMember.getAllTeamMembers.queryOptions(),
  );

  return (
    <div className="flex min-h-screen w-full flex-col overflow-x-hidden bg-[#F8F9FD] dark:bg-neutral-950">
      <LandingNavbar />

      {/* ================= HERO SECTION ================= */}
      <section className="relative overflow-hidden bg-gradient-to-r from-[#080B21] via-[#0D1236] to-[#121A4B] pt-14 pb-28 sm:pt-20 sm:pb-36 lg:pt-24 lg:pb-44">
        {/* Subtle background glow/stars */}
        <div className="pointer-events-none absolute inset-0 overflow-hidden opacity-30">
          <div className="absolute top-10 left-1/4 h-72 w-72 rounded-full bg-blue-600/20 blur-3xl" />
          <div className="absolute right-1/4 bottom-10 h-80 w-80 rounded-full bg-indigo-500/20 blur-3xl" />
        </div>

        <div className="relative z-10 mx-auto max-w-6xl px-4 sm:px-8">
          <div className="max-w-2xl space-y-6">
            {/* Brush Badge */}
            <div className="relative inline-flex items-center">
              <svg
                className="absolute inset-0 h-full w-full"
                viewBox="0 0 110 36"
                fill="none"
                xmlns="http://www.w3.org/2000/svg"
                preserveAspectRatio="none"
              >
                <path
                  d="M4.5 18C4 10 12 4.5 25 3.5C42 2.2 78 2 95 4C104 5 107.5 11 106 18C104.5 25 101 32 88 33C71 34.3 35 34.5 18 33C8.5 32.2 5 26 4.5 18Z"
                  fill="#4D3DB2"
                />
              </svg>
              <span className="relative z-10 px-4 py-1.5 text-xs font-semibold tracking-wide text-white">
                Tim Kami
              </span>
            </div>

            {/* Typography */}
            <div className="space-y-1">
              <h1 className="font-poppins text-4xl font-extrabold tracking-tight text-white sm:text-5xl lg:text-6xl">
                The People
              </h1>
              <div className="flex flex-wrap items-baseline gap-x-3">
                <h1 className="font-poppins text-4xl font-extrabold tracking-tight text-white sm:text-5xl lg:text-6xl">
                  Behind
                </h1>
                <div className="relative inline-block">
                  <span className="font-caveat text-5xl font-bold tracking-wide text-[#DEF244] sm:text-6xl lg:text-7xl">
                    Tepian K3
                  </span>
                  {/* Decorative scribble loop SVG under Tepian K3 */}
                  <svg
                    className="pointer-events-none absolute -right-2 -bottom-2 -left-2 h-7 w-[110%] text-[#DEF244]/80"
                    viewBox="0 0 200 30"
                    fill="none"
                    xmlns="http://www.w3.org/2000/svg"
                  >
                    <path
                      d="M5 18C45 6 150 4 192 14C198 15.5 190 24 165 25C110 27 25 24 10 18C-2 13 40 8 95 7"
                      stroke="currentColor"
                      strokeWidth="2.5"
                      strokeLinecap="round"
                      strokeLinejoin="round"
                    />
                  </svg>
                </div>
              </div>
            </div>

            {/* Subtitle */}
            <p className="max-w-lg text-base leading-relaxed font-normal text-slate-200/90 sm:text-lg">
              &ldquo;Gabungan pemikir kreatif dan pembuat sistem yang mengubah
              ide menjadi solusi digital.&rdquo;
            </p>
          </div>
        </div>

        {/* Bottom Curved Wave Transition to Section Below */}
        <div className="pointer-events-none absolute right-0 -bottom-1 left-0 z-10 w-full overflow-hidden leading-none">
          <svg
            viewBox="0 0 1440 96"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
            className="h-12 w-full object-cover sm:h-16 lg:h-24"
            preserveAspectRatio="none"
          >
            <path
              d="M0 96L1440 96L1440 0C1100 65 500 90 0 10V96Z"
              className="fill-[#F8F9FD] dark:fill-neutral-950"
            />
          </svg>
        </div>
      </section>

      {/* ================= MEET OUR TEAM SECTION ================= */}
      <section
        id="meet-team"
        className="mx-auto max-w-6xl px-4 pt-8 pb-16 sm:px-8 md:pb-24"
      >
        {/* Section Header (Matching Image 1: 2-column horizontal layout) */}
        <div className="flex flex-col gap-6 md:flex-row md:items-center">
          {/* Left Column: Badge + Heading */}
          <div className="shrink-0 space-y-2">
            <div className="relative inline-flex items-center">
              <svg
                className="absolute inset-0 h-full w-full"
                viewBox="0 0 110 36"
                fill="none"
                xmlns="http://www.w3.org/2000/svg"
                preserveAspectRatio="none"
              >
                <path
                  d="M4.5 18C4 10 12 4.5 25 3.5C42 2.2 78 2 95 4C104 5 107.5 11 106 18C104.5 25 101 32 88 33C71 34.3 35 34.5 18 33C8.5 32.2 5 26 4.5 18Z"
                  fill="#4D3DB2"
                />
              </svg>
              <span className="relative z-10 px-3.5 py-1 text-xs font-semibold text-white">
                Tepian K3
              </span>
            </div>
            <h2 className="font-poppins text-3xl font-extrabold tracking-tight text-slate-800 sm:text-4xl dark:text-white">
              Meet Our Team
            </h2>
          </div>

          {/* Vertical Divider (Desktop) */}
          <div className="hidden h-14 w-px bg-slate-300 md:block dark:bg-neutral-700" />

          {/* Right Column: Subtitle */}
          <div className="max-w-md">
            <p className="text-sm leading-relaxed text-slate-700 sm:text-base dark:text-neutral-300">
              Bersama dalam kolaborasi, berdedikasi untuk menghadirkan karya
              terbaik bagi Tepian K3.
            </p>
          </div>
        </div>

        {/* Team Grid */}
        <div className="mt-12">
          {isTeamLoading ? (
            <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
              {Array.from({ length: 8 }).map((_, i) => (
                <div
                  key={i}
                  className="space-y-4 rounded-2xl border border-slate-200 bg-white p-4 dark:border-neutral-800 dark:bg-neutral-900"
                >
                  <Skeleton className="aspect-[4/5] w-full rounded-xl" />
                  <Skeleton className="h-5 w-3/4 rounded-md" />
                  <Skeleton className="h-4 w-1/2 rounded-md" />
                  <Skeleton className="h-8 w-full rounded-md" />
                </div>
              ))}
            </div>
          ) : !teamMembers || teamMembers.length === 0 ? (
            <div className="flex flex-col items-center justify-center rounded-3xl border border-dashed border-slate-300 bg-white py-16 text-center dark:border-neutral-800 dark:bg-neutral-900">
              <div className="rounded-full bg-blue-50 p-4 text-blue-600 dark:bg-neutral-800 dark:text-blue-400">
                <IconUsers className="h-10 w-10" />
              </div>
              <h3 className="mt-4 text-lg font-bold text-slate-800 dark:text-white">
                Profil Anggota Tim Segera Hadir
              </h3>
              <p className="mt-1 max-w-md text-xs text-slate-500 dark:text-neutral-400">
                Data anggota tim sedang diperbarui oleh administrator. Silakan
                kunjungi kembali nanti.
              </p>
            </div>
          ) : (
            <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
              {teamMembers.map((member) => (
                <TeamMemberCard key={member.id} member={member} />
              ))}
            </div>
          )}
        </div>

        {/* ================= DEDICATION / QUOTE SECTION (Image 2 Card) ================= */}
        <div className="mt-20 flex justify-center">
          <div className="relative w-full max-w-2xl rounded-3xl border border-slate-100 bg-white p-8 shadow-[0_8px_30px_rgb(0,0,0,0.06)] sm:p-10 dark:border-neutral-800 dark:bg-neutral-900">
            {/* Top Quotes */}
            <div className="-mt-2 mb-2 flex items-center justify-between">
              <span className="font-serif text-5xl leading-none font-black text-[#4D3DB2] select-none sm:text-6xl">
                “
              </span>
              <span className="font-serif text-5xl leading-none font-black text-[#4D3DB2] select-none sm:text-6xl">
                ”
              </span>
            </div>

            {/* Quote Body */}
            <div className="space-y-4 px-2 sm:px-4">
              <p className="font-poppins text-lg leading-snug font-bold text-slate-800 sm:text-2xl dark:text-white">
                “Dedikasi untuk keselamatan, kontribusi untuk perubahan.”
              </p>
              <p className="text-base font-bold text-[#4D3DB2] sm:text-lg dark:text-indigo-400">
                -Tepian K3
              </p>
            </div>
          </div>
        </div>
      </section>

      <Footer />
    </div>
  );
}

// ##################
// end authored
// ##################
