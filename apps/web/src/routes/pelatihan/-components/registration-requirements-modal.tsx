// ##################
// authored (generated by claude, Jun 05 2026 15:15 WITA)
// ##################

import { useState, useEffect, useMemo } from "react";
import { useForm, Controller } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { useMutation, useQuery } from "@tanstack/react-query";
import { trpc } from "@/utils/trpc";
import z from "zod";
import { toast } from "sonner";
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import ComboBox from "@/components/ui/combobox";
import {
  Field,
  FieldError,
  FieldGroup,
  FieldLabel,
} from "@/components/ui/field";
import {
  Upload,
  Loader2,
  Check,
  User,
  Building2,
  FileText,
  AlertCircle,
} from "lucide-react";
import { cn } from "@/lib/utils";

interface RegistrationRequirementsModalProps {
  isOpen: boolean;
  onClose: () => void;
  pelatihanId: string;
  dynamicRequirements: any;
  userProfile: any;
  userDocuments: any;
  onSubmitSuccess: () => void;
}

const ALL_REQUIREMENT_LABELS: Record<string, string> = {
  participantNik: "NIK (16 Digit)",
  participantBirthPlace: "Tempat Lahir",
  participantBirthDate: "Tanggal Lahir",
  participantPhone: "Nomor WhatsApp/HP",
  participantAddress: "Alamat Lengkap",
  participantBloodType: "Golongan Darah",
  companyName: "Nama Perusahaan",
  companyAddress: "Alamat Perusahaan",
  companyProvinceId: "Provinsi Perusahaan",
  companyRegencyId: "Kota/Kabupaten Perusahaan",
  companyDistrictId: "Kecamatan Perusahaan",
  companyKbli: "KBLI Bidang Usaha",
  ktpDoc: "Scan KTP (Kartu Identitas)",
  employmentLetter: "Surat Keterangan Kerja",
  consentLetter: "Surat Pernyataan Kesediaan",
  diploma: "Ijazah Terakhir",
  // ##################
  // authored (generated by claude, Jun 10 2026 05:25 WITA)
  // ##################
  passPhoto: "Pass Foto Latar Merah",
  // ##################
  // end authored
  // ##################
};

export default function RegistrationRequirementsModal({
  isOpen,
  onClose,
  pelatihanId,
  dynamicRequirements,
  userProfile,
  userDocuments,
  onSubmitSuccess,
}: RegistrationRequirementsModalProps) {
  // Determine missing fields and documents
  const missingReqs = useMemo(() => {
    const missing: {
      personal: string[];
      company: string[];
      documents: string[];
    } = {
      personal: [],
      company: [],
      documents: [],
    };

    if (!dynamicRequirements) return missing;

    // Personal details check
    const personalFields = [
      { key: "participantName", label: "Nama Lengkap" },
      { key: "participantNik", label: "NIK (16 Digit)" },
      { key: "participantBirthPlace", label: "Tempat Lahir" },
      { key: "participantBirthDate", label: "Tanggal Lahir" },
      { key: "participantPhone", label: "Nomor WhatsApp/HP" },
      { key: "participantAddress", label: "Alamat Lengkap" },
      { key: "participantBloodType", label: "Golongan Darah" },
    ];
    for (const field of personalFields) {
      if (dynamicRequirements[field.key] && !userProfile?.[field.key]) {
        missing.personal.push(field.key);
      }
    }

    // Company details check
    const companyFields = [
      { key: "companyName", label: "Nama Perusahaan" },
      { key: "companyAddress", label: "Alamat Perusahaan" },
      { key: "companyProvinceId", label: "Provinsi Perusahaan" },
      { key: "companyRegencyId", label: "Kota/Kabupaten Perusahaan" },
      { key: "companyDistrictId", label: "Kecamatan Perusahaan" },
      { key: "companyKbli", label: "KBLI Bidang Usaha" },
    ];
    for (const field of companyFields) {
      if (dynamicRequirements[field.key] && !userProfile?.[field.key]) {
        missing.company.push(field.key);
      }
    }

    // Documents check
    const docs = [
      { key: "ktpDoc", type: "id_card", label: "Scan KTP" },
      {
        key: "employmentLetter",
        type: "employment_letter",
        label: "Surat Keterangan Kerja",
      },
      {
        key: "consentLetter",
        type: "employment_contract",
        label: "Surat Pernyataan Kesediaan",
      },
      { key: "diploma", type: "diploma", label: "Ijazah Terakhir" },
      // ##################
      // authored (generated by claude, Jun 10 2026 05:25 WITA)
      // ##################
      { key: "passPhoto", type: "pass_photo", label: "Pass Foto Latar Merah" },
      // ##################
      // end authored
      // ##################
    ];
    for (const doc of docs) {
      if (dynamicRequirements[doc.key]) {
        const docExists = userDocuments?.some((d: any) => d.type === doc.type);
        if (!docExists) {
          missing.documents.push(doc.key);
        }
      }
    }

    return missing;
  }, [dynamicRequirements, userProfile, userDocuments]);

  // Construct dynamic schema fields for validation of missing inputs only
  const formSchema = useMemo(() => {
    const schemaFields: any = {
      pelatihanId: z.string().uuid(),
    };

    if (missingReqs.personal.includes("participantName")) {
      schemaFields.participantName = z
        .string()
        .min(1, "Nama lengkap wajib diisi")
        .max(250);
    }
    if (missingReqs.personal.includes("participantNik")) {
      schemaFields.participantNik = z
        .string()
        .min(16, "NIK harus 16 digit")
        .max(16, "NIK harus 16 digit");
    }
    if (missingReqs.personal.includes("participantBirthPlace")) {
      schemaFields.participantBirthPlace = z
        .string()
        .min(1, "Tempat lahir wajib diisi")
        .max(250);
    }
    if (missingReqs.personal.includes("participantBirthDate")) {
      schemaFields.participantBirthDate = z
        .string()
        .min(1, "Tanggal lahir wajib diisi");
    }
    if (missingReqs.personal.includes("participantPhone")) {
      schemaFields.participantPhone = z
        .string()
        .min(1, "Nomor WhatsApp/HP wajib diisi")
        .max(50);
    }
    if (missingReqs.personal.includes("participantAddress")) {
      schemaFields.participantAddress = z
        .string()
        .min(1, "Alamat lengkap wajib diisi");
    }
    if (missingReqs.personal.includes("participantBloodType")) {
      schemaFields.participantBloodType = z
        .string()
        .min(1, "Golongan darah wajib diisi")
        .max(10);
    }

    if (missingReqs.company.includes("companyName")) {
      schemaFields.companyName = z
        .string()
        .min(1, "Nama perusahaan wajib diisi")
        .max(250);
    }
    if (missingReqs.company.includes("companyAddress")) {
      schemaFields.companyAddress = z
        .string()
        .min(1, "Alamat perusahaan wajib diisi");
    }
    if (missingReqs.company.includes("companyProvinceId")) {
      schemaFields.companyProvinceId = z
        .string()
        .uuid("Provinsi wajib dipilih");
    }
    if (missingReqs.company.includes("companyRegencyId")) {
      schemaFields.companyRegencyId = z
        .string()
        .uuid("Kota/Kabupaten wajib dipilih");
    }
    if (missingReqs.company.includes("companyDistrictId")) {
      schemaFields.companyDistrictId = z
        .string()
        .uuid("Kecamatan wajib dipilih");
    }
    if (missingReqs.company.includes("companyKbli")) {
      schemaFields.companyKbli = z.string().min(1, "KBLI wajib diisi").max(250);
    }

    if (missingReqs.documents.includes("ktpDoc")) {
      schemaFields.ktpDocUrl = z.string().url("Scan KTP wajib diunggah");
      schemaFields.ktpDocName = z.string().min(1);
    }
    if (missingReqs.documents.includes("employmentLetter")) {
      schemaFields.employmentLetterUrl = z
        .string()
        .url("Surat Keterangan Kerja wajib diunggah");
      schemaFields.employmentLetterName = z.string().min(1);
    }
    if (missingReqs.documents.includes("consentLetter")) {
      schemaFields.consentLetterUrl = z
        .string()
        .url("Surat Pernyataan Kesediaan wajib diunggah");
      schemaFields.consentLetterName = z.string().min(1);
    }
    if (missingReqs.documents.includes("diploma")) {
      schemaFields.diplomaUrl = z.string().url("Ijazah wajib diunggah");
      schemaFields.diplomaName = z.string().min(1);
    }
    // ##################
    // authored (generated by claude, Jun 10 2026 05:25 WITA)
    // ##################
    if (missingReqs.documents.includes("passPhoto")) {
      schemaFields.passPhotoUrl = z
        .string()
        .url("Pass Foto Latar Merah wajib diunggah");
      schemaFields.passPhotoName = z.string().min(1);
    }
    // ##################
    // end authored
    // ##################

    return z.object(schemaFields);
  }, [missingReqs]);

  const form = useForm<any>({
    resolver: zodResolver(formSchema),
    defaultValues: {
      pelatihanId,
      participantName: userProfile?.participantName || "",
      participantNik: userProfile?.participantNik || "",
      participantBirthPlace: userProfile?.participantBirthPlace || "",
      participantBirthDate: userProfile?.participantBirthDate
        ? userProfile.participantBirthDate.slice(0, 10)
        : "",
      participantPhone: userProfile?.participantPhone || "",
      participantAddress: userProfile?.participantAddress || "",
      participantBloodType: userProfile?.participantBloodType || "",
      companyName: userProfile?.companyName || "",
      companyAddress: userProfile?.companyAddress || "",
      companyProvinceId: userProfile?.companyProvinceId || "",
      companyRegencyId: userProfile?.companyRegencyId || "",
      companyDistrictId: userProfile?.companyDistrictId || "",
      companyKbli: userProfile?.companyKbli || "",
      ktpDocUrl: "",
      ktpDocName: "",
      employmentLetterUrl: "",
      employmentLetterName: "",
      consentLetterUrl: "",
      consentLetterName: "",
      diplomaUrl: "",
      diplomaName: "",
      // ##################
      // authored (generated by claude, Jun 10 2026 05:25 WITA)
      // ##################
      passPhotoUrl: "",
      passPhotoName: "",
      // ##################
      // end authored
      // ##################
    },
  });

  // Watch geolocation triggers
  const companyProvinceId = form.watch("companyProvinceId");
  const companyRegencyId = form.watch("companyRegencyId");

  const [provinceOpen, setProvinceOpen] = useState(false);
  const [regencyOpen, setRegencyOpen] = useState(false);
  const [districtOpen, setDistrictOpen] = useState(false);

  // Geolocation Queries
  const { data: provinces } = useQuery(
    trpc.platform.province.getAllProvinces.queryOptions(),
  );

  const { data: regencies } = useQuery({
    ...trpc.platform.regency.getAllRegenciesByProvinceId.queryOptions({
      provinceId: companyProvinceId,
    }),
    enabled: !!companyProvinceId,
  });

  const { data: districts } = useQuery({
    ...trpc.platform.district.getAllDistrictsByRegencyId.queryOptions({
      regencyId: companyRegencyId,
    }),
    enabled: !!companyRegencyId,
  });

  // File Upload Handling using profile's uploadDocument
  const uploadMutation = useMutation(
    trpc.pelatihan.profile.uploadDocument.mutationOptions({
      onError: (err) => {
        toast.error(`Gagal mengunggah berkas: ${err.message}`);
      },
    }),
  );

  const [uploadingState, setUploadingState] = useState<{
    ktpDoc: boolean;
    employmentLetter: boolean;
    consentLetter: boolean;
    diploma: boolean;
    // ##################
    // authored (generated by claude, Jun 10 2026 05:25 WITA)
    // ##################
    passPhoto: boolean;
    // ##################
    // end authored
    // ##################
  }>({
    ktpDoc: false,
    employmentLetter: false,
    consentLetter: false,
    diploma: false,
    // ##################
    // authored (generated by claude, Jun 10 2026 05:25 WITA)
    // ##################
    passPhoto: false,
    // ##################
    // end authored
    // ##################
  });

  const handleUploadFile = async (
    e: React.ChangeEvent<HTMLInputElement>,
    type:
      | "ktpDoc"
      | "employmentLetter"
      | "consentLetter"
      | "diploma"
      | "passPhoto",
    label: string,
  ) => {
    const file = e.target.files?.[0];
    if (!file) return;

    // ##################
    // authored (generated by claude, Jun 10 2026 05:25 WITA)
    // ##################
    if (type === "passPhoto") {
      if (!["image/jpeg", "image/png", "image/jpg"].includes(file.type)) {
        toast.error("Pass Foto harus berupa gambar (JPG, JPEG, PNG)");
        return;
      }
      if (file.size > 5 * 1024 * 1024) {
        toast.error("Ukuran Pass Foto tidak boleh melebihi 5MB");
        return;
      }
    } else {
      if (file.size > 10 * 1024 * 1024) {
        toast.error("Ukuran berkas tidak boleh melebihi 10MB");
        return;
      }
    }
    // ##################
    // end authored
    // ##################

    const name =
      form.getValues("participantName") ||
      userProfile?.participantName ||
      "Peserta";
    const comp =
      form.getValues("companyName") || userProfile?.companyName || "Perusahaan";
    const extension = file.name.split(".").pop();
    const cleanName = `${label}_${name}_${comp}`.replace(
      /[^a-zA-Z0-9_\-\s]/g,
      "",
    );
    const renamedFileName = `${cleanName}.${extension}`;

    const renamedFile = new File([file], renamedFileName, { type: file.type });

    setUploadingState((prev) => ({ ...prev, [type]: true }));

    // ##################
    // authored (generated by antigravity, Jun 09 2026 15:40 WITA)
    // ##################
    const docTypeMap: Record<string, string> = {
      ktpDoc: "id_card",
      employmentLetter: "employment_letter",
      consentLetter: "employment_contract",
      diploma: "diploma",
      // ##################
      // authored (generated by claude, Jun 10 2026 05:25 WITA)
      // ##################
      passPhoto: "pass_photo",
      // ##################
      // end authored
      // ##################
    };
    const mappedType = docTypeMap[type] as string;

    const formData = new FormData();
    formData.append("file", renamedFile);
    formData.append("type", mappedType);
    // ##################
    // end authored
    // ##################

    try {
      const res = await uploadMutation.mutateAsync(formData);
      form.setValue(`${type}Url`, res.fileUrl);
      form.setValue(`${type}Name`, res.fileName);
      form.clearErrors(`${type}Url` as any);
      toast.success(`Berkas ${label} berhasil diunggah!`);
    } catch (error) {
      console.error(error);
    } finally {
      setUploadingState((prev) => ({ ...prev, [type]: false }));
    }
  };

  // Profile Save Mutation
  const saveMutation = useMutation(
    trpc.pelatihan.profile.saveRequirementsAndUpdateProfile.mutationOptions({
      onSuccess: () => {
        toast.success("Profil pendaftaran berhasil diperbarui!");
        onSubmitSuccess();
        onClose();
      },
      onError: (err) => {
        toast.error(`Gagal menyimpan data pendaftaran: ${err.message}`);
      },
    }),
  );

  const handleSubmit = (data: any) => {
    saveMutation.mutate(data);
  };

  // Reset form when modal opens with fresh data
  useEffect(() => {
    if (isOpen) {
      form.reset({
        pelatihanId,
        participantName: userProfile?.participantName || "",
        participantNik: userProfile?.participantNik || "",
        participantBirthPlace: userProfile?.participantBirthPlace || "",
        participantBirthDate: userProfile?.participantBirthDate
          ? userProfile.participantBirthDate.slice(0, 10)
          : "",
        participantPhone: userProfile?.participantPhone || "",
        participantAddress: userProfile?.participantAddress || "",
        participantBloodType: userProfile?.participantBloodType || "",
        companyName: userProfile?.companyName || "",
        companyAddress: userProfile?.companyAddress || "",
        companyProvinceId: userProfile?.companyProvinceId || "",
        companyRegencyId: userProfile?.companyRegencyId || "",
        companyDistrictId: userProfile?.companyDistrictId || "",
        companyKbli: userProfile?.companyKbli || "",
        ktpDocUrl: "",
        ktpDocName: "",
        employmentLetterUrl: "",
        employmentLetterName: "",
        consentLetterUrl: "",
        consentLetterName: "",
        diplomaUrl: "",
        diplomaName: "",
        // ##################
        // authored (generated by claude, Jun 10 2026 05:25 WITA)
        // ##################
        passPhotoUrl: "",
        passPhotoName: "",
        // ##################
        // end authored
        // ##################
      });
    }
  }, [isOpen, pelatihanId, userProfile]);

  const hasMissingFields =
    missingReqs.personal.length > 0 ||
    missingReqs.company.length > 0 ||
    missingReqs.documents.length > 0;

  if (!hasMissingFields && isOpen) {
    // If no missing fields, immediately call onSubmitSuccess and close modal
    onSubmitSuccess();
    onClose();
    return null;
  }

  return (
    <Dialog open={isOpen} onOpenChange={(open) => !open && onClose()}>
      <DialogContent className="max-h-[90vh] max-w-2xl overflow-y-auto font-['Poppins']">
        <DialogHeader>
          <DialogTitle className="text-xl font-bold text-slate-800">
            Lengkapi Persyaratan Pendaftaran
          </DialogTitle>
          <DialogDescription className="text-sm text-slate-500">
            Silakan lengkapi beberapa berkas dan informasi pendaftaran yang
            diwajibkan oleh Admin Pelatihan untuk dapat mendaftar kelas ini.
          </DialogDescription>
        </DialogHeader>

        {/* Checklist Ringkasan Persyaratan (Filter Centang Otomatis) */}
        <div className="mt-4 rounded-xl border border-blue-100 bg-blue-50/50 p-4">
          <h4 className="mb-3 font-['Poppins'] text-xs font-bold tracking-wider text-blue-800 uppercase">
            Daftar Persyaratan Kelas
          </h4>
          <div className="grid grid-cols-1 gap-2.5 text-xs sm:grid-cols-2">
            {Object.entries(ALL_REQUIREMENT_LABELS).map(([key, label]) => {
              if (!dynamicRequirements?.[key]) return null;

              // Check if requirement is missing
              const isMissing =
                missingReqs.personal.includes(key) ||
                missingReqs.company.includes(key) ||
                missingReqs.documents.includes(key);

              return (
                <div
                  key={key}
                  className={cn(
                    "flex items-center gap-2.5 rounded-lg border p-2.5 transition-all duration-200",
                    isMissing
                      ? "border-amber-100 bg-amber-50/30 text-amber-900"
                      : "border-emerald-100 bg-emerald-50/30 text-emerald-900",
                  )}
                >
                  {isMissing ? (
                    <div className="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-amber-100 text-amber-700">
                      <AlertCircle className="h-3 w-3" />
                    </div>
                  ) : (
                    <div className="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-emerald-100 text-emerald-700">
                      <Check className="h-3 w-3" strokeWidth={3} />
                    </div>
                  )}
                  <span
                    className={cn(
                      "font-semibold",
                      !isMissing &&
                        "text-slate-400 line-through decoration-slate-300",
                    )}
                  >
                    {label}
                  </span>
                  <span
                    className={cn(
                      "ml-auto rounded-full px-1.5 py-0.5 text-[10px] font-bold",
                      isMissing
                        ? "bg-amber-100/60 text-amber-700"
                        : "bg-emerald-100/60 text-emerald-700",
                    )}
                  >
                    {isMissing ? "Belum Lengkap" : "Terpenuhi"}
                  </span>
                </div>
              );
            })}
          </div>
        </div>

        <form
          onSubmit={form.handleSubmit(handleSubmit)}
          className="mt-4 space-y-6"
        >
          <FieldGroup className="space-y-6">
            {/* 1. DATA DIRI */}
            {missingReqs.personal.length > 0 && (
              <div className="space-y-4">
                <div className="flex items-center gap-2 border-b border-slate-100 pb-2">
                  <User className="h-4.5 w-4.5 text-blue-600" />
                  <h3 className="text-sm font-bold tracking-wide text-slate-800 uppercase">
                    Data Diri Peserta
                  </h3>
                </div>

                <div className="grid grid-cols-1 gap-4 md:grid-cols-2">
                  {missingReqs.personal.includes("participantName") && (
                    <Controller
                      control={form.control}
                      name="participantName"
                      render={({ field, fieldState }) => (
                        <Field
                          data-invalid={fieldState.invalid}
                          className="space-y-1"
                        >
                          <FieldLabel className="text-xs font-bold text-slate-700">
                            Nama Lengkap *
                          </FieldLabel>
                          <Input
                            placeholder="Nama Lengkap sesuai KTP"
                            className="h-10 rounded-xl"
                            {...field}
                          />
                          {fieldState.invalid && (
                            <FieldError errors={[fieldState.error]} />
                          )}
                        </Field>
                      )}
                    />
                  )}

                  {missingReqs.personal.includes("participantNik") && (
                    <Controller
                      control={form.control}
                      name="participantNik"
                      render={({ field, fieldState }) => (
                        <Field
                          data-invalid={fieldState.invalid}
                          className="space-y-1"
                        >
                          <FieldLabel className="text-xs font-bold text-slate-700">
                            NIK (16 Digit) *
                          </FieldLabel>
                          <Input
                            maxLength={16}
                            placeholder="317xxxxxxxxxxxxx"
                            className="h-10 rounded-xl"
                            {...field}
                          />
                          {fieldState.invalid && (
                            <FieldError errors={[fieldState.error]} />
                          )}
                        </Field>
                      )}
                    />
                  )}

                  {missingReqs.personal.includes("participantBirthPlace") && (
                    <Controller
                      control={form.control}
                      name="participantBirthPlace"
                      render={({ field, fieldState }) => (
                        <Field
                          data-invalid={fieldState.invalid}
                          className="space-y-1"
                        >
                          <FieldLabel className="text-xs font-bold text-slate-700">
                            Tempat Lahir *
                          </FieldLabel>
                          <Input
                            placeholder="Kota/Kabupaten Lahir"
                            className="h-10 rounded-xl"
                            {...field}
                          />
                          {fieldState.invalid && (
                            <FieldError errors={[fieldState.error]} />
                          )}
                        </Field>
                      )}
                    />
                  )}

                  {missingReqs.personal.includes("participantBirthDate") && (
                    <Controller
                      control={form.control}
                      name="participantBirthDate"
                      render={({ field, fieldState }) => (
                        <Field
                          data-invalid={fieldState.invalid}
                          className="space-y-1"
                        >
                          <FieldLabel className="text-xs font-bold text-slate-700">
                            Tanggal Lahir *
                          </FieldLabel>
                          <Input
                            type="date"
                            className="h-10 rounded-xl"
                            {...field}
                          />
                          {fieldState.invalid && (
                            <FieldError errors={[fieldState.error]} />
                          )}
                        </Field>
                      )}
                    />
                  )}

                  {missingReqs.personal.includes("participantPhone") && (
                    <Controller
                      control={form.control}
                      name="participantPhone"
                      render={({ field, fieldState }) => (
                        <Field
                          data-invalid={fieldState.invalid}
                          className="space-y-1"
                        >
                          <FieldLabel className="text-xs font-bold text-slate-700">
                            Nomor WhatsApp/HP *
                          </FieldLabel>
                          <Input
                            placeholder="08xxxxxxxxxx"
                            className="h-10 rounded-xl"
                            {...field}
                          />
                          {fieldState.invalid && (
                            <FieldError errors={[fieldState.error]} />
                          )}
                        </Field>
                      )}
                    />
                  )}

                  {missingReqs.personal.includes("participantBloodType") && (
                    <Controller
                      control={form.control}
                      name="participantBloodType"
                      render={({ field, fieldState }) => (
                        <Field
                          data-invalid={fieldState.invalid}
                          className="space-y-1"
                        >
                          <FieldLabel className="text-xs font-bold text-slate-700">
                            Golongan Darah *
                          </FieldLabel>
                          <ComboBox
                            options={[
                              { id: "A", name: "A" },
                              { id: "B", name: "B" },
                              { id: "AB", name: "AB" },
                              { id: "O", name: "O" },
                            ]}
                            value={field.value}
                            onChange={field.onChange}
                            placeholder="Pilih Golongan Darah"
                            searchPlaceholder="Cari golongan..."
                            emptyMessage="Tidak ditemukan"
                            open={provinceOpen} // reuse open state helper or just inline simple dropdown
                            onOpenChange={setProvinceOpen}
                            className="h-10 rounded-xl"
                          />
                          {fieldState.invalid && (
                            <FieldError errors={[fieldState.error]} />
                          )}
                        </Field>
                      )}
                    />
                  )}
                </div>

                {missingReqs.personal.includes("participantAddress") && (
                  <Controller
                    control={form.control}
                    name="participantAddress"
                    render={({ field, fieldState }) => (
                      <Field
                        data-invalid={fieldState.invalid}
                        className="space-y-1"
                      >
                        <FieldLabel className="text-xs font-bold text-slate-700">
                          Alamat Lengkap *
                        </FieldLabel>
                        <Textarea
                          placeholder="Alamat domisili lengkap sesuai KTP"
                          className="min-h-[70px] rounded-xl"
                          {...field}
                        />
                        {fieldState.invalid && (
                          <FieldError errors={[fieldState.error]} />
                        )}
                      </Field>
                    )}
                  />
                )}
              </div>
            )}

            {/* 2. DATA PERUSAHAAN */}
            {missingReqs.company.length > 0 && (
              <div className="space-y-4">
                <div className="flex items-center gap-2 border-b border-slate-100 pb-2">
                  <Building2 className="h-4.5 w-4.5 text-blue-600" />
                  <h3 className="text-sm font-bold tracking-wide text-slate-800 uppercase">
                    Data Perusahaan (Sponsor)
                  </h3>
                </div>

                <div className="grid grid-cols-1 gap-4 md:grid-cols-2">
                  {missingReqs.company.includes("companyName") && (
                    <Controller
                      control={form.control}
                      name="companyName"
                      render={({ field, fieldState }) => (
                        <Field
                          data-invalid={fieldState.invalid}
                          className="space-y-1"
                        >
                          <FieldLabel className="text-xs font-bold text-slate-700">
                            Nama Perusahaan *
                          </FieldLabel>
                          <Input
                            placeholder="PT. Sinar Mas Kencana"
                            className="h-10 rounded-xl"
                            {...field}
                          />
                          {fieldState.invalid && (
                            <FieldError errors={[fieldState.error]} />
                          )}
                        </Field>
                      )}
                    />
                  )}

                  {missingReqs.company.includes("companyKbli") && (
                    <Controller
                      control={form.control}
                      name="companyKbli"
                      render={({ field, fieldState }) => (
                        <Field
                          data-invalid={fieldState.invalid}
                          className="space-y-1"
                        >
                          <FieldLabel className="text-xs font-bold text-slate-700">
                            KBLI Bidang Usaha *
                          </FieldLabel>
                          <Input
                            placeholder="Kode KBLI 5 digit (misal: 46631)"
                            className="h-10 rounded-xl"
                            {...field}
                          />
                          {fieldState.invalid && (
                            <FieldError errors={[fieldState.error]} />
                          )}
                        </Field>
                      )}
                    />
                  )}

                  {/* Provinsi */}
                  {missingReqs.company.includes("companyProvinceId") && (
                    <Controller
                      control={form.control}
                      name="companyProvinceId"
                      render={({ field, fieldState }) => (
                        <Field
                          data-invalid={fieldState.invalid}
                          className="space-y-1"
                        >
                          <FieldLabel className="text-xs font-bold text-slate-700">
                            Provinsi Perusahaan *
                          </FieldLabel>
                          <ComboBox
                            options={provinces || []}
                            value={field.value}
                            onChange={(val) => {
                              field.onChange(val);
                              form.setValue("companyRegencyId", "");
                              form.setValue("companyDistrictId", "");
                            }}
                            placeholder="Pilih Provinsi..."
                            searchPlaceholder="Cari Provinsi..."
                            emptyMessage="Provinsi tidak ditemukan"
                            open={provinceOpen}
                            onOpenChange={setProvinceOpen}
                            className="h-10 rounded-xl"
                          />
                          {fieldState.invalid && (
                            <FieldError errors={[fieldState.error]} />
                          )}
                        </Field>
                      )}
                    />
                  )}

                  {/* Kabupaten / Kota */}
                  {missingReqs.company.includes("companyRegencyId") && (
                    <Controller
                      control={form.control}
                      name="companyRegencyId"
                      render={({ field, fieldState }) => (
                        <Field
                          data-invalid={fieldState.invalid}
                          className="space-y-1"
                        >
                          <FieldLabel className="text-xs font-bold text-slate-700">
                            Kabupaten/Kota *
                          </FieldLabel>
                          <ComboBox
                            options={regencies || []}
                            value={field.value}
                            onChange={(val) => {
                              field.onChange(val);
                              form.setValue("companyDistrictId", "");
                            }}
                            placeholder="Pilih Kabupaten/Kota..."
                            searchPlaceholder="Cari Kabupaten/Kota..."
                            emptyMessage="Kabupaten/Kota tidak ditemukan"
                            open={regencyOpen}
                            onOpenChange={setRegencyOpen}
                            disabled={!companyProvinceId}
                            className="h-10 rounded-xl"
                          />
                          {fieldState.invalid && (
                            <FieldError errors={[fieldState.error]} />
                          )}
                        </Field>
                      )}
                    />
                  )}

                  {/* Kecamatan */}
                  {missingReqs.company.includes("companyDistrictId") && (
                    <Controller
                      control={form.control}
                      name="companyDistrictId"
                      render={({ field, fieldState }) => (
                        <Field
                          data-invalid={fieldState.invalid}
                          className="space-y-1"
                        >
                          <FieldLabel className="text-xs font-bold text-slate-700">
                            Kecamatan *
                          </FieldLabel>
                          <ComboBox
                            options={districts || []}
                            value={field.value}
                            onChange={field.onChange}
                            placeholder="Pilih Kecamatan..."
                            searchPlaceholder="Cari Kecamatan..."
                            emptyMessage="Kecamatan tidak ditemukan"
                            open={districtOpen}
                            onOpenChange={setDistrictOpen}
                            disabled={!companyRegencyId}
                            className="h-10 rounded-xl"
                          />
                          {fieldState.invalid && (
                            <FieldError errors={[fieldState.error]} />
                          )}
                        </Field>
                      )}
                    />
                  )}
                </div>

                {missingReqs.company.includes("companyAddress") && (
                  <Controller
                    control={form.control}
                    name="companyAddress"
                    render={({ field, fieldState }) => (
                      <Field
                        data-invalid={fieldState.invalid}
                        className="space-y-1"
                      >
                        <FieldLabel className="text-xs font-bold text-slate-700">
                          Alamat Perusahaan *
                        </FieldLabel>
                        <Textarea
                          placeholder="Alamat lengkap lokasi kantor perusahaan"
                          className="min-h-[70px] rounded-xl"
                          {...field}
                        />
                        {fieldState.invalid && (
                          <FieldError errors={[fieldState.error]} />
                        )}
                      </Field>
                    )}
                  />
                )}
              </div>
            )}

            {/* 3. DOKUMEN PENDUKUNG */}
            {missingReqs.documents.length > 0 && (
              <div className="space-y-4">
                <div className="flex items-center gap-2 border-b border-slate-100 pb-2">
                  <FileText className="h-4.5 w-4.5 text-blue-600" />
                  <h3 className="text-sm font-bold tracking-wide text-slate-800 uppercase">
                    Dokumen Persyaratan
                  </h3>
                </div>

                <div className="grid grid-cols-1 gap-6 md:grid-cols-2">
                  {/* Scan KTP */}
                  {missingReqs.documents.includes("ktpDoc") && (
                    <div className="space-y-2">
                      <FieldLabel className="text-xs font-bold text-slate-700">
                        Scan KTP (PDF, JPG, PNG - Max 10MB) *
                      </FieldLabel>
                      <Controller
                        control={form.control}
                        name="ktpDocUrl"
                        render={({ field, fieldState }) => (
                          <div className="space-y-1.5">
                            <div className="flex items-center gap-3">
                              <label className="flex h-10 cursor-pointer items-center justify-center gap-2 rounded-xl border border-dashed border-slate-300 bg-slate-50 px-4 text-xs font-semibold text-slate-600 transition hover:bg-slate-100 dark:border-slate-700 dark:bg-slate-800 dark:hover:bg-slate-700">
                                <Upload className="h-4 w-4" />
                                {uploadingState.ktpDoc
                                  ? "Mengunggah..."
                                  : "Pilih File"}
                                <input
                                  type="file"
                                  accept=".pdf,.png,.jpg,.jpeg"
                                  className="hidden"
                                  disabled={uploadingState.ktpDoc}
                                  onChange={(e) =>
                                    handleUploadFile(e, "ktpDoc", "Scan KTP")
                                  }
                                />
                              </label>
                              {field.value ? (
                                <span className="flex items-center gap-1 text-xs font-medium text-green-600">
                                  <Check className="h-4 w-4" /> Terunggah
                                </span>
                              ) : (
                                <span className="text-xs text-slate-400">
                                  Belum ada file
                                </span>
                              )}
                            </div>
                            {form.watch("ktpDocName") && (
                              <p className="max-w-xs truncate text-[10px] text-slate-500">
                                {form.watch("ktpDocName")}
                              </p>
                            )}
                            {fieldState.invalid && (
                              <FieldError errors={[fieldState.error]} />
                            )}
                          </div>
                        )}
                      />
                    </div>
                  )}

                  {/* Surat Keterangan Kerja */}
                  {missingReqs.documents.includes("employmentLetter") && (
                    <div className="space-y-2">
                      <FieldLabel className="text-xs font-bold text-slate-700">
                        Surat Keterangan Kerja *
                      </FieldLabel>
                      <Controller
                        control={form.control}
                        name="employmentLetterUrl"
                        render={({ field, fieldState }) => (
                          <div className="space-y-1.5">
                            <div className="flex items-center gap-3">
                              <label className="flex h-10 cursor-pointer items-center justify-center gap-2 rounded-xl border border-dashed border-slate-300 bg-slate-50 px-4 text-xs font-semibold text-slate-600 transition hover:bg-slate-100 dark:border-slate-700 dark:bg-slate-800 dark:hover:bg-slate-700">
                                <Upload className="h-4 w-4" />
                                {uploadingState.employmentLetter
                                  ? "Mengunggah..."
                                  : "Pilih File"}
                                <input
                                  type="file"
                                  accept=".pdf,.png,.jpg,.jpeg"
                                  className="hidden"
                                  disabled={uploadingState.employmentLetter}
                                  onChange={(e) =>
                                    handleUploadFile(
                                      e,
                                      "employmentLetter",
                                      "Surat Keterangan Kerja",
                                    )
                                  }
                                />
                              </label>
                              {field.value ? (
                                <span className="flex items-center gap-1 text-xs font-medium text-green-600">
                                  <Check className="h-4 w-4" /> Terunggah
                                </span>
                              ) : (
                                <span className="text-xs text-slate-400">
                                  Belum ada file
                                </span>
                              )}
                            </div>
                            {form.watch("employmentLetterName") && (
                              <p className="max-w-xs truncate text-[10px] text-slate-500">
                                {form.watch("employmentLetterName")}
                              </p>
                            )}
                            {fieldState.invalid && (
                              <FieldError errors={[fieldState.error]} />
                            )}
                          </div>
                        )}
                      />
                    </div>
                  )}

                  {/* Surat Pernyataan Kesediaan */}
                  {missingReqs.documents.includes("consentLetter") && (
                    <div className="space-y-2">
                      <FieldLabel className="text-xs font-bold text-slate-700">
                        Surat Pernyataan Kesediaan *
                      </FieldLabel>
                      <Controller
                        control={form.control}
                        name="consentLetterUrl"
                        render={({ field, fieldState }) => (
                          <div className="space-y-1.5">
                            <div className="flex items-center gap-3">
                              <label className="flex h-10 cursor-pointer items-center justify-center gap-2 rounded-xl border border-dashed border-slate-300 bg-slate-50 px-4 text-xs font-semibold text-slate-600 transition hover:bg-slate-100 dark:border-slate-700 dark:bg-slate-800 dark:hover:bg-slate-700">
                                <Upload className="h-4 w-4" />
                                {uploadingState.consentLetter
                                  ? "Mengunggah..."
                                  : "Pilih File"}
                                <input
                                  type="file"
                                  accept=".pdf,.png,.jpg,.jpeg"
                                  className="hidden"
                                  disabled={uploadingState.consentLetter}
                                  onChange={(e) =>
                                    handleUploadFile(
                                      e,
                                      "consentLetter",
                                      "Surat Pernyataan Kesediaan",
                                    )
                                  }
                                />
                              </label>
                              {field.value ? (
                                <span className="flex items-center gap-1 text-xs font-medium text-green-600">
                                  <Check className="h-4 w-4" /> Terunggah
                                </span>
                              ) : (
                                <span className="text-xs text-slate-400">
                                  Belum ada file
                                </span>
                              )}
                            </div>
                            {form.watch("consentLetterName") && (
                              <p className="max-w-xs truncate text-[10px] text-slate-500">
                                {form.watch("consentLetterName")}
                              </p>
                            )}
                            {fieldState.invalid && (
                              <FieldError errors={[fieldState.error]} />
                            )}
                          </div>
                        )}
                      />
                    </div>
                  )}

                  {/* Ijazah Terakhir */}
                  {missingReqs.documents.includes("diploma") && (
                    <div className="space-y-2">
                      <FieldLabel className="text-xs font-bold text-slate-700">
                        Ijazah Terakhir *
                      </FieldLabel>
                      <Controller
                        control={form.control}
                        name="diplomaUrl"
                        render={({ field, fieldState }) => (
                          <div className="space-y-1.5">
                            <div className="flex items-center gap-3">
                              <label className="flex h-10 cursor-pointer items-center justify-center gap-2 rounded-xl border border-dashed border-slate-300 bg-slate-50 px-4 text-xs font-semibold text-slate-600 transition hover:bg-slate-100 dark:border-slate-700 dark:bg-slate-800 dark:hover:bg-slate-700">
                                <Upload className="h-4 w-4" />
                                {uploadingState.diploma
                                  ? "Mengunggah..."
                                  : "Pilih File"}
                                <input
                                  type="file"
                                  accept=".pdf,.png,.jpg,.jpeg"
                                  className="hidden"
                                  disabled={uploadingState.diploma}
                                  onChange={(e) =>
                                    handleUploadFile(
                                      e,
                                      "diploma",
                                      "Ijazah Terakhir",
                                    )
                                  }
                                />
                              </label>
                              {field.value ? (
                                <span className="flex items-center gap-1 text-xs font-medium text-green-600">
                                  <Check className="h-4 w-4" /> Terunggah
                                </span>
                              ) : (
                                <span className="text-xs text-slate-400">
                                  Belum ada file
                                </span>
                              )}
                            </div>
                            {form.watch("diplomaName") && (
                              <p className="max-w-xs truncate text-[10px] text-slate-500">
                                {form.watch("diplomaName")}
                              </p>
                            )}
                            {fieldState.invalid && (
                              <FieldError errors={[fieldState.error]} />
                            )}
                          </div>
                        )}
                      />
                    </div>
                  )}

                  {/* Pass Foto Latar Merah */}
                  {/* ##################
                      authored (generated by claude, Jun 10 2026 05:25 WITA)
                  ################## */}
                  {missingReqs.documents.includes("passPhoto") && (
                    <div className="space-y-2">
                      <FieldLabel className="text-xs font-bold text-slate-700">
                        Pass Foto Latar Merah (JPG, JPEG, PNG - Max 5MB) *
                      </FieldLabel>
                      <Controller
                        control={form.control}
                        name="passPhotoUrl"
                        render={({ field, fieldState }) => (
                          <div className="space-y-1.5">
                            <div className="flex items-center gap-3">
                              <label className="flex h-10 cursor-pointer items-center justify-center gap-2 rounded-xl border border-dashed border-slate-300 bg-slate-50 px-4 text-xs font-semibold text-slate-600 transition hover:bg-slate-100 dark:border-slate-700 dark:bg-slate-800 dark:hover:bg-slate-700">
                                <Upload className="h-4 w-4" />
                                {uploadingState.passPhoto
                                  ? "Mengunggah..."
                                  : "Pilih File"}
                                <input
                                  type="file"
                                  accept="image/jpeg,image/png,image/jpg"
                                  className="hidden"
                                  disabled={uploadingState.passPhoto}
                                  onChange={(e) =>
                                    handleUploadFile(
                                      e,
                                      "passPhoto",
                                      "Pass Foto Latar Merah",
                                    )
                                  }
                                />
                              </label>
                              {field.value ? (
                                <span className="flex items-center gap-1 text-xs font-medium text-green-600">
                                  <Check className="h-4 w-4" /> Terunggah
                                </span>
                              ) : (
                                <span className="text-xs text-slate-400">
                                  Belum ada file
                                </span>
                              )}
                            </div>
                            {form.watch("passPhotoName") && (
                              <p className="max-w-xs truncate text-[10px] text-slate-500">
                                {form.watch("passPhotoName")}
                              </p>
                            )}
                            {fieldState.invalid && (
                              <FieldError errors={[fieldState.error]} />
                            )}
                          </div>
                        )}
                      />
                    </div>
                  )}
                  {/* ##################
                      end authored
                  ################## */}
                </div>
              </div>
            )}
          </FieldGroup>

          <DialogFooter className="gap-3 border-t border-slate-100 pt-5 sm:gap-0">
            <Button
              type="button"
              variant="outline"
              onClick={onClose}
              disabled={saveMutation.isPending}
              className="h-10 rounded-xl"
            >
              Batal
            </Button>
            <Button
              type="submit"
              disabled={
                saveMutation.isPending ||
                uploadMutation.isPending ||
                Object.values(uploadingState).some(Boolean)
              }
              className="h-10 rounded-xl bg-blue-600 font-medium text-white hover:bg-blue-700"
            >
              {saveMutation.isPending ? (
                <>
                  <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                  Menyimpan...
                </>
              ) : (
                "Simpan & Lanjutkan"
              )}
            </Button>
          </DialogFooter>
        </form>
      </DialogContent>
    </Dialog>
  );
}
