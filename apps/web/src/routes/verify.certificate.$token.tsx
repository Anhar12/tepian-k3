// ##################
// authored (generated by gemini, Jun 05 2026 18:20 WITA)
// ##################

import { createFileRoute, Link } from "@tanstack/react-router";
import { useQuery } from "@tanstack/react-query";
import { trpc } from "@/utils/trpc";
import { pageHead } from "@/utils/page-head";
import { z } from "zod";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";
import { Separator } from "@/components/ui/separator";
import {
  Award,
  CheckCircle2,
  XCircle,
  Shield,
  Calendar,
  User,
  GraduationCap,
  Download,
  Loader2,
  AlertTriangle,
  ExternalLink,
} from "lucide-react";

export const Route = createFileRoute("/verify/certificate/$token")({
  params: z.object({
    token: z.string(),
  }),
  head: () =>
    pageHead(
      "Verifikasi Sertifikat",
      "Verifikasi keaslian sertifikat pelatihan",
    ),
  component: VerifyCertificateComponent,
});

function VerifyCertificateComponent() {
  const { token } = Route.useParams();

  // Public tRPC query to verify certificate
  const verifyQuery = useQuery(
    trpc.pelatihan.certificate.verifyCertificate.queryOptions({ token }),
  );

  if (verifyQuery.isLoading) {
    return (
      <div className="flex min-h-screen items-center justify-center bg-slate-50 p-4 font-['Poppins']">
        <Card className="w-full max-w-md border-slate-100 shadow-xl">
          <CardContent className="pt-6">
            <div className="space-y-4 text-center">
              <div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-blue-50">
                <Loader2 className="h-8 w-8 animate-spin text-blue-600" />
              </div>
              <h2 className="font-['Plus_Jakarta_Sans'] text-xl font-extrabold text-slate-800">
                Memverifikasi Sertifikat...
              </h2>
              <p className="text-xs text-slate-500">
                Mohon tunggu, kami sedang memverifikasi keaslian sertifikat
                pelatihan ini
              </p>
            </div>
          </CardContent>
        </Card>
      </div>
    );
  }

  if (verifyQuery.isError || !verifyQuery.data) {
    return (
      <div className="flex min-h-screen items-center justify-center bg-slate-50 p-4 font-['Poppins']">
        <Card className="w-full max-w-md border-rose-100 shadow-xl">
          <CardContent className="pt-6">
            <div className="space-y-4 text-center">
              <div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-rose-50">
                <XCircle className="h-8 w-8 text-rose-600" />
              </div>
              <h2 className="font-['Plus_Jakarta_Sans'] text-xl font-extrabold text-slate-800">
                Sertifikat Tidak Valid
              </h2>
              <p className="text-xs font-medium text-rose-700">
                {verifyQuery.error?.message ||
                  "Sertifikat tidak terdaftar di sistem kami atau telah dicabut."}
              </p>
              <Separator />
              <div className="space-y-2 text-left text-xs text-slate-500">
                <p className="font-bold text-slate-600">
                  Penyebab kemungkinan:
                </p>
                <ul className="list-inside list-disc space-y-1">
                  <li>Token verifikasi salah atau dirubah</li>
                  <li>Sertifikat telah dicabut (revoked) oleh admin</li>
                  <li>Data pendaftaran terkait sudah tidak aktif</li>
                </ul>
              </div>
              <Button
                variant="outline"
                className="h-10 w-full rounded-xl text-xs"
                onClick={() => window.location.reload()}
              >
                Coba Lagi
              </Button>
            </div>
          </CardContent>
        </Card>
      </div>
    );
  }

  const { certificate, user, pelatihan } = verifyQuery.data;

  const isExpired = certificate.expiresAt
    ? new Date(certificate.expiresAt).getTime() < new Date().getTime()
    : false;

  return (
    <div className="min-h-screen overflow-y-auto bg-[#F8FAFC] px-4 py-12 font-['Poppins']">
      <div className="mx-auto max-w-2xl space-y-6">
        {/* Verification Success Banner */}
        <Alert className="rounded-2xl border-emerald-500 bg-emerald-50/70 shadow-sm">
          <CheckCircle2 className="h-5 w-5 text-emerald-600" />
          <AlertTitle className="font-['Plus_Jakarta_Sans'] text-sm font-extrabold text-emerald-950">
            Sertifikat Terverifikasi
          </AlertTitle>
          <AlertDescription className="text-xs text-emerald-800">
            Sertifikat ini adalah dokumen resmi yang sah yang dikeluarkan secara
            digital oleh <strong>Tepian K3</strong> untuk peserta yang
            bersangkutan.
          </AlertDescription>
        </Alert>

        {/* Certificate details card */}
        <Card className="overflow-hidden rounded-2xl border-slate-100 shadow-lg">
          <CardHeader className="bg-gradient-to-r from-blue-600 to-indigo-700 p-6 text-white">
            <div className="flex items-start justify-between">
              <div className="space-y-1">
                <div className="flex items-center gap-2">
                  <Award className="h-6 w-6" />
                  <CardTitle className="font-['Plus_Jakarta_Sans'] text-lg font-extrabold">
                    Sertifikat Kelulusan Resmi
                  </CardTitle>
                </div>
                <CardDescription className="font-mono text-xs font-semibold text-blue-100">
                  No. Sertifikat: {certificate.certificateNumber}
                </CardDescription>
              </div>
              <Badge
                className={`text-[10px] text-white ${isExpired ? "bg-rose-500" : "bg-emerald-500"}`}
              >
                {isExpired ? "Kadaluarsa" : "Aktif / Berlaku"}
              </Badge>
            </div>
          </CardHeader>

          <CardContent className="space-y-6 p-6">
            {/* Participant Details */}
            <div className="space-y-3">
              <h3 className="flex items-center gap-1.5 border-b pb-1.5 text-xs font-bold tracking-wider text-slate-800 uppercase">
                <User className="h-4 w-4 text-blue-600" />
                Informasi Penerima
              </h3>
              <div className="grid grid-cols-1 gap-4 md:grid-cols-2">
                <div>
                  <span className="block text-[10px] font-bold text-slate-400 uppercase">
                    Nama Lengkap
                  </span>
                  <span className="text-sm font-bold text-slate-800">
                    {user.name}
                  </span>
                </div>
                <div>
                  <span className="block text-[10px] font-bold text-slate-400 uppercase">
                    Email Peserta
                  </span>
                  <span className="text-xs font-semibold text-slate-600">
                    {user.email}
                  </span>
                </div>
              </div>
            </div>

            {/* Course Details */}
            <div className="space-y-3">
              <h3 className="flex items-center gap-1.5 border-b pb-1.5 text-xs font-bold tracking-wider text-slate-800 uppercase">
                <GraduationCap className="h-4 w-4 text-blue-600" />
                Informasi Pelatihan
              </h3>
              <div className="grid grid-cols-1 gap-4 md:grid-cols-2">
                <div>
                  <span className="block text-[10px] font-bold text-slate-400 uppercase">
                    Nama Program
                  </span>
                  <span className="text-sm font-bold text-slate-800">
                    {pelatihan.title}
                  </span>
                </div>
                <div>
                  <span className="block text-[10px] font-bold text-slate-400 uppercase">
                    Tipe Pelatihan
                  </span>
                  <span className="text-xs font-semibold text-slate-600 capitalize">
                    {pelatihan.type}
                  </span>
                </div>
                <div>
                  <span className="block text-[10px] font-bold text-slate-400 uppercase">
                    Durasi Program
                  </span>
                  <span className="text-xs font-semibold text-slate-600">
                    {pelatihan.duration} Jam
                  </span>
                </div>
                {pelatihan.instructorName && (
                  <div>
                    <span className="block text-[10px] font-bold text-slate-400 uppercase">
                      Instruktur Pengajar
                    </span>
                    <span className="text-xs font-semibold text-slate-600">
                      {pelatihan.instructorName}
                    </span>
                  </div>
                )}
              </div>
            </div>

            {/* Scoring & Validity */}
            <div className="space-y-3">
              <h3 className="flex items-center gap-1.5 border-b pb-1.5 text-xs font-bold tracking-wider text-slate-800 uppercase">
                <Calendar className="h-4 w-4 text-blue-600" />
                Masa Berlaku & Hasil
              </h3>
              <div className="grid grid-cols-1 gap-4 md:grid-cols-2">
                <div>
                  <span className="block text-[10px] font-bold text-slate-400 uppercase">
                    Tanggal Kelulusan
                  </span>
                  <span className="text-xs font-semibold text-slate-700">
                    {new Date(certificate.completionDate).toLocaleDateString(
                      "id-ID",
                      {
                        day: "numeric",
                        month: "long",
                        year: "numeric",
                      },
                    )}
                  </span>
                </div>
                <div>
                  <span className="block text-[10px] font-bold text-slate-400 uppercase">
                    Tanggal Terbit
                  </span>
                  <span className="text-xs font-semibold text-slate-700">
                    {new Date(certificate.issuedAt).toLocaleDateString(
                      "id-ID",
                      {
                        day: "numeric",
                        month: "long",
                        year: "numeric",
                      },
                    )}
                  </span>
                </div>
                {certificate.expiresAt && (
                  <div>
                    <span className="block text-[10px] font-bold text-slate-400 uppercase">
                      Berlaku Sampai
                    </span>
                    <span className="text-xs font-semibold text-slate-700">
                      {new Date(certificate.expiresAt).toLocaleDateString(
                        "id-ID",
                        {
                          day: "numeric",
                          month: "long",
                          year: "numeric",
                        },
                      )}
                    </span>
                  </div>
                )}
                <div>
                  <span className="block text-[10px] font-bold text-slate-400 uppercase">
                    Nilai Kelulusan
                  </span>
                  <span className="rounded bg-blue-50 px-2 py-0.5 text-[10px] font-bold text-blue-700">
                    {certificate.finalScore} / 100
                  </span>
                </div>
              </div>
            </div>

            {/* Actions */}
            <Separator />
            <div className="flex flex-col gap-3 sm:flex-row">
              <Button
                asChild
                className="h-11 flex-1 rounded-xl bg-blue-600 text-xs font-bold text-white hover:bg-blue-700"
              >
                <a
                  href={certificate.documentUrl}
                  target="_blank"
                  rel="noreferrer"
                >
                  <Download className="mr-2 h-4 w-4" /> Download File Sertifikat
                  (PDF)
                </a>
              </Button>
              <Button
                variant="outline"
                asChild
                className="h-11 flex-1 rounded-xl text-xs font-bold text-slate-600"
              >
                <Link to="/">
                  <ExternalLink className="mr-2 h-4 w-4" /> Kunjungi Beranda
                </Link>
              </Button>
            </div>
          </CardContent>
        </Card>

        {/* Verification explanation */}
        <Card className="rounded-2xl border-slate-100 shadow-sm">
          <CardHeader className="p-5">
            <CardTitle className="flex items-center gap-2 text-sm font-bold text-slate-800">
              <Shield className="h-4 w-4 text-blue-600" />
              Sistem Validasi Sertifikat Digital
            </CardTitle>
          </CardHeader>
          <CardContent className="space-y-2 px-5 pb-5 text-xs leading-relaxed text-slate-500">
            <p>
              Tepian K3 menerapkan sistem tanda tangan digital dan verifikasi
              token kriptografis. Kode unik yang tertanam pada tautan dan QR
              Code sertifikat memastikan bahwa berkas ini benar-benar
              dikeluarkan oleh Tepian K3 dan bebas dari modifikasi ilegal.
            </p>
            <p>
              Apabila terdapat perbedaan nama, nilai, atau program pelatihan
              antara lembar verifikasi ini dengan lembar cetak sertifikat fisik,
              maka data pada lembar verifikasi online ini adalah yang dianggap
              benar dan sah.
            </p>
          </CardContent>
        </Card>

        {/* Legal Disclaimer */}
        <Alert className="rounded-2xl">
          <AlertTriangle className="h-4 w-4 text-amber-500" />
          <AlertTitle className="text-xs font-bold text-slate-800">
            Keabsahan Dokumen Elektronik
          </AlertTitle>
          <AlertDescription className="mt-1 text-[10px] leading-relaxed text-slate-500">
            Verifikasi ini merupakan bentuk keabsahan dokumen elektronik sesuai
            dengan Undang-Undang Republik Indonesia Nomor 11 Tahun 2008 tentang
            Informasi dan Transaksi Elektronik (UU ITE).
          </AlertDescription>
        </Alert>
      </div>
    </div>
  );
}
