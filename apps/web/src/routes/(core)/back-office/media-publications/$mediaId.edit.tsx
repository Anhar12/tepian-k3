// ##################
// authored (generated by claude, Jun 14 2026 20:55 WITA)
// ##################

import { createFileRoute } from "@tanstack/react-router";
import { pageHead } from "@/utils/page-head";
import { requirePermission } from "@/utils/require-permission";
import { useRedirectBackWithTimeout } from "@/lib/redirect-back-with-timeout";
import { Controller, useForm } from "react-hook-form";
import { z } from "zod";
import mediaPublicationsSchema from "@tepian-k3/schema/platform/media-publications.schema";
import { zodResolver } from "@hookform/resolvers/zod";
import { queryClient, trpc } from "@/utils/trpc";
import { useMutation, useQuery } from "@tanstack/react-query";
import { globalErrorToast, globalSuccessToast } from "@/lib/toast";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import {
  Field,
  FieldError,
  FieldGroup,
  FieldLabel,
} from "@/components/ui/field";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Spinner } from "@/components/ui/spinner";
import { toFormData } from "@/utils/form-data-mapper";
import SingleImageUpload from "@/components/ui/single-image-upload";
import { Checkbox } from "@/components/ui/checkbox";
import { Textarea } from "@/components/ui/textarea";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover";
import { cn } from "@/lib/utils";
import { format } from "date-fns";
import { CalendarIcon } from "lucide-react";
import { Calendar } from "@/components/ui/calendar";
import {
  SkeletonButton,
  SkeletonImageUpload,
  SkeletonInput,
} from "@/components/ui/skeleton-generator";

export const Route = createFileRoute(
  "/(core)/back-office/media-publications/$mediaId/edit",
)({
  params: z.object({
    mediaId: z.uuidv7(),
  }),
  beforeLoad: async ({ context }) =>
    await requirePermission(context, {
      permission: "media-publications.update",
    }),
  component: RouteComponent,
  head: () => pageHead("Edit Media & Publikasi"),
});

/**
 * Komponen halaman formulir penyuntingan Media & Publikasi di Back Office.
 * Memuat data yang sudah ada berdasarkan parameters ID dan memungkinkan
 * admin untuk melakukan pengkinian informasi beserta thumbnail gambar.
 *
 * @returns {JSX.Element} Elemen halaman Back Office penyuntingan Media & Publikasi.
 */
function RouteComponent() {
  const { mediaId } = Route.useParams();
  const redirectBack = useRedirectBackWithTimeout();

  const { data: media, isLoading } = useQuery(
    trpc.platform.mediaPublications.getMediaById.queryOptions({ id: mediaId }),
  );

  const form = useForm<
    z.infer<typeof mediaPublicationsSchema.updateMediaSchema>
  >({
    resolver: zodResolver(mediaPublicationsSchema.updateMediaSchema),
    values: {
      id: mediaId,
      title: media?.title ?? "",
      description: media?.description ?? "",
      content: media?.content ?? "",
      category: media?.category ?? "media",
      publisher: media?.publisher ?? "Balai K3 Samarinda",
      isPublished: media?.isPublished,
      publishedAt: media?.publishedAt
        ? format(new Date(media.publishedAt), "yyyy-MM-dd'T'HH:mm")
        : undefined,
    },
  });

  const updateMediaMutation = useMutation({
    ...trpc.platform.mediaPublications.updateMedia.mutationOptions(),
    onSuccess: async () => {
      await queryClient.invalidateQueries(
        trpc.platform.mediaPublications.getMediaById.queryOptions({
          id: mediaId,
        }),
      );
      globalSuccessToast("Media dan publikasi berhasil diperbarui");
      await redirectBack();
    },
    onError: (error) => {
      globalErrorToast(
        `Gagal memperbarui media dan publikasi: ${error.message}`,
      );
    },
  });

  const handleSubmit = (
    data: z.infer<typeof mediaPublicationsSchema.updateMediaSchema>,
  ) => {
    const formData = toFormData(data);
    updateMediaMutation.mutate(formData);
  };

  if (isLoading) {
    return (
      <div className="flex flex-col gap-6">
        <Card>
          <CardHeader>
            <CardTitle>Perbarui Media & Publikasi</CardTitle>
            <CardDescription>
              Isi formulir di bawah untuk memperbarui informasi.
            </CardDescription>
          </CardHeader>
          <CardContent>
            <div className="flex flex-col gap-4">
              {Array.from({ length: 5 }).map((_, index) =>
                index === 0 ? (
                  <SkeletonImageUpload key={index} />
                ) : (
                  <SkeletonInput className="w-full" key={index} />
                ),
              )}
              <SkeletonButton className="w-full" />
            </div>
          </CardContent>
        </Card>
      </div>
    );
  }

  return (
    <div className="flex flex-col gap-6">
      <Card>
        <CardHeader>
          <CardTitle>Perbarui Media & Publikasi</CardTitle>
          <CardDescription>
            Isi formulir di bawah untuk memperbarui informasi.
          </CardDescription>
        </CardHeader>
        <CardContent>
          <form
            onSubmit={form.handleSubmit(handleSubmit)}
            className="flex flex-col gap-4"
          >
            <FieldGroup>
              <Controller
                control={form.control}
                name="image"
                render={({ field, fieldState }) => (
                  <Field
                    data-invalid={fieldState.invalid}
                    className="space-y-1"
                  >
                    <FieldLabel className="font-bold">
                      Gambar Thumbnail
                    </FieldLabel>
                    <SingleImageUpload
                      {...field}
                      value={field.value || media?.imageUrl}
                      error={fieldState.error?.message}
                    />
                  </Field>
                )}
              />

              <Controller
                name="title"
                control={form.control}
                render={({ field, fieldState }) => (
                  <Field
                    data-invalid={fieldState.invalid}
                    className="space-y-1"
                  >
                    <FieldLabel className="font-bold">Judul</FieldLabel>
                    <Input placeholder="Masukkan judul" {...field} />
                    {fieldState.invalid && (
                      <FieldError errors={[fieldState.error]} />
                    )}
                  </Field>
                )}
              />

              <Controller
                name="category"
                control={form.control}
                render={({ field, fieldState }) => (
                  <Field
                    data-invalid={fieldState.invalid}
                    className="space-y-1"
                  >
                    <FieldLabel className="font-bold">Kategori</FieldLabel>
                    <Select
                      name={field.name}
                      value={field.value}
                      onValueChange={field.onChange}
                    >
                      <SelectTrigger
                        className="w-full"
                        aria-invalid={fieldState.invalid}
                      >
                        <SelectValue placeholder="Pilih Kategori" />
                      </SelectTrigger>
                      <SelectContent>
                        <SelectItem value="media">Media</SelectItem>
                        <SelectItem value="publikasi">Publikasi</SelectItem>
                      </SelectContent>
                    </Select>
                    {fieldState.invalid && (
                      <FieldError errors={[fieldState.error]} />
                    )}
                  </Field>
                )}
              />

              <Controller
                name="publisher"
                control={form.control}
                render={({ field, fieldState }) => (
                  <Field
                    data-invalid={fieldState.invalid}
                    className="space-y-1"
                  >
                    <FieldLabel className="font-bold">Penerbit</FieldLabel>
                    <Input placeholder="Masukkan nama penerbit" {...field} />
                    {fieldState.invalid && (
                      <FieldError errors={[fieldState.error]} />
                    )}
                  </Field>
                )}
              />

              <Controller
                name="description"
                control={form.control}
                render={({ field, fieldState }) => (
                  <Field
                    data-invalid={fieldState.invalid}
                    className="space-y-1"
                  >
                    <FieldLabel className="font-bold">
                      Ringkasan (Deskripsi Singkat)
                    </FieldLabel>
                    <Textarea
                      placeholder="Masukkan ringkasan singkat"
                      className="min-h-20"
                      {...field}
                      value={field.value ?? ""}
                    />
                    {fieldState.invalid && (
                      <FieldError errors={[fieldState.error]} />
                    )}
                  </Field>
                )}
              />

              <Controller
                name="content"
                control={form.control}
                render={({ field, fieldState }) => (
                  <Field
                    data-invalid={fieldState.invalid}
                    className="space-y-1"
                  >
                    <FieldLabel className="font-bold">
                      Konten Lengkap
                    </FieldLabel>
                    <Textarea
                      placeholder="Masukkan isi lengkap konten"
                      className="min-h-48"
                      {...field}
                    />
                    {fieldState.invalid && (
                      <FieldError errors={[fieldState.error]} />
                    )}
                  </Field>
                )}
              />

              <Controller
                control={form.control}
                name="isPublished"
                render={({ field, fieldState }) => (
                  <Field
                    data-invalid={fieldState.invalid}
                    className="space-y-1"
                    orientation="horizontal"
                  >
                    <Checkbox
                      id="form-isPublished"
                      name={field.name}
                      aria-invalid={fieldState.invalid}
                      checked={field.value}
                      onCheckedChange={field.onChange}
                    />
                    <FieldLabel
                      htmlFor="form-isPublished"
                      className="font-normal"
                    >
                      Publish langsung setelah dibuat
                    </FieldLabel>
                  </Field>
                )}
              />

              <Controller
                control={form.control}
                name="publishedAt"
                render={({ field, fieldState }) => (
                  <Field
                    data-invalid={fieldState.invalid}
                    className="space-y-1"
                  >
                    <FieldLabel className="font-bold">
                      Tanggal Publikasi (Opsional)
                    </FieldLabel>
                    <Popover>
                      <PopoverTrigger asChild>
                        <Button
                          variant="outline"
                          className={cn(
                            "w-60 pl-3 text-left font-normal",
                            !field.value && "text-muted-foreground",
                          )}
                        >
                          {field.value
                            ? format(new Date(field.value), "PPP")
                            : "Pilih tanggal publikasi"}
                          <CalendarIcon className="ml-auto h-4 w-4 opacity-50" />
                        </Button>
                      </PopoverTrigger>
                      <PopoverContent className="w-auto p-0" align="start">
                        <Calendar
                          mode="single"
                          selected={
                            field.value ? new Date(field.value) : undefined
                          }
                          onSelect={(value) => {
                            field.onChange(value?.toISOString() ?? null);
                          }}
                          autoFocus
                        />
                      </PopoverContent>
                    </Popover>
                    {fieldState.invalid && (
                      <FieldError errors={[fieldState.error]} />
                    )}
                  </Field>
                )}
              />
            </FieldGroup>

            <Button
              type="submit"
              className="mt-4"
              disabled={updateMediaMutation.isPending}
            >
              {updateMediaMutation.isPending ? <Spinner /> : null}
              Simpan Perubahan
            </Button>
          </form>
        </CardContent>
      </Card>
    </div>
  );
}

// ##################
// end authored
// ##################
