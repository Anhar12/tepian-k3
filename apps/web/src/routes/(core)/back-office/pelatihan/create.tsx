// ##################
// authored (generated by Gemini 3.5 Flash, Jun 01 2026 14:30 WITA)
// ##################

import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { useState, useCallback, useMemo, useEffect } from "react";
import { useForm, type UseFormReturn } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { useMutation, useQuery } from "@tanstack/react-query";
import { trpc, queryClient } from "@/utils/trpc";
import { pageHead } from "@/utils/page-head";
import { requirePermission } from "@/utils/require-permission";
import { globalSuccessToast, globalErrorToast } from "@/lib/toast";
import { getPublicUrl } from "@/utils/url";
import pelatihanSchema from "@tepian-k3/schema/pelatihan/pelatihan.schema";
import z from "zod";
import ImageWithFallback from "@/components/image-with-fallback";
import { Checkbox } from "@/components/ui/checkbox";
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
  FormDescription,
} from "@/components/ui/form";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Button } from "@/components/ui/button";
import { Switch } from "@/components/ui/switch";
import { Label } from "@/components/ui/label";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";
import {
  IconPlus,
  IconFileTypePdf,
  IconPresentation,
  IconVideo,
  IconFile,
  IconLink,
  IconEdit,
  IconTrash,
  IconFolder,
  IconFolderOpen,
  IconChevronDown,
  IconChevronRight,
  IconCheck,
  IconLoader,
} from "@tabler/icons-react";
import { cn } from "@/lib/utils";
import { v7 as uuidv7 } from "uuid";

// ─── Route Definition ───────────────────────────────────────────────────────

export const Route = createFileRoute("/(core)/back-office/pelatihan/create")({
  validateSearch: z.object({
    type: z.enum(["elearning", "bimtek", "webinar"]).optional(),
  }),
  beforeLoad: async ({ context }) =>
    await requirePermission(context, { permission: "pelatihan.create" }),
  component: RouteComponent,
  head: () => pageHead("Buat Kursus Baru"),
});

// ─── Types ───────────────────────────────────────────────────────────────────

type CreatePelatihanValues = z.infer<
  typeof pelatihanSchema.createPelatihanSchema
>;

interface LocalMaterial {
  id: string;
  title: string;
  description: string | null;
  type: "ppt" | "pdf" | "video" | "document" | "link";
  fileUrl: string | null;
  fileSize: number | null;
  duration: number | null;
  isPreview: boolean;
}

interface LocalModule {
  id: string;
  title: string;
  description: string | null;
  materials: LocalMaterial[];
}

// ─── Step Configuration ──────────────────────────────────────────────────────

const ELEARNING_STEPS = [
  {
    number: 1,
    title: "Informasi Dasar",
    description: "Detail utama kursus",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
        <polyline points="14,2 14,8 20,8" />
        <line x1="16" y1="13" x2="8" y2="13" />
        <line x1="16" y1="17" x2="8" y2="17" />
        <polyline points="10,9 9,9 8,9" />
      </svg>
    ),
  },
  {
    number: 2,
    title: "Deskripsi Kursus",
    description: "Jelaskan tentang kursus",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" />
      </svg>
    ),
  },
  {
    number: 3,
    title: "Pengaturan Course",
    description: "Level, durasi, nilai kelulusan",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <rect x="3" y="4" width="18" height="18" rx="2" ry="2" />
        <line x1="16" y1="2" x2="16" y2="6" />
        <line x1="8" y1="2" x2="8" y2="6" />
        <line x1="3" y1="10" x2="21" y2="10" />
      </svg>
    ),
  },
  {
    number: 4,
    title: "Gambar & Media",
    description: "Thumbnail dan media kursus",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <rect x="3" y="3" width="18" height="18" rx="2" ry="2" />
        <circle cx="8.5" cy="8.5" r="1.5" />
        <polyline points="21 15 16 10 5 21" />
      </svg>
    ),
  },
  {
    number: 5,
    title: "Materi & Quiz",
    description: "Tambahkan materi pembelajaran",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z" />
        <line x1="12" y1="11" x2="12" y2="17" />
        <line x1="9" y1="14" x2="15" y2="14" />
      </svg>
    ),
  },
  {
    number: 6,
    title: "Persyaratan Pendaftaran",
    description: "Centang persyaratan dokumen",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2.5"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <path d="M9 12l2 2 4-4m5.6 1.7a9 9 0 11-18.4 0 9 9 0 0118.4 0z" />
      </svg>
    ),
  },
  {
    number: 7,
    title: "Review & Publikasi",
    description: "Tinjau dan publikasikan",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <polyline points="9 11 12 14 22 4" />
        <path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11" />
      </svg>
    ),
  },
];

const BIMTEK_STEPS = [
  {
    number: 1,
    title: "Informasi Dasar",
    description: "Detail utama Bimtek",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
        <polyline points="14,2 14,8 20,8" />
        <line x1="16" y1="13" x2="8" y2="13" />
        <line x1="16" y1="17" x2="8" y2="17" />
        <polyline points="10,9 9,9 8,9" />
      </svg>
    ),
  },
  {
    number: 2,
    title: "Deskripsi Pelatihan",
    description: "Jelaskan tentang program",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" />
      </svg>
    ),
  },
  {
    number: 3,
    title: "Jadwal & Fasilitas",
    description: "Tanggal, tempat & akomodasi",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <rect x="3" y="4" width="18" height="18" rx="2" ry="2" />
        <line x1="16" y1="2" x2="16" y2="6" />
        <line x1="8" y1="2" x2="8" y2="6" />
        <line x1="3" y1="10" x2="21" y2="10" />
      </svg>
    ),
  },
  {
    number: 4,
    title: "Gambar & Media",
    description: "Thumbnail & brosur Bimtek",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <rect x="3" y="3" width="18" height="18" rx="2" ry="2" />
        <circle cx="8.5" cy="8.5" r="1.5" />
        <polyline points="21 15 16 10 5 21" />
      </svg>
    ),
  },
  {
    number: 5,
    title: "Persyaratan Pendaftaran",
    description: "Centang persyaratan dokumen",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2.5"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <path d="M9 12l2 2 4-4m5.6 1.7a9 9 0 11-18.4 0 9 9 0 0118.4 0z" />
      </svg>
    ),
  },
  {
    number: 6,
    title: "Review & Publikasi",
    description: "Tinjau jadwal & publikasikan",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <polyline points="9 11 12 14 22 4" />
        <path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11" />
      </svg>
    ),
  },
];

const WEBINAR_STEPS = [
  {
    number: 1,
    title: "Informasi Dasar",
    description: "Detail pelatihan",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
        <polyline points="14,2 14,8 20,8" />
        <line x1="16" y1="13" x2="8" y2="13" />
        <line x1="16" y1="17" x2="8" y2="17" />
        <polyline points="10,9 9,9 8,9" />
      </svg>
    ),
  },
  {
    number: 2,
    title: "Deskripsi Pelatihan",
    description: "Jelaskan tentang program",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" />
      </svg>
    ),
  },
  {
    number: 3,
    title: "Pengaturan",
    description: "Level, durasi, sertifikat, dll",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <rect x="3" y="4" width="18" height="18" rx="2" ry="2" />
        <line x1="16" y1="2" x2="16" y2="6" />
        <line x1="8" y1="2" x2="8" y2="6" />
        <line x1="3" y1="10" x2="21" y2="10" />
      </svg>
    ),
  },
  {
    number: 4,
    title: "Gambar & Media",
    description: "Thumbnail dan media kursus",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <rect x="3" y="3" width="18" height="18" rx="2" ry="2" />
        <circle cx="8.5" cy="8.5" r="1.5" />
        <polyline points="21 15 16 10 5 21" />
      </svg>
    ),
  },
  {
    number: 5,
    title: "Jadwal & materi",
    description: "Tambahkan materi pembelajaran",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z" />
        <line x1="12" y1="11" x2="12" y2="17" />
        <line x1="9" y1="14" x2="15" y2="14" />
      </svg>
    ),
  },
  {
    number: 6,
    title: "Persyaratan Pendaftaran",
    description: "Centang persyaratan dokumen",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2.5"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <path d="M9 12l2 2 4-4m5.6 1.7a9 9 0 11-18.4 0 9 9 0 0118.4 0z" />
      </svg>
    ),
  },
  {
    number: 7,
    title: "Review & Publikasi",
    description: "Tinjau dan publikasikan",
    icon: (
      <svg
        width="20"
        height="20"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        <polyline points="9 11 12 14 22 4" />
        <path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11" />
      </svg>
    ),
  },
];

const typeConfig: Record<
  string,
  { label: string; Icon: typeof IconFile; color: string; bg: string }
> = {
  video: {
    label: "Video",
    Icon: IconVideo,
    color: "text-red-600",
    bg: "bg-red-50",
  },
  pdf: {
    label: "PDF",
    Icon: IconFileTypePdf,
    color: "text-orange-600",
    bg: "bg-orange-50",
  },
  ppt: {
    label: "PPT",
    Icon: IconPresentation,
    color: "text-yellow-600",
    bg: "bg-yellow-50",
  },
  document: {
    label: "Dokumen",
    Icon: IconFile,
    color: "text-blue-600",
    bg: "bg-blue-50",
  },
  link: {
    label: "Link",
    Icon: IconLink,
    color: "text-violet-600",
    bg: "bg-violet-50",
  },
};

function formatFileSize(bytes: number | null) {
  if (!bytes) return null;
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(0)} KB`;
  return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
}

// ─── Sidebar Step Indicator ───────────────────────────────────────────────────

// ─── Sidebar Step Indicator ───────────────────────────────────────────────────

function WizardSidebar({
  currentStep,
  completedSteps,
  steps,
  type,
}: {
  currentStep: number;
  completedSteps: Set<number>;
  steps: typeof ELEARNING_STEPS;
  type: string;
}) {
  return (
    <aside className="flex w-[280px] min-w-[280px] shrink-0 flex-col justify-between border-r border-slate-100 bg-white p-6">
      <div>
        <div className="mb-6">
          <div className="mb-1 flex items-center gap-2">
            <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-[#1061D6]">
              <svg
                width="16"
                height="16"
                viewBox="0 0 24 24"
                fill="none"
                stroke="white"
                strokeWidth="2.5"
              >
                <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5" />
              </svg>
            </div>
            <span className="text-sm font-bold text-[#334155]">Tepian K3</span>
          </div>
          <p className="pl-10 text-xs font-medium text-[#768094]">
            {type === "bimtek"
              ? "Manajemen Bimtek"
              : type === "webinar"
                ? "Manajemen Webinar"
                : "Manajemen E-Learning"}
          </p>
        </div>

        {/* Step List */}
        <div className="flex flex-col gap-1">
          {steps.map((step, idx) => {
            const isCompleted = completedSteps.has(step.number);
            const isActive = currentStep === step.number;

            return (
              <div key={step.number} className="relative">
                {idx < steps.length - 1 && (
                  <div
                    className={cn(
                      "absolute top-[40px] left-[22px] h-6 w-0.5 transition-colors duration-300",
                      isCompleted ? "bg-[#1061D6]" : "bg-slate-100",
                    )}
                  />
                )}

                <div
                  className={cn(
                    "flex items-start gap-3 rounded-xl p-2.5 transition-all duration-200",
                    isActive
                      ? "border border-blue-100/70 bg-blue-50"
                      : "hover:bg-slate-50",
                  )}
                >
                  <div
                    className={cn(
                      "flex h-7 w-7 shrink-0 items-center justify-center rounded-full text-xs font-bold transition-all duration-300",
                      isCompleted
                        ? "bg-[#1061D6] text-white shadow-[0_0_0_3px_rgba(16,97,214,0.15)]"
                        : isActive
                          ? "border-2 border-[#1061D6] bg-white text-[#1061D6] shadow-[0_0_0_3px_rgba(16,97,214,0.15)]"
                          : "border-2 border-transparent bg-slate-100 text-slate-400",
                    )}
                  >
                    {isCompleted ? (
                      <IconCheck className="size-4" strokeWidth={3} />
                    ) : (
                      step.number
                    )}
                  </div>

                  <div className="flex min-w-0 flex-col pt-0.5">
                    <span
                      className={cn(
                        "text-xs font-bold transition-colors",
                        isActive
                          ? "text-[#1061D6]"
                          : isCompleted
                            ? "text-[#334155]"
                            : "text-slate-400",
                      )}
                    >
                      {step.title}
                    </span>
                    <span className="mt-0.5 text-[10px] leading-tight font-medium text-[#768094]">
                      {step.description}
                    </span>
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        {/* Tips Box */}
        <div className="mt-8 flex flex-col gap-2 rounded-xl border border-blue-100/50 bg-[#F0F5FF]/80 p-4 shadow-sm">
          <div className="flex items-center gap-2 text-[#1061D6]">
            <svg
              width="14"
              height="14"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              strokeWidth="2.5"
              strokeLinecap="round"
              strokeLinejoin="round"
              className="shrink-0"
            >
              <circle cx="12" cy="12" r="10" />
              <line x1="12" y1="16" x2="12" y2="12" />
              <line x1="12" y1="8" x2="12.01" y2="8" />
            </svg>
            <span className="text-[10px] font-bold tracking-wide uppercase">
              Tips
            </span>
          </div>
          <p className="text-[10px] leading-relaxed font-medium text-[#768094]">
            Pastikan semua informasi sudah diisi dengan benar sebelum
            mempublikasikan kursus.
          </p>
        </div>
      </div>

      <div className="border-t border-slate-100 pt-4">
        <Link
          to="/back-office/pelatihan"
          className="flex items-center gap-2 text-xs font-semibold text-[#768094] transition-colors hover:text-[#1061D6]"
        >
          <svg
            width="14"
            height="14"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="2.5"
            strokeLinecap="round"
            strokeLinejoin="round"
          >
            <line x1="19" y1="12" x2="5" y2="12" />
            <polyline points="12,19 5,12 12,5" />
          </svg>
          <span>Kembali</span>
        </Link>
      </div>
    </aside>
  );
}

// ─── Step Components ──────────────────────────────────────────────────────────

function Step1InformasiDasar({
  form,
  categories,
  categoriesLoading,
}: {
  form: UseFormReturn<CreatePelatihanValues>;
  categories: any[];
  categoriesLoading: boolean;
}) {
  // ##################
  // authored (generated by gemini, Jun 09 2026 07:15 WITA)
  // ##################
  /**
   * Generates a URL-safe slug from a training title, prefixed with the training type.
   * This prevents 409 Conflict errors when different training types share the same title.
   * Example: "Dasar K3" + type "bimtek" → "bimtek-dasar-k3"
   */
  const autoSlug = (title: string, type?: string) => {
    const baseSlug = title
      .toLowerCase()
      .replace(/[^a-z0-9\s-]/g, "")
      .trim()
      .replace(/\s+/g, "-");
    const currentType = type ?? form.getValues("type") ?? "elearning";
    return `${currentType}-${baseSlug}`;
  };
  // ##################
  // end authored
  // ##################

  return (
    <div className="flex flex-col gap-5">
      <div>
        <h2 className="text-lg font-bold text-[#334155]">Informasi Dasar</h2>
        <p className="mt-0.5 text-xs text-[#768094]">
          Lengkapi informasi utama untuk mengidentifikasi kursus Anda
        </p>
      </div>

      <FormField
        control={form.control}
        name="title"
        render={({ field }) => (
          <FormItem>
            <FormLabel className="text-xs font-bold text-[#475569]">
              Judul Kursus / Program <span className="text-red-500">*</span>
            </FormLabel>
            <FormControl>
              <Input
                placeholder="Contoh: Dasar Keselamatan & Kesehatan Kerja"
                className="h-10 rounded-lg border-slate-200 text-xs focus:border-[#1061D6]"
                {...field}
                onChange={(e) => {
                  field.onChange(e.target.value);
                  // Auto slug generation - slug is always hidden/internal now
                  form.setValue("slug", autoSlug(e.target.value), {
                    shouldValidate: true,
                  });
                }}
              />
            </FormControl>
            <FormMessage className="text-[10px]" />
          </FormItem>
        )}
      />

      <div className="grid grid-cols-2 gap-4">
        <FormField
          control={form.control}
          name="type"
          render={({ field }) => (
            <FormItem>
              <FormLabel className="text-xs font-bold text-[#475569]">
                Tipe Kursus <span className="text-red-500">*</span>
              </FormLabel>
              <Select
                onValueChange={(newType) => {
                  field.onChange(newType);
                  const currentTitle = form.getValues("title");
                  if (currentTitle) {
                    form.setValue("slug", autoSlug(currentTitle, newType), {
                      shouldValidate: true,
                    });
                  }
                }}
                value={field.value}
              >
                <FormControl>
                  <SelectTrigger className="h-10 rounded-lg border-slate-200 text-xs">
                    <SelectValue placeholder="Pilih Tipe" />
                  </SelectTrigger>
                </FormControl>
                <SelectContent>
                  <SelectItem value="elearning">
                    <div className="flex items-center gap-2 text-xs">
                      <div className="h-1.5 w-1.5 rounded-full bg-blue-500" />
                      E-Learning (Mandiri)
                    </div>
                  </SelectItem>
                  <SelectItem value="bimtek">
                    <div className="flex items-center gap-2 text-xs">
                      <div className="h-1.5 w-1.5 rounded-full bg-purple-500" />
                      Bimtek (Terjadwal)
                    </div>
                  </SelectItem>
                  <SelectItem value="webinar">
                    <div className="flex items-center gap-2 text-xs">
                      <div className="h-1.5 w-1.5 rounded-full bg-orange-500" />
                      Webinar (Online)
                    </div>
                  </SelectItem>
                </SelectContent>
              </Select>
              <FormMessage className="text-[10px]" />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="categoryId"
          render={({ field }) => (
            <FormItem>
              <FormLabel className="text-xs font-bold text-[#475569]">
                Kategori (Opsional)
              </FormLabel>
              <Select
                onValueChange={(val) =>
                  field.onChange(val === "none" ? null : val)
                }
                value={field.value ?? "none"}
              >
                <FormControl>
                  <SelectTrigger className="h-10 rounded-lg border-slate-200 text-xs">
                    <SelectValue
                      placeholder={
                        categoriesLoading ? "Memuat..." : "Pilih Kategori"
                      }
                    />
                  </SelectTrigger>
                </FormControl>
                <SelectContent>
                  <SelectItem value="none" className="text-xs">
                    Tidak Dipilih
                  </SelectItem>
                  {categories.map((cat) => (
                    <SelectItem key={cat.id} value={cat.id} className="text-xs">
                      {cat.name}
                    </SelectItem>
                  ))}
                </SelectContent>
              </Select>
              <FormMessage className="text-[10px]" />
            </FormItem>
          )}
        />
      </div>

      <FormField
        control={form.control}
        name="level"
        render={({ field }) => (
          <FormItem>
            <FormLabel className="text-xs font-bold text-[#475569]">
              Tingkat Kesulitan (Opsional)
            </FormLabel>
            <Select
              onValueChange={(val) =>
                field.onChange(val === "none" ? null : val)
              }
              value={field.value ?? "none"}
            >
              <FormControl>
                <SelectTrigger className="h-10 rounded-lg border-slate-200 text-xs">
                  <SelectValue placeholder="Pilih Level" />
                </SelectTrigger>
              </FormControl>
              <SelectContent>
                <SelectItem value="none" className="text-xs">
                  Tidak Dipilih
                </SelectItem>
                <SelectItem value="beginner" className="text-xs">
                  Pemula (Beginner)
                </SelectItem>
                <SelectItem value="intermediate" className="text-xs">
                  Menengah (Intermediate)
                </SelectItem>
                <SelectItem value="advanced" className="text-xs">
                  Lanjutan (Advanced)
                </SelectItem>
              </SelectContent>
            </Select>
            <FormMessage className="text-[10px]" />
          </FormItem>
        )}
      />
    </div>
  );
}

function Step2Deskripsi({
  form,
  benefits,
  setBenefits,
}: {
  form: UseFormReturn<CreatePelatihanValues>;
  benefits: string[];
  setBenefits: React.Dispatch<React.SetStateAction<string[]>>;
}) {
  const handleAddBenefit = () => setBenefits([...benefits, ""]);
  const handleRemoveBenefit = (index: number) => {
    if (benefits.length > 1) {
      setBenefits(benefits.filter((_, i) => i !== index));
    } else {
      setBenefits([""]);
    }
  };
  const handleBenefitChange = (index: number, val: string) => {
    setBenefits(benefits.map((b, i) => (i === index ? val : b)));
  };

  return (
    <div className="flex flex-col gap-5">
      <div>
        <h2 className="text-lg font-bold text-[#334155]">
          Deskripsi Kursus / Pelatihan
        </h2>
        <p className="mt-0.5 text-xs text-[#768094]">
          Jelaskan tentang isi, kompetensi, dan manfaat pelatihan ini
        </p>
      </div>

      <FormField
        control={form.control}
        name="description"
        render={({ field }) => (
          <FormItem>
            <FormLabel className="text-xs font-bold text-[#475569]">
              Deskripsi Lengkap <span className="text-red-500">*</span>
            </FormLabel>
            <FormControl>
              <div className="overflow-hidden rounded-xl border border-slate-200 bg-[#F8FAFC]">
                {/* Cosmetic Rich Text Toolbar */}
                <div className="flex items-center gap-1 border-b border-slate-200 bg-white p-2">
                  <button
                    type="button"
                    className="rounded p-1.5 text-slate-500 hover:bg-slate-100 hover:text-slate-800"
                    title="Bold"
                  >
                    <span className="text-xs font-bold">B</span>
                  </button>
                  <button
                    type="button"
                    className="rounded p-1.5 text-slate-500 hover:bg-slate-100 hover:text-slate-800"
                    title="Italic"
                  >
                    <span className="text-xs italic">I</span>
                  </button>
                  <button
                    type="button"
                    className="rounded p-1.5 text-slate-500 hover:bg-slate-100 hover:text-slate-800"
                    title="Underline"
                  >
                    <span className="text-xs underline">U</span>
                  </button>
                  <div className="mx-1 h-4 w-px bg-slate-200" />
                  <button
                    type="button"
                    className="rounded p-1.5 text-slate-500 hover:bg-slate-100 hover:text-slate-800"
                    title="Bulleted List"
                  >
                    <svg
                      className="h-4 w-4"
                      fill="none"
                      viewBox="0 0 24 24"
                      stroke="currentColor"
                      strokeWidth="2"
                    >
                      <line x1="9" y1="6" x2="20" y2="6" />
                      <line x1="9" y1="12" x2="20" y2="12" />
                      <line x1="9" y1="18" x2="20" y2="18" />
                      <circle cx="5" cy="6" r="1" fill="currentColor" />
                      <circle cx="5" cy="12" r="1" fill="currentColor" />
                      <circle cx="5" cy="18" r="1" fill="currentColor" />
                    </svg>
                  </button>
                  <button
                    type="button"
                    className="rounded p-1.5 text-slate-500 hover:bg-slate-100 hover:text-slate-800"
                    title="Numbered List"
                  >
                    <svg
                      className="h-4 w-4"
                      fill="none"
                      viewBox="0 0 24 24"
                      stroke="currentColor"
                      strokeWidth="2"
                    >
                      <line x1="10" y1="6" x2="20" y2="6" />
                      <line x1="10" y1="12" x2="20" y2="12" />
                      <line x1="10" y1="18" x2="20" y2="18" />
                    </svg>
                  </button>
                </div>
                <Textarea
                  placeholder="Tulis silabus secara ringkas, target audiens, prasyarat, dan tujuan pembelajaran agar calon peserta tertarik mendaftar..."
                  className="min-h-[160px] w-full resize-none border-none bg-transparent p-4 text-xs focus:ring-0 focus:outline-none focus-visible:ring-0"
                  {...field}
                  value={field.value || ""}
                />
              </div>
            </FormControl>
            <div className="flex justify-between text-[10px] text-slate-400">
              <span>Masukkan penjelasan lengkap mengenai pelatihan ini.</span>
              <span>{(field.value || "").length} karakter</span>
            </div>
            <FormMessage className="text-[10px]" />
          </FormItem>
        )}
      />

      {/* Benefit Section from Figma */}
      <div className="mt-3 space-y-3">
        <div className="flex items-center justify-between">
          <div>
            <Label className="text-xs font-bold text-[#475569]">
              Benefit Pelatihan (Opsional)
            </Label>
            <p className="text-[10px] text-slate-400">
              Manfaat yang akan didapatkan peserta setelah mengikuti program ini
            </p>
          </div>
          <Button
            type="button"
            variant="outline"
            size="sm"
            onClick={handleAddBenefit}
            className="h-8 rounded-lg border-blue-200 text-xs font-semibold text-[#1061D6] hover:bg-blue-50"
          >
            <IconPlus className="mr-1 size-3.5" />
            Tambah Benefit
          </Button>
        </div>

        <div className="space-y-2">
          {benefits.map((benefit, index) => (
            <div key={index} className="flex items-center gap-2">
              <span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-blue-50 text-xs font-bold text-[#1061D6]">
                {index + 1}
              </span>
              <Input
                placeholder="Contoh: Mendapatkan sertifikat kelulusan kompetensi K3"
                value={benefit}
                onChange={(e) => handleBenefitChange(index, e.target.value)}
                className="h-10 flex-1 rounded-lg border-slate-200 text-xs focus:border-[#1061D6]"
              />
              <Button
                type="button"
                variant="ghost"
                size="icon"
                onClick={() => handleRemoveBenefit(index)}
                className="h-10 w-10 text-slate-400 hover:bg-red-50 hover:text-red-500"
                title="Hapus"
              >
                <IconTrash className="size-4" />
              </Button>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}

function Step3PengaturanCourse({
  form,
  dbPelatihan,
  pelatihanLoading,
  hasCertificate,
  setHasCertificate,
}: {
  form: UseFormReturn<CreatePelatihanValues>;
  dbPelatihan: any;
  pelatihanLoading: boolean;
  hasCertificate: boolean;
  setHasCertificate: (val: boolean) => void;
}) {
  return (
    <div className="flex flex-col gap-5">
      <div>
        <h2 className="text-lg font-bold text-[#334155]">Pengaturan Course</h2>
        <p className="mt-0.5 text-xs text-[#768094]">
          Atur parameter penilaian, jangka waktu belajar, dan pendidik
        </p>
      </div>

      <div className="grid grid-cols-2 gap-4">
        <FormField
          control={form.control}
          name="duration"
          render={({ field }) => (
            <FormItem>
              <FormLabel className="text-xs font-bold text-[#475569]">
                Jangka Waktu Akses (Hari){" "}
                <span className="text-red-500">*</span>
              </FormLabel>
              <FormControl>
                <Input
                  type="number"
                  min={1}
                  placeholder="30"
                  className="h-10 rounded-lg border-slate-200 text-xs focus:border-[#1061D6]"
                  {...field}
                  onChange={(e) => field.onChange(Number(e.target.value))}
                />
              </FormControl>
              <FormMessage className="text-[10px]" />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="instructorName"
          render={({ field }) => (
            <FormItem>
              <FormLabel className="text-xs font-bold text-[#475569]">
                Nama Instruktur / Narasumber{" "}
                <span className="text-red-500">*</span>
              </FormLabel>
              <FormControl>
                <Input
                  placeholder="Ahmad Ramadhan, S.T., M.K3."
                  className="h-10 rounded-lg border-slate-200 text-xs focus:border-[#1061D6]"
                  {...field}
                  value={field.value || ""}
                />
              </FormControl>
              <FormMessage className="text-[10px]" />
            </FormItem>
          )}
        />
      </div>

      <FormField
        control={form.control}
        name="minimumScore"
        render={({ field }) => (
          <FormItem className="flex flex-col gap-2 rounded-xl border border-slate-100 bg-slate-50/50 p-4">
            <div className="flex items-center justify-between">
              <FormLabel className="text-xs font-bold text-[#475569]">
                Nilai Lulus Minimum:
              </FormLabel>
              <span className="rounded-md bg-blue-100/70 px-2 py-0.5 text-xs font-bold text-[#1061D6]">
                {field.value || 70}%
              </span>
            </div>
            <FormControl>
              <input
                type="range"
                min="50"
                max="100"
                step="5"
                className="h-1.5 w-full cursor-pointer rounded-lg bg-slate-200 accent-[#1061D6]"
                {...field}
                value={field.value || 70}
                onChange={(e) => field.onChange(Number(e.target.value))}
              />
            </FormControl>
            <div className="flex justify-between px-0.5 text-[8px] font-semibold text-slate-400">
              <span>50%</span>
              <span>75%</span>
              <span>100%</span>
            </div>
            <FormMessage className="text-[10px]" />
          </FormItem>
        )}
      />

      {/* Sektor Penerbitan Sertifikat (Figma Style) */}
      <div className="space-y-3">
        <Label className="text-xs font-bold text-[#475569]">
          Penerbitan Sertifikat
        </Label>
        <p className="text-[10px] text-slate-400">
          Tentukan apakah pelatihan ini menerbitkan sertifikat kelulusan bagi
          peserta.
        </p>

        <div className="grid grid-cols-2 gap-4">
          <button
            type="button"
            onClick={() => setHasCertificate(true)}
            className={cn(
              "flex flex-col gap-1.5 rounded-xl p-4 text-left transition-all",
              hasCertificate
                ? "border-2 border-[#1E5EF3] bg-[#EBF2FF] shadow-sm"
                : "border border-slate-200 bg-white hover:bg-slate-50",
            )}
            style={{ height: "106px" }}
          >
            <div className="flex w-full items-center justify-between">
              <span className="text-sm font-bold text-slate-800">
                🎓 Tersedia Sertifikat
              </span>
              {hasCertificate && (
                <span className="text-xs font-bold text-[#1E5EF3]">✓</span>
              )}
            </div>
            <span className="text-[10px] leading-relaxed text-slate-500">
              Peserta yang menyelesaikan pelatihan dan lulus ujian berhak atas
              sertifikat digital.
            </span>
          </button>

          <button
            type="button"
            onClick={() => setHasCertificate(false)}
            className={cn(
              "flex flex-col gap-1.5 rounded-xl p-4 text-left transition-all",
              !hasCertificate
                ? "border-2 border-[#1E5EF3] bg-[#EBF2FF] shadow-sm"
                : "border border-slate-200 bg-white hover:bg-slate-50",
            )}
            style={{ height: "106px" }}
          >
            <div className="flex w-full items-center justify-between">
              <span className="text-sm font-bold text-slate-800">
                ❌ Tanpa Sertifikat
              </span>
              {!hasCertificate && (
                <span className="text-xs font-bold text-[#1E5EF3]">✓</span>
              )}
            </div>
            <span className="text-[10px] leading-relaxed text-slate-500">
              Pelatihan ini hanya untuk media belajar mandiri tanpa menerbitkan
              tanda kelulusan.
            </span>
          </button>
        </div>
      </div>

      {/* Format Penomoran Sertifikat (Only shown when hasCertificate is true) */}
      {hasCertificate && (
        <FormField
          control={form.control}
          name="certificateNumberFormat"
          render={({ field }) => (
            <FormItem>
              <FormLabel className="text-xs font-bold text-[#475569]">
                Format Nomor Sertifikat
              </FormLabel>
              <FormControl>
                <Input
                  placeholder="Contoh: K3-ELE-10/{MONTH}/{YEAR}"
                  className="h-10 rounded-lg border-slate-200 text-xs focus:border-[#1061D6]"
                  {...field}
                  value={field.value || ""}
                />
              </FormControl>
              <FormDescription className="text-[9px] text-[#768094]">
                Gunakan tag dinamis seperti {"{MONTH}"} atau {"{YEAR}"} untuk
                format penomoran otomatis
              </FormDescription>
              <FormMessage className="text-[10px]" />
            </FormItem>
          )}
        />
      )}

      {/* Prasyarat (Opsional) */}
      <div className="space-y-2.5">
        <Label className="text-xs font-bold text-[#475569]">
          Prasyarat Kursus / Pelatihan (Opsional)
        </Label>
        <p className="text-[10px] text-slate-400">
          Pilih pelatihan lain yang wajib diselesaikan terlebih dahulu oleh
          peserta sebelum mengambil kelas ini.
        </p>
        <div className="max-h-[120px] space-y-2.5 overflow-y-auto rounded-xl border border-slate-200 bg-slate-50/50 p-4">
          {pelatihanLoading ? (
            <div className="flex items-center gap-1.5 text-xs text-slate-400">
              <IconLoader className="size-3.5 animate-spin" />
              <span>Memuat daftar pelatihan...</span>
            </div>
          ) : !dbPelatihan?.data || dbPelatihan.data.length === 0 ? (
            <span className="text-[10px] font-medium text-slate-400">
              Tidak ada pelatihan lain yang tersedia
            </span>
          ) : (
            dbPelatihan.data.map((p: any) => {
              const isChecked = (form.watch("prerequisiteIds") || []).includes(
                p.id,
              );
              return (
                <label
                  key={p.id}
                  className="flex cursor-pointer items-center gap-2.5 text-xs font-medium text-slate-700 select-none"
                >
                  <Checkbox
                    checked={isChecked}
                    onCheckedChange={(checked) => {
                      const current = form.getValues("prerequisiteIds") || [];
                      if (checked) {
                        form.setValue("prerequisiteIds", [...current, p.id], {
                          shouldValidate: true,
                        });
                      } else {
                        form.setValue(
                          "prerequisiteIds",
                          current.filter((id) => id !== p.id),
                          { shouldValidate: true },
                        );
                      }
                    }}
                  />
                  <span>{p.title}</span>
                </label>
              );
            })
          )}
        </div>
      </div>

      {/* Konfigurasi Absensi */}
      <div className="mt-1 space-y-4 rounded-xl border border-slate-100 bg-slate-50/50 p-4">
        <div>
          <h3 className="text-xs font-bold text-[#334155]">
            Konfigurasi Absensi & Kehadiran
          </h3>
          <p className="mt-0.5 text-[10px] text-[#768094]">
            Atur apakah absensi kehadiran wajib dipenuhi untuk pelatihan ini dan
            persentase minimumnya.
          </p>
        </div>

        <div className="space-y-3">
          <FormField
            control={form.control}
            name="attendanceRequired"
            render={({ field }) => (
              <FormItem className="flex flex-row items-start space-y-0 space-x-3 rounded-lg border border-slate-100 bg-white p-3">
                <FormControl>
                  <Checkbox
                    checked={field.value}
                    onCheckedChange={field.onChange}
                  />
                </FormControl>
                <div className="space-y-0.5 leading-none">
                  <FormLabel className="cursor-pointer text-xs font-semibold text-[#475569] select-none">
                    Absensi Kehadiran Wajib
                  </FormLabel>
                  <FormDescription className="text-[9px] text-slate-400">
                    Centang jika peserta wajib melakukan presensi kehadiran pada
                    jadwal yang ditentukan.
                  </FormDescription>
                </div>
              </FormItem>
            )}
          />

          {form.watch("attendanceRequired") && (
            <FormField
              control={form.control}
              name="minAttendancePercentage"
              render={({ field }) => (
                <FormItem>
                  <FormLabel className="text-xs font-bold text-[#475569]">
                    Persentase Kehadiran Minimum (%)
                  </FormLabel>
                  <FormControl>
                    <Input
                      type="number"
                      min="0"
                      max="100"
                      placeholder="Contoh: 85"
                      className="h-10 rounded-lg border-slate-200 text-xs focus:border-[#1061D6]"
                      {...field}
                      onChange={(e) => field.onChange(Number(e.target.value))}
                    />
                  </FormControl>
                  <FormDescription className="text-[9px] text-slate-400">
                    Persentase kehadiran minimum untuk kelayakan penerbitan
                    sertifikat.
                  </FormDescription>
                  <FormMessage className="text-[10px]" />
                </FormItem>
              )}
            />
          )}
        </div>
      </div>
    </div>
  );
}

const AVAILABLE_FACILITIES = [
  { id: "Sertifikat Resmi K3", name: "Sertifikat Resmi K3", icon: "🎓" },
  { id: "Makan Siang & Snack", name: "Makan Siang & Snack", icon: "🍱" },
  { id: "Coffee Break 2x", name: "Coffee Break 2x", icon: "☕" },
  {
    id: "Training Kit & Souvenir",
    name: "Training Kit & Souvenir",
    icon: "🎒",
  },
  { id: "Modul & Handout Cetak", name: "Modul & Handout Cetak", icon: "📘" },
  {
    id: "Ruang Kelas Full AC & Wifi",
    name: "Ruang Kelas Full AC & Wifi",
    icon: "🏢",
  },
];

function Step3BimtekJadwalFasilitas({
  form,
}: {
  form: UseFormReturn<CreatePelatihanValues>;
}) {
  const selectedFacilities = form.watch("facilities") || [];

  const toggleFacility = (facilityName: string) => {
    const current = form.getValues("facilities") || [];
    if (current.includes(facilityName)) {
      form.setValue(
        "facilities",
        current.filter((item) => item !== facilityName),
        { shouldValidate: true },
      );
    } else {
      form.setValue("facilities", [...current, facilityName], {
        shouldValidate: true,
      });
    }
  };

  return (
    <div className="flex flex-col gap-6">
      <div>
        <h2 className="text-lg font-bold text-[#334155]">
          Jadwal & Fasilitas Bimtek
        </h2>
        <p className="mt-0.5 text-xs text-[#768094]">
          Tentukan kapan, di mana, kapasitas, instruktur, dan fasilitas apa saja
          yang didapatkan peserta Bimtek ini
        </p>
      </div>

      <div className="grid grid-cols-2 gap-4">
        <FormField
          control={form.control}
          name="startDate"
          render={({ field }) => (
            <FormItem>
              <FormLabel className="text-xs font-bold text-[#475569]">
                Tanggal Mulai <span className="text-red-500">*</span>
              </FormLabel>
              <FormControl>
                <Input
                  type="date"
                  className="h-10 rounded-lg border-slate-200 bg-white text-xs focus:border-[#1061D6]"
                  {...field}
                  value={field.value ? field.value.substring(0, 10) : ""}
                  onChange={(e) =>
                    field.onChange(
                      e.target.value
                        ? new Date(e.target.value).toISOString()
                        : null,
                    )
                  }
                />
              </FormControl>
              <FormMessage className="text-[10px]" />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="endDate"
          render={({ field }) => (
            <FormItem>
              <FormLabel className="text-xs font-bold text-[#475569]">
                Tanggal Selesai <span className="text-red-500">*</span>
              </FormLabel>
              <FormControl>
                <Input
                  type="date"
                  className="h-10 rounded-lg border-slate-200 bg-white text-xs focus:border-[#1061D6]"
                  {...field}
                  value={field.value ? field.value.substring(0, 10) : ""}
                  onChange={(e) =>
                    field.onChange(
                      e.target.value
                        ? new Date(e.target.value).toISOString()
                        : null,
                    )
                  }
                />
              </FormControl>
              <FormMessage className="text-[10px]" />
            </FormItem>
          )}
        />
      </div>

      <div className="grid grid-cols-2 gap-4">
        <FormField
          control={form.control}
          name="location"
          render={({ field }) => (
            <FormItem>
              <FormLabel className="text-xs font-bold text-[#475569]">
                Lokasi / Tempat Pelaksanaan{" "}
                <span className="text-red-500">*</span>
              </FormLabel>
              <FormControl>
                <Input
                  placeholder="Contoh: Hotel Tepian K3, Ruang Mahakam, Samarinda"
                  className="h-10 rounded-lg border-slate-200 text-xs focus:border-[#1061D6]"
                  {...field}
                  value={field.value || ""}
                />
              </FormControl>
              <FormMessage className="text-[10px]" />
            </FormItem>
          )}
        />

        <FormField
          control={form.control}
          name="capacity"
          render={({ field }) => (
            <FormItem>
              <FormLabel className="text-xs font-bold text-[#475569]">
                Kapasitas Peserta Maksimal{" "}
                <span className="text-red-500">*</span>
              </FormLabel>
              <FormControl>
                <Input
                  type="number"
                  min={1}
                  placeholder="Contoh: 30"
                  className="h-10 rounded-lg border-slate-200 bg-white text-xs focus:border-[#1061D6]"
                  {...field}
                  value={field.value ?? ""}
                  onChange={(e) =>
                    field.onChange(
                      e.target.value ? Number(e.target.value) : null,
                    )
                  }
                />
              </FormControl>
              <FormMessage className="text-[10px]" />
            </FormItem>
          )}
        />
      </div>

      <FormField
        control={form.control}
        name="instructorName"
        render={({ field }) => (
          <FormItem>
            <FormLabel className="text-xs font-bold text-[#475569]">
              Nama Instruktur / Narasumber{" "}
              <span className="text-red-500">*</span>
            </FormLabel>
            <FormControl>
              <Input
                placeholder="Ahmad Ramadhan, S.T., M.K3."
                className="h-10 rounded-lg border-slate-200 text-xs focus:border-[#1061D6]"
                {...field}
                value={field.value || ""}
              />
            </FormControl>
            <FormMessage className="text-[10px]" />
          </FormItem>
        )}
      />

      <div className="flex flex-col gap-2">
        <Label className="text-xs font-bold text-[#475569]">
          Fasilitas Pelatihan
        </Label>
        <p className="-mt-1 mb-1 text-[10px] text-[#768094]">
          Pilih fasilitas eksklusif yang termasuk dalam paket pendaftaran Bimtek
          ini
        </p>

        <div className="grid grid-cols-2 gap-3 sm:grid-cols-3">
          {AVAILABLE_FACILITIES.map((facility) => {
            const isSelected = selectedFacilities.includes(facility.name);
            return (
              <button
                key={facility.id}
                type="button"
                onClick={() => toggleFacility(facility.name)}
                className={cn(
                  "group flex items-center gap-3 rounded-xl border p-3 text-left transition-all",
                  isSelected
                    ? "border-[#1061D6] bg-blue-50/50 shadow-sm"
                    : "border-slate-200 bg-white hover:bg-slate-50",
                )}
              >
                <div
                  className={cn(
                    "flex h-8 w-8 shrink-0 items-center justify-center rounded-lg text-lg transition-transform duration-200 group-hover:scale-110",
                    isSelected ? "bg-blue-100/60" : "bg-slate-100",
                  )}
                >
                  {facility.icon}
                </div>
                <div className="flex min-w-0 flex-col">
                  <span
                    className={cn(
                      "truncate text-[11px] leading-snug font-bold",
                      isSelected ? "text-[#1061D6]" : "text-[#475569]",
                    )}
                  >
                    {facility.name}
                  </span>
                  <span className="text-[8px] font-medium text-[#768094]">
                    Included
                  </span>
                </div>
              </button>
            );
          })}
        </div>
      </div>

      {/* Konfigurasi Absensi */}
      <div className="mt-2 space-y-4 rounded-xl border border-slate-100 bg-slate-50/50 p-4">
        <div>
          <h3 className="text-xs font-bold text-[#334155]">
            Konfigurasi Absensi & Kehadiran
          </h3>
          <p className="mt-0.5 text-[10px] text-[#768094]">
            Atur apakah absensi kehadiran wajib dipenuhi untuk pelatihan ini dan
            persentase minimumnya.
          </p>
        </div>

        <div className="space-y-3">
          <FormField
            control={form.control}
            name="attendanceRequired"
            render={({ field }) => (
              <FormItem className="flex flex-row items-start space-y-0 space-x-3 rounded-lg border border-slate-100 bg-white p-3">
                <FormControl>
                  <Checkbox
                    checked={field.value}
                    onCheckedChange={field.onChange}
                  />
                </FormControl>
                <div className="space-y-0.5 leading-none">
                  <FormLabel className="cursor-pointer text-xs font-semibold text-[#475569] select-none">
                    Absensi Kehadiran Wajib
                  </FormLabel>
                  <FormDescription className="text-[9px] text-slate-400">
                    Centang jika peserta wajib melakukan presensi kehadiran pada
                    jadwal yang ditentukan.
                  </FormDescription>
                </div>
              </FormItem>
            )}
          />

          {form.watch("attendanceRequired") && (
            <FormField
              control={form.control}
              name="minAttendancePercentage"
              render={({ field }) => (
                <FormItem>
                  <FormLabel className="text-xs font-bold text-[#475569]">
                    Persentase Kehadiran Minimum (%)
                  </FormLabel>
                  <FormControl>
                    <Input
                      type="number"
                      min="0"
                      max="100"
                      placeholder="Contoh: 85"
                      className="h-10 rounded-lg border-slate-200 text-xs focus:border-[#1061D6]"
                      {...field}
                      onChange={(e) => field.onChange(Number(e.target.value))}
                    />
                  </FormControl>
                  <FormDescription className="text-[9px] text-slate-400">
                    Persentase kehadiran minimum untuk kelayakan penerbitan
                    sertifikat.
                  </FormDescription>
                  <FormMessage className="text-[10px]" />
                </FormItem>
              )}
            />
          )}
        </div>
      </div>
    </div>
  );
}

function Step4GambarMedia({
  form,
}: {
  form: UseFormReturn<CreatePelatihanValues>;
}) {
  const [isUploading, setIsUploading] = useState(false);

  const uploadMutation = useMutation(
    trpc.pelatihan.base.uploadThumbnail.mutationOptions({
      onSuccess: (data) => {
        form.setValue("thumbnailUrl", data.key, { shouldValidate: true });
        globalSuccessToast("Gambar berhasil diunggah");
        setIsUploading(false);
      },
      onError: (error) => {
        globalErrorToast(`Gagal mengunggah gambar: ${error.message}`);
        setIsUploading(false);
      },
    }),
  );

  const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    if (file) {
      if (file.size > 5 * 1024 * 1024) {
        globalErrorToast("Ukuran file harus kurang dari 5MB");
        return;
      }
      if (!file.type.startsWith("image/")) {
        globalErrorToast("Harap pilih berkas gambar (JPG, PNG, WEBP)");
        return;
      }
      setIsUploading(true);
      const formData = new FormData();
      formData.append("file", file);
      uploadMutation.mutate(formData);
    }
  };

  const resolvedThumbnailUrl = useMemo(() => {
    const val = form.watch("thumbnailUrl");
    if (!val) return "";
    if (
      val.startsWith("http://") ||
      val.startsWith("https://") ||
      val.startsWith("data:")
    ) {
      return val;
    }
    return getPublicUrl(val);
  }, [form.watch("thumbnailUrl")]);

  return (
    <div className="flex flex-col gap-5">
      <div>
        <h2 className="text-lg font-bold text-[#334155]">Gambar & Media</h2>
        <p className="mt-0.5 text-xs text-[#768094]">
          Tentukan sampul representatif. Anda dapat memasukkan URL gambar
          eksternal atau mengunggah berkas gambar langsung dari perangkat Anda.
        </p>
      </div>

      <div className="grid grid-cols-1 items-start gap-6 md:grid-cols-2">
        {/* Left Column: Form Controls */}
        <div className="grid gap-4 rounded-xl border border-slate-100 bg-slate-50/50 p-5">
          <div className="grid gap-2">
            <Label className="text-xs font-bold text-[#475569]">
              Pilihan 1: Unggah Berkas Gambar
            </Label>
            <div className="flex flex-wrap items-center gap-3">
              <input
                type="file"
                accept="image/*"
                id="thumbnail-file"
                className="hidden"
                onChange={handleFileChange}
                disabled={isUploading}
              />
              <Button
                type="button"
                variant="outline"
                className="h-10 rounded-lg border-slate-200 bg-white text-xs font-bold text-[#475569] hover:bg-slate-50"
                onClick={() =>
                  document.getElementById("thumbnail-file")?.click()
                }
                disabled={isUploading}
              >
                {isUploading ? (
                  <>
                    <IconLoader className="mr-1.5 size-4 animate-spin text-slate-400" />
                    Mengunggah...
                  </>
                ) : (
                  "Pilih & Unggah File"
                )}
              </Button>
              <span className="text-[10px] text-[#768094]">Maks. 5MB</span>
            </div>
          </div>

          <div className="relative flex items-center py-1">
            <div className="flex-grow border-t border-slate-200"></div>
            <span className="mx-3 flex-shrink text-[9px] font-bold text-slate-400 uppercase">
              Atau
            </span>
            <div className="flex-grow border-t border-slate-200"></div>
          </div>

          <FormField
            control={form.control}
            name="thumbnailUrl"
            render={({ field }) => (
              <FormItem>
                <FormLabel className="text-xs font-bold text-[#475569]">
                  Pilihan 2: Tempel URL Gambar
                </FormLabel>
                <FormControl>
                  <Input
                    placeholder="https://images.unsplash.com/photo-..."
                    className="h-10 rounded-lg border-slate-200 bg-white text-xs focus:border-[#1061D6]"
                    {...field}
                    value={field.value || ""}
                  />
                </FormControl>
                <FormMessage className="text-[10px]" />
              </FormItem>
            )}
          />
        </div>

        {/* Right Column: Always-Visible Preview Box */}
        <div className="flex flex-col gap-2 rounded-xl border border-slate-100 bg-slate-50/50 p-5">
          <span className="text-[10px] font-bold text-slate-400 uppercase">
            Preview Sampul
          </span>
          {form.watch("thumbnailUrl") ? (
            <div className="group relative flex aspect-video w-full items-center justify-center overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">
              <ImageWithFallback
                src={resolvedThumbnailUrl}
                alt="Sampul preview"
                className="h-full w-full object-cover"
              />
              <button
                type="button"
                onClick={() =>
                  form.setValue("thumbnailUrl", "", { shouldValidate: true })
                }
                className="absolute top-2 right-2 rounded-full bg-red-500/80 p-1.5 text-white opacity-0 transition-opacity duration-200 group-hover:opacity-100 hover:bg-red-600"
                title="Hapus gambar"
              >
                <IconTrash className="size-4" />
              </button>
            </div>
          ) : (
            <div className="flex aspect-video w-full flex-col items-center justify-center gap-2 rounded-xl border-2 border-dashed border-slate-200 bg-white p-6 text-center">
              <div className="flex h-9 w-9 items-center justify-center rounded-full bg-slate-100">
                <IconFile className="size-4 text-slate-400" />
              </div>
              <span className="text-[10px] font-semibold text-slate-400">
                Belum ada preview sampul
              </span>
            </div>
          )}
        </div>
      </div>
    </div>
  );
}

// ##################
// authored (generated by gemini, Jun 09 2026 07:15 WITA)
// ##################
function StepReviewPublikasi({
  form,
  localModulesCount,
  localMaterialsCount,
  benefits,
  hasCertificate,
}: {
  form: UseFormReturn<CreatePelatihanValues>;
  localModulesCount: number;
  localMaterialsCount: number;
  benefits: string[];
  hasCertificate: boolean;
}) {
  const isFree = !form.watch("price") || form.watch("price") === 0;
  const type = form.watch("type");
  const startDate = form.watch("startDate");
  const endDate = form.watch("endDate");
  const selectedFacilities = form.watch("facilities") || [];

  const formatDate = (dateStr: string | null | undefined) => {
    if (!dateStr) return "—";
    try {
      const d = new Date(dateStr);
      return d.toLocaleDateString("id-ID", {
        weekday: "long",
        year: "numeric",
        month: "long",
        day: "numeric",
      });
    } catch {
      return dateStr;
    }
  };

  const resolvedThumbnailUrl = useMemo(() => {
    const val = form.watch("thumbnailUrl");
    if (!val) return "";
    if (
      val.startsWith("http://") ||
      val.startsWith("https://") ||
      val.startsWith("data:")
    ) {
      return val;
    }
    return getPublicUrl(val);
  }, [form.watch("thumbnailUrl")]);

  const activeBenefits = useMemo(() => {
    return benefits.filter((b) => b.trim() !== "");
  }, [benefits]);

  const prerequisiteCount = (form.watch("prerequisiteIds") || []).length;

  return (
    <div className="flex flex-col gap-5">
      <div>
        <h2 className="text-lg font-bold text-[#334155]">Review & Publikasi</h2>
        <p className="mt-0.5 text-xs text-[#768094]">
          Tentukan skema harga, batasan jumlah peserta, dan tinjau rangkuman
          keseluruhan sebelum mempublikasikan pelatihan Anda.
        </p>
      </div>

      <div className="flex flex-col gap-3 rounded-xl border border-slate-200 bg-white p-4">
        <h3 className="flex items-center gap-1.5 text-xs font-bold text-[#334155]">
          <span>💰</span> Skema Pembayaran
        </h3>
        <div className="grid grid-cols-2 gap-3">
          {[
            { label: "Gratis", value: 0, icon: "🎁" },
            { label: "Berbayar", value: null, icon: "💳" },
          ].map((opt) => {
            const isSelected = opt.value === 0 ? isFree : !isFree;
            return (
              <button
                key={opt.label}
                type="button"
                onClick={() => {
                  if (opt.value === 0) {
                    form.setValue("price", 0);
                    form.setValue("discountPrice", null);
                  } else {
                    form.setValue("price", type === "bimtek" ? 1500000 : 99000);
                  }
                }}
                className={cn(
                  "flex items-center gap-2.5 rounded-xl border-2 p-3 text-left transition-all",
                  isSelected
                    ? "border-[#1061D6] bg-blue-50/50"
                    : "border-slate-200 bg-white hover:bg-slate-50",
                )}
              >
                <span className="text-base">{opt.icon}</span>
                <span
                  className={cn(
                    "text-xs font-bold",
                    isSelected ? "text-[#1061D6]" : "text-[#475569]",
                  )}
                >
                  {opt.label}
                </span>
              </button>
            );
          })}
        </div>

        {!isFree && (
          <div className="mt-1.5 grid grid-cols-2 gap-3">
            <FormField
              control={form.control}
              name="price"
              render={({ field }) => (
                <FormItem>
                  <FormLabel className="text-[10px] font-bold text-[#475569]">
                    Harga Asli (Rp)
                  </FormLabel>
                  <FormControl>
                    <div className="relative h-10">
                      <span className="absolute top-1/2 left-3 -translate-y-1/2 text-xs font-bold text-[#768094]">
                        Rp
                      </span>
                      <Input
                        type="number"
                        className="h-10 rounded-lg border-slate-200 pl-9 text-xs"
                        {...field}
                        onChange={(e) => field.onChange(Number(e.target.value))}
                      />
                    </div>
                  </FormControl>
                  <FormMessage className="text-[10px]" />
                </FormItem>
              )}
            />

            <FormField
              control={form.control}
              name="discountPrice"
              render={({ field }) => (
                <FormItem>
                  <FormLabel className="text-[10px] font-bold text-[#475569]">
                    Harga Promo / Coreng (Rp)
                  </FormLabel>
                  <FormControl>
                    <div className="relative h-10">
                      <span className="absolute top-1/2 left-3 -translate-y-1/2 text-xs font-bold text-[#768094]">
                        Rp
                      </span>
                      <Input
                        type="number"
                        className="h-10 rounded-lg border-slate-200 pl-9 text-xs"
                        placeholder="Tidak ada diskon"
                        {...field}
                        value={field.value ?? ""}
                        onChange={(e) =>
                          field.onChange(
                            e.target.value ? Number(e.target.value) : null,
                          )
                        }
                      />
                    </div>
                  </FormControl>
                  <FormMessage className="text-[10px]" />
                </FormItem>
              )}
            />
          </div>
        )}
      </div>

      <div className="grid grid-cols-2 gap-4">
        {type !== "bimtek" && (
          <FormField
            control={form.control}
            name="capacity"
            render={({ field }) => (
              <FormItem>
                <FormLabel className="text-xs font-bold text-[#475569]">
                  Kapasitas Peserta Maks. (Opsional)
                </FormLabel>
                <FormControl>
                  <Input
                    type="number"
                    min={1}
                    placeholder="Tidak dibatasi (kosongkan)"
                    className="h-10 rounded-lg border-slate-200 text-xs"
                    {...field}
                    value={field.value ?? ""}
                    onChange={(e) =>
                      field.onChange(
                        e.target.value ? Number(e.target.value) : null,
                      )
                    }
                  />
                </FormControl>
                <FormMessage className="text-[10px]" />
              </FormItem>
            )}
          />
        )}

        <FormField
          control={form.control}
          name="status"
          render={({ field }) => (
            <FormItem className={cn(type === "bimtek" && "col-span-2")}>
              <FormLabel className="text-xs font-bold text-[#475569]">
                Status Penerbitan <span className="text-red-500">*</span>
              </FormLabel>
              <Select onValueChange={field.onChange} value={field.value}>
                <FormControl>
                  <SelectTrigger className="h-10 rounded-lg border-slate-200 text-xs">
                    <SelectValue placeholder="Pilih status" />
                  </SelectTrigger>
                </FormControl>
                <SelectContent>
                  <SelectItem value="draft" className="text-xs">
                    Draft (Internal)
                  </SelectItem>
                  <SelectItem value="published" className="text-xs">
                    Published (Terbit Umum)
                  </SelectItem>
                </SelectContent>
              </Select>
              <FormMessage className="text-[10px]" />
            </FormItem>
          )}
        />
      </div>

      {/* Rangkuman Detail (Premium UI) */}
      <div className="space-y-4 rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
        <h3 className="flex items-center gap-2 border-b border-slate-100 pb-2 text-sm font-bold text-[#1E293B]">
          <span>📋</span> Rangkuman Pelatihan Baru
        </h3>

        <div className="grid grid-cols-1 gap-6 md:grid-cols-3">
          {/* Sisi Kiri: Thumbnail & Benefits */}
          <div className="space-y-4 md:col-span-1">
            <div className="relative flex aspect-video w-full items-center justify-center overflow-hidden rounded-xl border border-slate-200 bg-slate-50 shadow-sm">
              {form.watch("thumbnailUrl") ? (
                <ImageWithFallback
                  src={resolvedThumbnailUrl}
                  alt="Sampul pelatihan"
                  className="h-full w-full object-cover"
                />
              ) : (
                <div className="flex flex-col items-center gap-1.5 text-slate-400">
                  <IconFile className="size-8 stroke-[1.5]" />
                  <span className="text-[10px] font-semibold">
                    Tanpa Sampul
                  </span>
                </div>
              )}
              <span className="absolute top-2 left-2 rounded-lg bg-blue-600 px-2 py-0.5 text-[9px] font-bold text-white capitalize shadow-sm">
                {type === "elearning"
                  ? "E-Learning"
                  : type === "bimtek"
                    ? "Bimtek"
                    : "Webinar"}
              </span>
            </div>

            <div className="space-y-2 rounded-xl border border-slate-100 bg-slate-50 p-4">
              <h4 className="text-[10px] font-bold tracking-wider text-slate-400 uppercase">
                Benefit Pelatihan
              </h4>
              {activeBenefits.length === 0 ? (
                <p className="text-[10px] font-medium text-slate-400 italic">
                  Tidak ada benefit yang ditambahkan
                </p>
              ) : (
                <ul className="space-y-1.5">
                  {activeBenefits.map((benefit, i) => (
                    <li
                      key={i}
                      className="flex items-start gap-2 text-[10px] font-medium text-slate-600"
                    >
                      <span className="shrink-0 font-bold text-green-500">
                        ✓
                      </span>
                      <span className="leading-snug">{benefit}</span>
                    </li>
                  ))}
                </ul>
              )}
            </div>
          </div>

          {/* Sisi Kanan: Detail Information Grid */}
          <div className="grid grid-cols-2 gap-x-6 gap-y-4 text-xs md:col-span-2">
            <div className="col-span-2 flex flex-col gap-0.5">
              <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                Judul Pelatihan
              </span>
              <span className="mt-0.5 text-sm leading-snug font-bold text-slate-800">
                {form.watch("title") || "—"}
              </span>
            </div>

            <div className="flex flex-col gap-0.5">
              <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                Kategori
              </span>
              <span className="mt-0.5 font-bold text-slate-700">
                {form.watch("categoryId") && form.watch("categoryId") !== "none"
                  ? "Terpilih"
                  : "Tidak Dipilih"}
              </span>
            </div>

            <div className="flex flex-col gap-0.5">
              <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                Tingkat Kesulitan
              </span>
              <span className="mt-0.5 font-bold text-slate-700">
                {form.watch("level") === "beginner"
                  ? "Pemula"
                  : form.watch("level") === "intermediate"
                    ? "Menengah"
                    : form.watch("level") === "advanced"
                      ? "Lanjutan"
                      : "Tidak Dipilih"}
              </span>
            </div>

            <div className="flex flex-col gap-0.5">
              <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                Biaya Pendaftaran
              </span>
              <span className="mt-0.5 font-bold text-slate-800">
                {isFree ? (
                  <span className="font-bold text-green-600">Gratis</span>
                ) : (
                  `Rp ${(form.watch("price") || 0).toLocaleString("id-ID")}`
                )}
              </span>
            </div>

            <div className="flex flex-col gap-0.5">
              <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                Sertifikat Kelulusan
              </span>
              <span className="mt-0.5 font-bold text-slate-700">
                {hasCertificate ? "🎓 Tersedia" : "❌ Tidak Tersedia"}
              </span>
            </div>

            <div className="flex flex-col gap-0.5">
              <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                Instruktur Utama
              </span>
              <span className="mt-0.5 font-bold text-slate-700">
                {form.watch("instructorName") || "—"}
              </span>
            </div>

            <div className="flex flex-col gap-0.5">
              <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                Kelas Prasyarat
              </span>
              <span className="mt-0.5 font-bold text-slate-700">
                {prerequisiteCount > 0
                  ? `${prerequisiteCount} Kelas Prasyarat`
                  : "Tidak ada"}
              </span>
            </div>

            {type === "bimtek" ? (
              <>
                <div className="col-span-2 flex flex-col gap-0.5">
                  <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                    Tanggal Pelaksanaan
                  </span>
                  <span className="mt-0.5 font-bold text-slate-700">
                    {formatDate(startDate)} s.d. {formatDate(endDate)}
                  </span>
                </div>
                <div className="col-span-2 flex flex-col gap-0.5">
                  <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                    Lokasi Kegiatan
                  </span>
                  <span className="mt-0.5 font-bold text-slate-700">
                    {form.watch("location") || "—"}
                  </span>
                </div>
                <div className="flex flex-col gap-0.5">
                  <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                    Kapasitas Maksimal
                  </span>
                  <span className="mt-0.5 font-bold text-slate-700">
                    {form.watch("capacity")
                      ? `${form.watch("capacity")} Peserta`
                      : "Tidak dibatasi"}
                  </span>
                </div>
                <div className="col-span-2 flex flex-col gap-0.5">
                  <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                    Fasilitas yang Didapat
                  </span>
                  <div className="mt-1 flex flex-wrap gap-1.5">
                    {selectedFacilities.length === 0 ? (
                      <span className="text-[10px] text-slate-400 italic">
                        Tidak ada fasilitas khusus
                      </span>
                    ) : (
                      selectedFacilities.map((fac) => (
                        <span
                          key={fac}
                          className="rounded-full border border-blue-100 bg-blue-50 px-2 py-0.5 text-[9px] font-semibold text-blue-700"
                        >
                          {fac}
                        </span>
                      ))
                    )}
                  </div>
                </div>
              </>
            ) : (
              <>
                <div className="flex flex-col gap-0.5">
                  <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                    Jangka Waktu Akses
                  </span>
                  <span className="mt-0.5 font-bold text-slate-700">
                    {form.watch("duration")} Hari
                  </span>
                </div>
                <div className="flex flex-col gap-0.5">
                  <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                    Batas Nilai Kelulusan
                  </span>
                  <span className="mt-0.5 font-bold text-slate-700">
                    {form.watch("minimumScore")}%
                  </span>
                </div>
                <div className="flex flex-col gap-0.5">
                  <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                    Struktur Kurikulum
                  </span>
                  <span className="mt-0.5 font-bold text-[#1061D6]">
                    {localModulesCount} Materi / {localMaterialsCount} Modul
                  </span>
                </div>
              </>
            )}

            <div className="col-span-2 mt-1 flex flex-col gap-0.5">
              <span className="text-[9px] font-bold tracking-wider text-slate-400 uppercase">
                Persyaratan Berkas Pendaftaran
              </span>
              <div className="mt-1 flex flex-wrap gap-1.5">
                {Object.entries(form.watch("dynamicRequirements") || {}).filter(
                  ([_, v]) => v,
                ).length === 0 ? (
                  <span className="text-[10px] text-slate-400 italic">
                    Tidak memerlukan persyaratan dokumen khusus
                  </span>
                ) : (
                  Object.entries(form.watch("dynamicRequirements") || {})
                    .filter(([_, v]) => v)
                    .map(([k]) => {
                      const labels: Record<string, string> = {
                        participantNik: "NIK",
                        participantBirthPlace: "Tempat Lahir",
                        participantBirthDate: "Tanggal Lahir",
                        participantPhone: "Nomor HP/WA",
                        participantAddress: "Alamat",
                        participantBloodType: "Golongan Darah",
                        companyName: "Nama Perusahaan",
                        companyAddress: "Alamat Perusahaan",
                        companyProvinceId: "Provinsi Perusahaan",
                        companyRegencyId: "Kota Perusahaan",
                        companyDistrictId: "Kecamatan Perusahaan",
                        companyKbli: "KBLI",
                        ktpDoc: "Scan KTP",
                        employmentLetter: "Surat Kerja",
                        consentLetter: "Surat Kesediaan",
                        diploma: "Ijazah",
                        // ##################
                        // authored (generated by claude, Jun 10 2026 05:25 WITA)
                        // ##################
                        passPhoto: "Pass Foto Latar Merah",
                        // ##################
                        // end authored
                        // ##################
                      };
                      return (
                        <span
                          key={k}
                          className="rounded-full border border-slate-200 bg-slate-100 px-2 py-0.5 text-[9px] font-semibold text-slate-600"
                        >
                          {labels[k] || k}
                        </span>
                      );
                    })
                )}
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
// ##################
// end authored
// ##################

function StepPersyaratanPendaftaran({
  form,
}: {
  form: UseFormReturn<CreatePelatihanValues>;
}) {
  return (
    <div className="flex flex-col gap-6">
      <div>
        <h2 className="text-lg font-bold text-[#334155]">
          Persyaratan Pendaftaran
        </h2>
        <p className="mt-0.5 text-xs text-[#768094]">
          Centang data dan berkas yang wajib dilengkapi oleh peserta saat
          mendaftar kelas ini. Sifatnya opsional, boleh dicentang atau tidak.
        </p>
      </div>

      <div className="space-y-6 rounded-xl border border-slate-100 bg-slate-50/50 p-5">
        <div className="space-y-3">
          <h4 className="text-xs font-bold tracking-wider text-slate-500 uppercase">
            Data Diri Peserta
          </h4>
          <div className="grid grid-cols-1 gap-3 md:grid-cols-2">
            {[
              { id: "participantNik", label: "NIK (16 Digit)" },
              { id: "participantBirthPlace", label: "Tempat Lahir" },
              { id: "participantBirthDate", label: "Tanggal Lahir" },
              { id: "participantPhone", label: "Nomor WhatsApp/HP" },
              { id: "participantAddress", label: "Alamat Lengkap" },
              { id: "participantBloodType", label: "Golongan Darah" },
            ].map((field) => (
              <FormField
                key={field.id}
                control={form.control}
                name={`dynamicRequirements.${field.id}` as any}
                render={({ field: f }) => (
                  <FormItem className="flex flex-row items-center justify-between rounded-xl border border-slate-100 bg-white p-4 shadow-sm">
                    <div className="space-y-0.5">
                      <FormLabel className="cursor-pointer text-xs font-bold text-[#334155] select-none">
                        {field.label}
                      </FormLabel>
                    </div>
                    <FormControl>
                      <Switch
                        checked={!!f.value}
                        onCheckedChange={f.onChange}
                      />
                    </FormControl>
                  </FormItem>
                )}
              />
            ))}
          </div>
        </div>

        <div className="space-y-3">
          <h4 className="text-xs font-bold tracking-wider text-slate-500 uppercase">
            Data Perusahaan (Sponsor)
          </h4>
          <div className="grid grid-cols-1 gap-3 md:grid-cols-2">
            {[
              { id: "companyName", label: "Nama Perusahaan" },
              { id: "companyAddress", label: "Alamat Perusahaan" },
              { id: "companyProvinceId", label: "Provinsi Perusahaan" },
              { id: "companyRegencyId", label: "Kota/Kabupaten Perusahaan" },
              { id: "companyDistrictId", label: "Kecamatan Perusahaan" },
              { id: "companyKbli", label: "KBLI Bidang Usaha" },
            ].map((field) => (
              <FormField
                key={field.id}
                control={form.control}
                name={`dynamicRequirements.${field.id}` as any}
                render={({ field: f }) => (
                  <FormItem className="flex flex-row items-center justify-between rounded-xl border border-slate-100 bg-white p-4 shadow-sm">
                    <div className="space-y-0.5">
                      <FormLabel className="cursor-pointer text-xs font-bold text-[#334155] select-none">
                        {field.label}
                      </FormLabel>
                    </div>
                    <FormControl>
                      <Switch
                        checked={!!f.value}
                        onCheckedChange={f.onChange}
                      />
                    </FormControl>
                  </FormItem>
                )}
              />
            ))}
          </div>
        </div>

        <div className="space-y-3">
          <h4 className="text-xs font-bold tracking-wider text-slate-500 uppercase">
            Dokumen Yang Wajib Diupload
          </h4>
          <div className="grid grid-cols-1 gap-3 md:grid-cols-2">
            {[
              { id: "ktpDoc", label: "Scan KTP (Kartu Identitas)" },
              { id: "employmentLetter", label: "Surat Keterangan Kerja" },
              { id: "consentLetter", label: "Surat Pernyataan Kesediaan" },
              { id: "diploma", label: "Ijazah Terakhir" },
              // ##################
              // authored (generated by claude, Jun 10 2026 05:25 WITA)
              // ##################
              { id: "passPhoto", label: "Pass Foto Latar Merah" },
              // ##################
              // end authored
              // ##################
            ].map((field) => (
              <FormField
                key={field.id}
                control={form.control}
                name={`dynamicRequirements.${field.id}` as any}
                render={({ field: f }) => (
                  <FormItem className="flex flex-row items-center justify-between rounded-xl border border-slate-100 bg-white p-4 shadow-sm">
                    <div className="space-y-0.5">
                      <FormLabel className="cursor-pointer text-xs font-bold text-[#334155] select-none">
                        {field.label}
                      </FormLabel>
                    </div>
                    <FormControl>
                      <Switch
                        checked={!!f.value}
                        onCheckedChange={f.onChange}
                      />
                    </FormControl>
                  </FormItem>
                )}
              />
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}

// ─── Main Route Component ────────────────────────────────────────────────────

function RouteComponent() {
  const { type } = Route.useSearch();
  const navigate = useNavigate();
  const [currentStep, setCurrentStep] = useState(1);
  const [completedSteps, setCompletedSteps] = useState<Set<number>>(new Set());

  // Fetch categories dynamically from database
  const { data: dbCategories, isLoading: categoriesLoading } = useQuery(
    trpc.pelatihan.base.getAllCategories.queryOptions(),
  );

  const { data: dbPelatihan, isLoading: pelatihanLoading } = useQuery(
    trpc.pelatihan.base.getAllPelatihan.queryOptions({ perPage: 100 }),
  );

  // Local Curriculum State for Step 5 (Materi & Quiz)
  const [localModules, setLocalModules] = useState<LocalModule[]>([]);
  const [localOrphans, setLocalOrphans] = useState<LocalMaterial[]>([]);

  // Local State for Benefits & Certificate configurations
  const [benefits, setBenefits] = useState<string[]>([""]);
  const [hasCertificate, setHasCertificate] = useState(true);
  const [materialModalStep, setMaterialModalStep] = useState(1);

  // Dialog control states
  const [isModuleDialogOpen, setIsModuleDialogOpen] = useState(false);
  const [editingModuleId, setEditingModuleId] = useState<string | null>(null);
  const [moduleTitle, setModuleTitle] = useState("");
  const [moduleDesc, setModuleDesc] = useState("");

  const [isMaterialDialogOpen, setIsMaterialDialogOpen] = useState(false);
  const [editingMaterialId, setEditingMaterialId] = useState<string | null>(
    null,
  );
  const [activeModuleIdForMaterial, setActiveModuleIdForMaterial] =
    useState<string>("none");

  // Material dialog form states
  const [materialTitle, setMaterialTitle] = useState("");
  const [materialDesc, setMaterialDesc] = useState("");
  const [materialType, setMaterialType] = useState<
    "ppt" | "pdf" | "video" | "document" | "link"
  >("pdf");
  const [materialUrl, setMaterialUrl] = useState("");
  const [materialDuration, setMaterialDuration] = useState("");
  const [materialSize, setMaterialSize] = useState("");
  const [materialIsPreview, setMaterialIsPreview] = useState(false);

  // Accordion expanded modules matching back-office curriculum style
  const [expandedModules, setExpandedModules] = useState<
    Record<string, boolean>
  >({});

  const toggleExpand = (modId: string) => {
    setExpandedModules((prev) => ({
      ...prev,
      [modId]: !prev[modId],
    }));
  };

  // Reset material step when dialog opens
  useEffect(() => {
    if (isMaterialDialogOpen) {
      setMaterialModalStep(editingMaterialId ? 2 : 1);
    }
  }, [isMaterialDialogOpen, editingMaterialId]);

  // Confirmation & Success Dialog control states
  const [isConfirmDialogOpen, setIsConfirmDialogOpen] = useState(false);
  const [isSuccessDialogOpen, setIsSuccessDialogOpen] = useState(false);
  const [pendingFormData, setPendingFormData] =
    useState<CreatePelatihanValues | null>(null);

  const form = useForm<CreatePelatihanValues>({
    resolver: zodResolver(pelatihanSchema.createPelatihanSchema) as any,
    defaultValues: {
      title: "",
      slug: "",
      description: "",
      shortDescription: "",
      level: null as any,
      type: type || "elearning",
      duration: 30,
      capacity: null,
      price: 0,
      discountPrice: null,
      prerequisiteIds: [],
      minimumScore: 70,
      status: "draft",
      thumbnailUrl: "",
      instructorName: "",
      instructorBio: "",
      categoryId: null,
      startDate: null,
      endDate: null,
      location: "",
      facilities: [],
      dynamicRequirements: {
        participantNik: false,
        participantBirthPlace: false,
        participantBirthDate: false,
        participantPhone: false,
        participantAddress: false,
        participantBloodType: false,
        companyName: false,
        companyAddress: false,
        companyProvinceId: false,
        companyRegencyId: false,
        companyDistrictId: false,
        companyKbli: false,
        ktpDoc: false,
        employmentLetter: false,
        consentLetter: false,
        diploma: false,
        // ##################
        // authored (generated by claude, Jun 10 2026 05:25 WITA)
        // ##################
        passPhoto: false,
        // ##################
        // end authored
        // ##################
      },
      attendanceRequired: true,
      minAttendancePercentage: 85,
    },
  });

  // Auto-populate shortDescription from first 150 chars of description
  const watchDescription = form.watch("description");
  useEffect(() => {
    if (watchDescription) {
      const plainText = watchDescription.replace(/<[^>]*>/g, "");
      const shortDesc =
        plainText.substring(0, 147) + (plainText.length > 147 ? "..." : "");
      form.setValue("shortDescription", shortDesc, { shouldValidate: true });
    } else {
      form.setValue("shortDescription", "");
    }
  }, [watchDescription, form]);

  // Dynamic step configuration
  const steps = useMemo(() => {
    const t = form.watch("type");
    if (t === "bimtek") return BIMTEK_STEPS;
    if (t === "webinar") return WEBINAR_STEPS;
    return ELEARNING_STEPS;
  }, [form.watch("type")]);

  // Adjust step bounds if user changes course type dynamically
  useEffect(() => {
    if (currentStep > steps.length) {
      setCurrentStep(steps.length);
    }
  }, [steps.length, currentStep]);

  // tRPC Mutations
  const createMutation = useMutation(
    trpc.pelatihan.base.createPelatihan.mutationOptions(),
  );

  const createModuleMutation = useMutation(
    trpc.pelatihan.materials.createModule.mutationOptions(),
  );

  const createMaterialMutation = useMutation(
    trpc.pelatihan.materials.createMaterial.mutationOptions(),
  );

  // local calculation counts
  const localModulesCount = localModules.length;
  const localMaterialsCount = useMemo(() => {
    const fromModules = localModules.reduce(
      (acc, m) => acc + m.materials.length,
      0,
    );
    return fromModules + localOrphans.length;
  }, [localModules, localOrphans]);

  // Open Dialog triggers
  const openNewModule = () => {
    setEditingModuleId(null);
    setModuleTitle("");
    setModuleDesc("");
    setIsModuleDialogOpen(true);
  };

  const openEditModule = (mod: LocalModule) => {
    setEditingModuleId(mod.id);
    setModuleTitle(mod.title);
    setModuleDesc(mod.description || "");
    setIsModuleDialogOpen(true);
  };

  const handleModuleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    if (!moduleTitle.trim()) return;

    if (editingModuleId) {
      setLocalModules((prev) =>
        prev.map((m) =>
          m.id === editingModuleId
            ? { ...m, title: moduleTitle, description: moduleDesc || null }
            : m,
        ),
      );
    } else {
      const newMod: LocalModule = {
        id: uuidv7(),
        title: moduleTitle,
        description: moduleDesc || null,
        materials: [],
      };
      setLocalModules((prev) => [...prev, newMod]);
      // Default to expand newly added modules
      setExpandedModules((prev) => ({ ...prev, [newMod.id]: true }));
    }

    setIsModuleDialogOpen(false);
    setEditingModuleId(null);
  };

  const deleteModule = (id: string) => {
    if (
      confirm("Apakah Anda yakin ingin menghapus modul ini beserta isinya?")
    ) {
      setLocalModules((prev) => prev.filter((m) => m.id !== id));
    }
  };

  const openNewMaterial = (modId: string = "none") => {
    setEditingMaterialId(null);
    setActiveModuleIdForMaterial(modId);
    setMaterialTitle("");
    setMaterialDesc("");
    setMaterialType("pdf");
    setMaterialUrl("");
    setMaterialDuration("");
    setMaterialSize("");
    setMaterialIsPreview(false);
    setIsMaterialDialogOpen(true);
  };

  const openEditMaterial = (mat: LocalMaterial, modId: string = "none") => {
    setEditingMaterialId(mat.id);
    setActiveModuleIdForMaterial(modId);
    setMaterialTitle(mat.title);
    setMaterialDesc(mat.description || "");
    setMaterialType(mat.type);
    setMaterialUrl(mat.fileUrl || "");
    setMaterialDuration(mat.duration ? String(mat.duration) : "");
    setMaterialSize(mat.fileSize ? String(mat.fileSize) : "");
    setMaterialIsPreview(mat.isPreview);
    setIsMaterialDialogOpen(true);
  };

  const handleMaterialSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    if (!materialTitle.trim()) return;

    const payload: LocalMaterial = {
      id: editingMaterialId || uuidv7(),
      title: materialTitle,
      description: materialDesc || null,
      type: materialType,
      fileUrl: materialUrl || null,
      fileSize: materialSize ? Number(materialSize) : null,
      duration: materialDuration ? Number(materialDuration) : null,
      isPreview: materialIsPreview,
    };

    if (editingMaterialId) {
      if (activeModuleIdForMaterial === "none") {
        setLocalOrphans((prev) =>
          prev.map((mat) => (mat.id === editingMaterialId ? payload : mat)),
        );
      } else {
        setLocalModules((prev) =>
          prev.map((mod) =>
            mod.id === activeModuleIdForMaterial
              ? {
                  ...mod,
                  materials: mod.materials.map((mat) =>
                    mat.id === editingMaterialId ? payload : mat,
                  ),
                }
              : mod,
          ),
        );
      }
    } else {
      if (activeModuleIdForMaterial === "none") {
        setLocalOrphans((prev) => [...prev, payload]);
      } else {
        setLocalModules((prev) =>
          prev.map((mod) =>
            mod.id === activeModuleIdForMaterial
              ? { ...mod, materials: [...mod.materials, payload] }
              : mod,
          ),
        );
      }
    }

    setIsMaterialDialogOpen(false);
    setEditingMaterialId(null);
  };

  const deleteMaterial = (matId: string, modId: string = "none") => {
    if (confirm("Apakah Anda yakin ingin menghapus materi ini?")) {
      if (modId === "none") {
        setLocalOrphans((prev) => prev.filter((m) => m.id !== matId));
      } else {
        setLocalModules((prev) =>
          prev.map((m) =>
            m.id === modId
              ? { ...m, materials: m.materials.filter((x) => x.id !== matId) }
              : m,
          ),
        );
      }
    }
  };

  // Navigations
  const markCompleted = useCallback((step: number) => {
    setCompletedSteps((prev) => new Set([...prev, step]));
  }, []);

  const handleNext = useCallback(async () => {
    const isBimtek = form.getValues("type") === "bimtek";
    if (currentStep === 1) {
      const valid = await form.trigger(["title", "slug", "type"]);
      if (!valid) return;
    } else if (currentStep === 2) {
      const valid = await form.trigger(["description"]);
      if (!valid) return;
    } else if (currentStep === 3) {
      if (isBimtek) {
        const valid = await form.trigger([
          "startDate",
          "endDate",
          "location",
          "capacity",
          "instructorName",
        ]);
        if (!valid) return;
      } else {
        const valid = await form.trigger([
          "duration",
          "instructorName",
          "minimumScore",
        ]);
        if (!valid) return;
      }
    } else if (currentStep === 4) {
      const valid = await form.trigger(["thumbnailUrl"]);
      if (!valid) return;
    }

    markCompleted(currentStep);
    setCurrentStep((prev) => Math.min(prev + 1, steps.length));
  }, [currentStep, form, markCompleted, steps]);

  const handlePrev = useCallback(() => {
    setCurrentStep((prev) => Math.max(prev - 1, 1));
  }, []);

  // Multi-Step DB Transaction Submission
  const [isSubmittingSequentially, setIsSubmittingSequentially] =
    useState(false);

  // Pre-submit interceptor: opens Confirmation Dialog if form passes validation
  const handlePreSubmit = (data: CreatePelatihanValues) => {
    setPendingFormData(data);
    setIsConfirmDialogOpen(true);
  };

  // Triggers sequential transaction when "Simpan" is clicked on confirmation dialog
  const executeSave = async () => {
    if (!pendingFormData) return;
    setIsConfirmDialogOpen(false);
    setIsSubmittingSequentially(true);
    markCompleted(steps.length);

    try {
      // Format benefits as requirements list
      const formattedBenefits = benefits
        .filter((b) => b.trim() !== "")
        .map((b) => `• ${b}`)
        .join("\n");

      // 1. Create base training record in PostgreSQL
      // ##################
      // authored (generated by gemini, Jun 09 2026 07:15 WITA)
      // ##################
      const payload = {
        ...pendingFormData,
        requirements: formattedBenefits || pendingFormData.requirements,
        categoryId: !pendingFormData.categoryId
          ? null
          : pendingFormData.categoryId,
        level: !pendingFormData.level ? null : pendingFormData.level,
        prerequisiteIds: pendingFormData.prerequisiteIds || [],
      };
      // ##################
      // end authored
      // ##################

      const result = await createMutation.mutateAsync(payload);
      const createdPelatihanId = result.id;

      if (pendingFormData.type !== "bimtek") {
        // 2. Iterate and create modules sequentially
        for (const mod of localModules) {
          const modResult = await createModuleMutation.mutateAsync({
            pelatihanId: createdPelatihanId,
            title: mod.title,
            description: mod.description,
          });
          const createdModuleId = modResult.id;

          // 2a. Iterate and create materials for this module
          for (const mat of mod.materials) {
            await createMaterialMutation.mutateAsync({
              pelatihanId: createdPelatihanId,
              moduleId: createdModuleId,
              title: mat.title,
              description: mat.description,
              type: mat.type,
              fileUrl: mat.fileUrl,
              fileSize: mat.fileSize,
              duration: mat.duration,
              isPreview: mat.isPreview,
            });
          }
        }

        // 3. Iterate and create orphan materials (without module)
        for (const mat of localOrphans) {
          await createMaterialMutation.mutateAsync({
            pelatihanId: createdPelatihanId,
            moduleId: null,
            title: mat.title,
            description: mat.description,
            type: mat.type,
            fileUrl: mat.fileUrl,
            fileSize: mat.fileSize,
            duration: mat.duration,
            isPreview: mat.isPreview,
          });
        }
      }

      // Invalidate query cache so training list refreshes
      queryClient.invalidateQueries(
        trpc.pelatihan.base.getAllPelatihan.queryOptions({}),
      );

      // Turn off loading spinner and trigger beautiful success modal dialog
      setIsSubmittingSequentially(false);
      setIsSuccessDialogOpen(true);

      // Auto-redirect to training list after 2 seconds matching figma screen 3 background
      setTimeout(() => {
        setIsSuccessDialogOpen(false);
        navigate({
          to: "/back-office/pelatihan",
          search: { type: pendingFormData.type },
        });
      }, 2000);
    } catch (error: any) {
      setIsSubmittingSequentially(false);
      globalErrorToast(
        error.message || "Gagal menyimpan kurikulum",
        "Terjadi Kesalahan",
      );
    }
  };

  const handleSubmit = form.handleSubmit(handlePreSubmit, (errors) => {
    console.error("Hook Form Validation Errors:", errors);
    globalErrorToast(
      "Pastikan semua form yang wajib diisi telah terisi dengan benar.",
      "Validasi Gagal",
    );
  });

  const hasModules = localModules.length > 0;
  const hasOrphans = localOrphans.length > 0;

  return (
    <div className="flex min-h-[85vh] flex-col bg-[#F8FAFC] font-['Poppins']">
      {/* Sequential Transaction Overlay */}
      {isSubmittingSequentially && (
        <div className="fixed inset-0 z-50 flex flex-col items-center justify-center gap-4 bg-slate-900/60 p-6 text-center text-white">
          <IconLoader className="size-10 animate-spin text-blue-400" />
          <h3 className="text-lg font-bold">Menyimpan Pelatihan...</h3>
          <p className="max-w-sm text-xs text-slate-300">
            Mohon jangan tutup jendela ini. Sistem sedang mengunggah rekaman
            kursus, modul, dan materi secara berurutan ke database.
          </p>
        </div>
      )}

      {/* Header bar */}
      <header className="mb-4 flex h-16 shrink-0 items-center justify-between gap-3 rounded-xl border-b border-slate-100 bg-white px-6 shadow-sm">
        <nav className="flex items-center gap-2 text-sm">
          <span className="text-lg font-bold text-[#4D4D4D]">
            {form.watch("type") === "bimtek"
              ? "Bimtek"
              : form.watch("type") === "webinar"
                ? "Webinar"
                : "E-Learning"}
          </span>
          <span className="font-semibold text-[#768094]">/</span>
          <span className="font-bold text-[#768094]">
            {form.watch("type") === "bimtek"
              ? "Bimtek Baru"
              : form.watch("type") === "webinar"
                ? "Webinar Baru"
                : "Kursus Baru"}
          </span>
        </nav>
        <div className="flex items-center gap-1.5 rounded-full bg-slate-100 px-3 py-1.5 text-xs font-semibold text-[#768094]">
          <span className="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-[#1061D6] text-[10px] font-bold text-white">
            {currentStep}
          </span>
          <span>dari {steps.length} langkah</span>
        </div>
      </header>

      {/* Main content: sidebar + forms */}
      <div className="flex min-h-[600px] flex-1 items-stretch gap-6">
        <WizardSidebar
          currentStep={currentStep}
          completedSteps={completedSteps}
          steps={steps}
          type={form.watch("type") || "elearning"}
        />

        {/* Content Box */}
        <div className="flex flex-1 flex-col overflow-hidden rounded-xl border border-slate-100 bg-white shadow-sm">
          <Form {...form}>
            <form
              onSubmit={handleSubmit}
              className="flex h-full flex-1 flex-col justify-between"
            >
              {/* Scrollable Form Panel */}
              <div className="max-h-[75vh] flex-1 overflow-y-auto p-8">
                {currentStep === 1 && (
                  <Step1InformasiDasar
                    form={form as any}
                    categories={dbCategories || []}
                    categoriesLoading={categoriesLoading}
                  />
                )}

                {form.watch("type") === "bimtek" ? (
                  <>
                    {currentStep === 2 && (
                      <Step2Deskripsi
                        form={form as any}
                        benefits={benefits}
                        setBenefits={setBenefits}
                      />
                    )}
                    {currentStep === 3 && (
                      <Step3BimtekJadwalFasilitas form={form as any} />
                    )}
                    {currentStep === 4 && (
                      <Step4GambarMedia form={form as any} />
                    )}
                    {currentStep === 5 && (
                      <StepPersyaratanPendaftaran form={form as any} />
                    )}
                    {currentStep === 6 && (
                      <StepReviewPublikasi
                        form={form as any}
                        localModulesCount={localModulesCount}
                        localMaterialsCount={localMaterialsCount}
                        benefits={benefits}
                        hasCertificate={hasCertificate}
                      />
                    )}
                  </>
                ) : (
                  <>
                    {currentStep === 2 && (
                      <Step2Deskripsi
                        form={form as any}
                        benefits={benefits}
                        setBenefits={setBenefits}
                      />
                    )}
                    {currentStep === 3 && (
                      <Step3PengaturanCourse
                        form={form as any}
                        dbPelatihan={dbPelatihan}
                        pelatihanLoading={pelatihanLoading}
                        hasCertificate={hasCertificate}
                        setHasCertificate={setHasCertificate}
                      />
                    )}
                    {currentStep === 4 && (
                      <Step4GambarMedia form={form as any} />
                    )}

                    {/* Step 5: Interactive Materi & Quiz Curriculum Builder */}
                    {currentStep === 5 && (
                      <div className="space-y-5">
                        <div className="flex flex-wrap items-center justify-between gap-4 border-b border-slate-100 pb-3">
                          <div>
                            <h2 className="text-lg font-bold text-[#334155]">
                              {form.watch("type") === "webinar"
                                ? "Jadwal & Materi"
                                : "Materi & Modul"}
                            </h2>
                            <p className="mt-0.5 text-xs text-[#768094]">
                              {form.watch("type") === "webinar"
                                ? "Tambahkan jadwal & materi pembelajaran!"
                                : "Kelompokkan modul ke dalam materi terstruktur sebelum kursus diterbitkan"}
                            </p>
                          </div>
                          <div className="flex items-center gap-2">
                            {form.watch("type") === "webinar" ? (
                              <Button
                                type="button"
                                className="h-9 rounded-lg bg-[#1061D6] text-xs font-bold text-white hover:bg-blue-700"
                                onClick={openNewModule}
                              >
                                <IconPlus className="mr-1 size-4 text-white" />
                                Tambah jadwal
                              </Button>
                            ) : (
                              <>
                                <Button
                                  type="button"
                                  className="h-9 rounded-lg bg-[#1061D6] text-xs font-bold text-white hover:bg-blue-700"
                                  onClick={openNewModule}
                                >
                                  <IconPlus className="mr-1 size-4 text-white" />
                                  Tambah Materi
                                </Button>
                                <Button
                                  type="button"
                                  variant="outline"
                                  className="h-9 rounded-lg border-slate-200 text-xs font-bold text-[#475569]"
                                  onClick={() => openNewMaterial("none")}
                                >
                                  <IconPlus className="mr-1 size-4 text-[#475569]" />
                                  Tambah Modul
                                </Button>
                              </>
                            )}
                          </div>
                        </div>

                        {/* Modules and Materials lists */}
                        <div className="space-y-4">
                          {localModules.map((mod) => {
                            const isExpanded =
                              expandedModules[mod.id] !== false; // Default expanded
                            const materialsCount = mod.materials.length;

                            return (
                              <div
                                key={mod.id}
                                className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"
                              >
                                {/* Module Header */}
                                <div className="flex items-center justify-between gap-4 border-b border-slate-100 bg-slate-50/70 p-3">
                                  <div
                                    className="flex min-w-0 flex-1 cursor-pointer items-center gap-2.5"
                                    onClick={() => toggleExpand(mod.id)}
                                  >
                                    <div className="shrink-0 text-slate-500">
                                      {form.watch("type") === "webinar" ? (
                                        <svg
                                          width="20"
                                          height="20"
                                          viewBox="0 0 24 24"
                                          fill="none"
                                          stroke="#1061D6"
                                          strokeWidth="2.5"
                                          strokeLinecap="round"
                                          strokeLinejoin="round"
                                          className="shrink-0"
                                        >
                                          <rect
                                            x="3"
                                            y="4"
                                            width="18"
                                            height="18"
                                            rx="2"
                                            ry="2"
                                          />
                                          <line x1="16" y1="2" x2="16" y2="6" />
                                          <line x1="8" y1="2" x2="8" y2="6" />
                                          <line
                                            x1="3"
                                            y1="10"
                                            x2="21"
                                            y2="10"
                                          />
                                        </svg>
                                      ) : isExpanded ? (
                                        <IconFolderOpen className="size-5 text-[#1061D6]" />
                                      ) : (
                                        <IconFolder className="size-5 text-[#768094]" />
                                      )}
                                    </div>
                                    <div className="min-w-0 flex-1">
                                      <h3 className="truncate text-xs font-bold text-[#334155]">
                                        {mod.title}
                                      </h3>
                                      {mod.description && (
                                        <p className="truncate text-[10px] text-[#768094]">
                                          {mod.description}
                                        </p>
                                      )}
                                    </div>
                                    <span className="shrink-0 rounded-full bg-slate-200/80 px-2 py-0.5 text-[9px] font-bold text-[#475569]">
                                      {materialsCount} Modul
                                    </span>
                                  </div>

                                  {/* Module action triggers */}
                                  <div className="flex shrink-0 items-center gap-0.5">
                                    <Button
                                      type="button"
                                      variant="ghost"
                                      size="icon"
                                      className="h-7 w-7 rounded-md text-slate-400 hover:bg-slate-100 hover:text-[#1061D6]"
                                      title={
                                        form.watch("type") === "webinar"
                                          ? "Tambah Materi ke Sesi ini"
                                          : "Tambah Modul ke Materi ini"
                                      }
                                      onClick={() => openNewMaterial(mod.id)}
                                    >
                                      <IconPlus className="size-4" />
                                    </Button>
                                    <Button
                                      type="button"
                                      variant="ghost"
                                      size="icon"
                                      className="h-7 w-7 rounded-md text-slate-400 hover:bg-slate-100 hover:text-[#1061D6]"
                                      title={
                                        form.watch("type") === "webinar"
                                          ? "Edit Jadwal"
                                          : "Edit Materi"
                                      }
                                      onClick={() => openEditModule(mod)}
                                    >
                                      <IconEdit className="size-3.5" />
                                    </Button>
                                    <Button
                                      type="button"
                                      variant="ghost"
                                      size="icon"
                                      className="h-7 w-7 rounded-md text-slate-400 hover:bg-red-50 hover:text-red-500"
                                      title={
                                        form.watch("type") === "webinar"
                                          ? "Hapus Jadwal"
                                          : "Hapus Materi"
                                      }
                                      onClick={() => deleteModule(mod.id)}
                                    >
                                      <IconTrash className="size-3.5" />
                                    </Button>
                                    <Button
                                      type="button"
                                      variant="ghost"
                                      size="icon"
                                      className="h-7 w-7 rounded-md text-slate-400 hover:bg-slate-100"
                                      onClick={() => toggleExpand(mod.id)}
                                    >
                                      {isExpanded ? (
                                        <IconChevronDown className="size-4" />
                                      ) : (
                                        <IconChevronRight className="size-4" />
                                      )}
                                    </Button>
                                  </div>
                                </div>

                                {/* Module materials */}
                                {isExpanded && (
                                  <div className="divide-y divide-slate-100 bg-white">
                                    {materialsCount === 0 ? (
                                      <div className="p-6 text-center text-[10px] font-medium text-slate-400">
                                        Belum ada modul di materi ini. Klik ikon
                                        tambah (+) untuk memasukkan modul.
                                      </div>
                                    ) : (
                                      mod.materials.map((mat, mIdx) => {
                                        const cfg =
                                          typeConfig[mat.type] ??
                                          typeConfig.document!;
                                        const sizeLabel = formatFileSize(
                                          mat.fileSize,
                                        );

                                        return (
                                          <div
                                            key={mat.id}
                                            className="group flex items-center gap-3 p-3.5 transition-colors hover:bg-slate-50/50"
                                          >
                                            <div className="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-[#1061D6]/10 text-[9px] font-bold text-[#1061D6]">
                                              {mIdx + 1}
                                            </div>
                                            <div
                                              className={cn(
                                                "shrink-0 rounded-lg p-1.5",
                                                cfg.bg,
                                              )}
                                            >
                                              <cfg.Icon
                                                className={cn(
                                                  "size-4",
                                                  cfg.color,
                                                )}
                                              />
                                            </div>
                                            <div className="min-w-0 flex-1">
                                              <div className="flex items-center gap-1.5">
                                                <h4 className="truncate text-xs font-bold text-[#334155]">
                                                  {mat.title}
                                                </h4>
                                                {mat.isPreview && (
                                                  <span className="shrink-0 rounded-full border border-blue-100 bg-blue-50 px-1.5 py-0.5 text-[8px] font-bold text-blue-700">
                                                    Preview
                                                  </span>
                                                )}
                                              </div>
                                              {mat.description && (
                                                <p className="mt-0.5 truncate text-[10px] text-slate-500">
                                                  {mat.description}
                                                </p>
                                              )}
                                              <div className="mt-0.5 flex items-center gap-1.5 text-[9px] font-semibold text-[#768094]">
                                                <span className={cfg.color}>
                                                  {cfg.label}
                                                </span>
                                                <span>·</span>
                                                {mat.duration && (
                                                  <>
                                                    <span>
                                                      {mat.duration} menit
                                                    </span>
                                                    <span>·</span>
                                                  </>
                                                )}
                                                {sizeLabel && (
                                                  <span>{sizeLabel}</span>
                                                )}
                                              </div>
                                            </div>

                                            <div className="flex shrink-0 items-center gap-0.5 opacity-0 transition-opacity group-hover:opacity-100">
                                              <Button
                                                type="button"
                                                variant="ghost"
                                                size="icon"
                                                className="h-7 w-7 rounded-md text-slate-400 hover:bg-slate-100 hover:text-[#1061D6]"
                                                onClick={() =>
                                                  openEditMaterial(mat, mod.id)
                                                }
                                              >
                                                <IconEdit className="size-3.5" />
                                              </Button>
                                              <Button
                                                type="button"
                                                variant="ghost"
                                                size="icon"
                                                className="h-7 w-7 rounded-md text-slate-400 hover:bg-red-50 hover:text-red-500"
                                                onClick={() =>
                                                  deleteMaterial(mat.id, mod.id)
                                                }
                                              >
                                                <IconTrash className="size-3.5" />
                                              </Button>
                                            </div>
                                          </div>
                                        );
                                      })
                                    )}
                                  </div>
                                )}
                              </div>
                            );
                          })}

                          {/* Loose / Orphan Materials */}
                          {hasOrphans && (
                            <div className="mt-4 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">
                              <div className="flex items-center justify-between border-b border-slate-100 bg-slate-50/70 p-3">
                                <div>
                                  <h3 className="text-xs font-bold text-[#334155]">
                                    Modul Tanpa Materi
                                  </h3>
                                  <p className="text-[10px] text-[#768094]">
                                    Berkas pembelajaran lepas
                                  </p>
                                </div>
                                <span className="rounded-full bg-slate-200/80 px-2 py-0.5 text-[9px] font-bold text-[#475569]">
                                  {localOrphans.length} Modul
                                </span>
                              </div>
                              <div className="divide-y divide-slate-100">
                                {localOrphans.map((mat, oIdx) => {
                                  const cfg =
                                    typeConfig[mat.type] ??
                                    typeConfig.document!;
                                  const sizeLabel = formatFileSize(
                                    mat.fileSize,
                                  );

                                  return (
                                    <div
                                      key={mat.id}
                                      className="group flex items-center gap-3 p-3.5 transition-colors hover:bg-slate-50/50"
                                    >
                                      <div className="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-[#1061D6]/10 text-[9px] font-bold text-[#1061D6]">
                                        {oIdx + 1}
                                      </div>
                                      <div
                                        className={cn(
                                          "shrink-0 rounded-lg p-1.5",
                                          cfg.bg,
                                        )}
                                      >
                                        <cfg.Icon
                                          className={cn("size-4", cfg.color)}
                                        />
                                      </div>
                                      <div className="min-w-0 flex-1">
                                        <div className="flex items-center gap-1.5">
                                          <h4 className="truncate text-xs font-bold text-[#334155]">
                                            {mat.title}
                                          </h4>
                                          {mat.isPreview && (
                                            <span className="shrink-0 rounded-full border border-blue-100 bg-blue-50 px-1.5 py-0.5 text-[8px] font-bold text-blue-700">
                                              Preview
                                            </span>
                                          )}
                                        </div>
                                        {mat.description && (
                                          <p className="mt-0.5 truncate text-[10px] text-slate-500">
                                            {mat.description}
                                          </p>
                                        )}
                                        <div className="mt-0.5 flex items-center gap-1.5 text-[9px] font-semibold text-[#768094]">
                                          <span className={cfg.color}>
                                            {cfg.label}
                                          </span>
                                          <span>·</span>
                                          {mat.duration && (
                                            <>
                                              <span>{mat.duration} menit</span>
                                              <span>·</span>
                                            </>
                                          )}
                                          {sizeLabel && (
                                            <span>{sizeLabel}</span>
                                          )}
                                        </div>
                                      </div>

                                      <div className="flex shrink-0 items-center gap-0.5 opacity-0 transition-opacity group-hover:opacity-100">
                                        <Button
                                          type="button"
                                          variant="ghost"
                                          size="icon"
                                          className="h-7 w-7 rounded-md text-slate-400 hover:bg-slate-100 hover:text-[#1061D6]"
                                          onClick={() =>
                                            openEditMaterial(mat, "none")
                                          }
                                        >
                                          <IconEdit className="size-3.5" />
                                        </Button>
                                        <Button
                                          type="button"
                                          variant="ghost"
                                          size="icon"
                                          className="h-7 w-7 rounded-md text-slate-400 hover:bg-red-50 hover:text-red-500"
                                          onClick={() =>
                                            deleteMaterial(mat.id, "none")
                                          }
                                        >
                                          <IconTrash className="size-3.5" />
                                        </Button>
                                      </div>
                                    </div>
                                  );
                                })}
                              </div>
                            </div>
                          )}

                          {/* Empty curriculum state matching figma screen 1 */}
                          {form.watch("type") === "webinar"
                            ? !hasModules && (
                                <div className="flex w-full flex-col items-center justify-center rounded-xl border border-slate-100 bg-white p-16 text-center shadow-sm">
                                  <div className="mb-4 flex justify-center">
                                    <svg
                                      width="120"
                                      height="96"
                                      viewBox="0 0 120 96"
                                      fill="none"
                                      xmlns="http://www.w3.org/2000/svg"
                                    >
                                      {/* Background abstract decorations */}
                                      <path
                                        d="M12 48H28"
                                        stroke="#E2E8F0"
                                        strokeWidth="3"
                                        strokeLinecap="round"
                                      />
                                      <path
                                        d="M92 36H104"
                                        stroke="#E2E8F0"
                                        strokeWidth="3"
                                        strokeLinecap="round"
                                      />
                                      <path
                                        d="M16 68H36"
                                        stroke="#E2E8F0"
                                        strokeWidth="3"
                                        strokeLinecap="round"
                                      />
                                      <path
                                        d="M84 60H100"
                                        stroke="#E2E8F0"
                                        strokeWidth="3"
                                        strokeLinecap="round"
                                      />

                                      {/* Back paper */}
                                      <rect
                                        x="42"
                                        y="16"
                                        width="38"
                                        height="52"
                                        rx="4"
                                        fill="white"
                                        stroke="#93C5FD"
                                        strokeWidth="2.5"
                                      />
                                      <line
                                        x1="50"
                                        y1="26"
                                        x2="72"
                                        y2="26"
                                        stroke="#E2E8F0"
                                        strokeWidth="2"
                                        strokeLinecap="round"
                                      />
                                      <line
                                        x1="50"
                                        y1="34"
                                        x2="72"
                                        y2="34"
                                        stroke="#E2E8F0"
                                        strokeWidth="2"
                                        strokeLinecap="round"
                                      />

                                      {/* Front paper */}
                                      <rect
                                        x="48"
                                        y="24"
                                        width="38"
                                        height="52"
                                        rx="4"
                                        fill="white"
                                        stroke="#3B82F6"
                                        strokeWidth="2.5"
                                      />
                                      {/* Lines in front paper */}
                                      <line
                                        x1="56"
                                        y1="36"
                                        x2="78"
                                        y2="36"
                                        stroke="#93C5FD"
                                        strokeWidth="2"
                                        strokeLinecap="round"
                                      />
                                      <line
                                        x1="56"
                                        y1="44"
                                        x2="78"
                                        y2="44"
                                        stroke="#93C5FD"
                                        strokeWidth="2"
                                        strokeLinecap="round"
                                      />
                                      <line
                                        x1="56"
                                        y1="52"
                                        x2="70"
                                        y2="52"
                                        stroke="#93C5FD"
                                        strokeWidth="2"
                                        strokeLinecap="round"
                                      />
                                      <line
                                        x1="56"
                                        y1="60"
                                        x2="66"
                                        y2="60"
                                        stroke="#93C5FD"
                                        strokeWidth="2"
                                        strokeLinecap="round"
                                      />
                                    </svg>
                                  </div>
                                  <h4 className="mb-1 text-sm font-bold text-[#334155]">
                                    Belum ada Jadwal
                                  </h4>
                                  <p className="mb-6 max-w-sm text-xs leading-relaxed text-[#768094]">
                                    Tambahkan Jadwal & materi pembelajaran untuk
                                    pelatihan anda!
                                  </p>
                                  <Button
                                    type="button"
                                    className="flex h-10 items-center gap-1.5 rounded-lg bg-[#1061D6] px-6 text-xs font-bold text-white shadow-sm hover:bg-blue-700"
                                    onClick={openNewModule}
                                  >
                                    <IconPlus className="size-4" />
                                    Tambah jadwal
                                  </Button>
                                </div>
                              )
                            : !hasModules &&
                              !hasOrphans && (
                                <div className="flex flex-col items-center justify-center gap-3 rounded-xl border-2 border-dashed border-slate-200 bg-slate-50/50 p-12 text-center">
                                  <div className="flex h-14 w-14 items-center justify-center rounded-full bg-slate-100">
                                    <IconFolder className="size-7 text-slate-400" />
                                  </div>
                                  <h4 className="text-sm font-bold text-[#334155]">
                                    Kurikulum Course Kosong
                                  </h4>
                                  <p className="max-w-sm text-xs text-[#768094]">
                                    Buat materi pertama untuk mengelompokkan
                                    modul atau tambahkan berkas modul lepas
                                    langsung di form wizard.
                                  </p>
                                  <div className="mt-2 flex items-center gap-2">
                                    <Button
                                      type="button"
                                      className="h-9 rounded-lg bg-[#1061D6] text-xs font-bold text-white hover:bg-blue-700"
                                      onClick={openNewModule}
                                    >
                                      Buat Materi Pertama
                                    </Button>
                                    <Button
                                      type="button"
                                      variant="outline"
                                      className="h-9 rounded-lg border-slate-200 text-xs font-bold text-[#475569]"
                                      onClick={() => openNewMaterial("none")}
                                    >
                                      Tambah Modul Lepas
                                    </Button>
                                  </div>
                                </div>
                              )}
                        </div>
                      </div>
                    )}

                    {currentStep === 6 && (
                      <StepPersyaratanPendaftaran form={form as any} />
                    )}

                    {currentStep === 7 && (
                      <StepReviewPublikasi
                        form={form as any}
                        localModulesCount={localModulesCount}
                        localMaterialsCount={localMaterialsCount}
                        benefits={benefits}
                        hasCertificate={hasCertificate}
                      />
                    )}
                  </>
                )}
              </div>

              {/* Footer navigation */}
              <div className="flex shrink-0 items-center justify-between border-t border-slate-100 bg-white p-6">
                <Button
                  type="button"
                  variant="outline"
                  onClick={handlePrev}
                  disabled={currentStep === 1}
                  className="flex h-9 items-center gap-1.5 border-slate-200 text-xs font-bold text-[#334155] disabled:opacity-30"
                >
                  <svg
                    width="14"
                    height="14"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    strokeWidth="2.5"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                  >
                    <polyline points="15,18 9,12 15,6" />
                  </svg>
                  Sebelumnya
                </Button>

                <div className="flex items-center gap-2">
                  {steps.map((s) => (
                    <div
                      key={s.number}
                      className={cn(
                        "h-1.5 w-1.5 rounded-full transition-all duration-300",
                        s.number === currentStep
                          ? "w-4 bg-[#1061D6]"
                          : completedSteps.has(s.number)
                            ? "bg-[#1061D6]/40"
                            : "bg-slate-200",
                      )}
                    />
                  ))}
                </div>

                {currentStep < steps.length ? (
                  <Button
                    type="button"
                    onClick={handleNext}
                    className="flex h-9 items-center gap-1.5 rounded-lg bg-[#1061D6] text-xs font-bold text-white shadow-sm hover:bg-[#0d52b8]"
                  >
                    Selanjutnya
                    <svg
                      width="14"
                      height="14"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      strokeWidth="2.5"
                      strokeLinecap="round"
                      strokeLinejoin="round"
                    >
                      <polyline points="9,18 15,12 9,6" />
                    </svg>
                  </Button>
                ) : (
                  <Button
                    type="submit"
                    disabled={
                      createMutation.isPending || isSubmittingSequentially
                    }
                    className="flex h-9 items-center gap-1.5 rounded-lg bg-[#1061D6] text-xs font-bold text-white shadow-sm hover:bg-[#0d52b8]"
                  >
                    {createMutation.isPending || isSubmittingSequentially ? (
                      <>
                        <IconLoader className="size-4 animate-spin" />
                        Menyimpan...
                      </>
                    ) : (
                      <>
                        <IconCheck className="size-4" strokeWidth={3} />
                        Buat{" "}
                        {form.watch("type") === "bimtek"
                          ? "Bimtek"
                          : form.watch("type") === "webinar"
                            ? "Webinar"
                            : "Kursus"}
                      </>
                    )}
                  </Button>
                )}
              </div>
            </form>
          </Form>
        </div>
      </div>

      {/* ─── Modal Dialog: Tambah Modul Baru (Figma Screen 2) ────────────────── */}
      <Dialog open={isModuleDialogOpen} onOpenChange={setIsModuleDialogOpen}>
        <DialogContent className="font-['Poppins'] sm:max-w-[420px]">
          <form onSubmit={handleModuleSubmit}>
            <DialogHeader>
              <DialogTitle className="text-sm font-bold text-[#1E293B]">
                {form.watch("type") === "webinar"
                  ? editingModuleId
                    ? "Edit Jadwal"
                    : "Tambah Jadwal Baru"
                  : editingModuleId
                    ? "Edit Materi"
                    : "Tambah Materi Baru"}
              </DialogTitle>
              <DialogDescription className="mt-1 text-[10px] leading-normal text-[#64748B]">
                {form.watch("type") === "webinar"
                  ? "Buat jadwal terstruktur untuk sesi webinar dan materi pembelajaran Anda."
                  : "Buat materi terstruktur untuk mengelompokkan modul pembelajaran dalam kursus ini."}
              </DialogDescription>
            </DialogHeader>
            <div className="grid gap-3.5 py-4">
              <div className="grid gap-1">
                <Label
                  htmlFor="mod-title"
                  className="text-[10px] font-bold text-[#475569]"
                >
                  {form.watch("type") === "webinar"
                    ? "Nama Jadwal"
                    : "Nama Materi"}{" "}
                  <span className="text-red-500">*</span>
                </Label>
                <Input
                  id="mod-title"
                  placeholder={
                    form.watch("type") === "webinar"
                      ? "Contoh: Sesi 1 - Pengenalan K3 & Regulasi Dasar"
                      : "Contoh: Materi 1 - Dasar Keselamatan Kerja"
                  }
                  value={moduleTitle}
                  onChange={(e) => setModuleTitle(e.target.value)}
                  className="h-10 rounded-lg border-slate-200 text-xs"
                  required
                />
              </div>
              <div className="grid gap-1">
                <Label
                  htmlFor="mod-desc"
                  className="text-[10px] font-bold text-[#475569]"
                >
                  {form.watch("type") === "webinar"
                    ? "Deskripsi & Waktu Jadwal"
                    : "Deskripsi Materi"}
                </Label>
                <Textarea
                  id="mod-desc"
                  placeholder={
                    form.watch("type") === "webinar"
                      ? "Contoh: Senin, 15 Juni 2026 - 10:00 s.d 12:00 WITA. Pembicara: John Doe."
                      : "Jelaskan secara ringkas kompetensi yang dipelajari di materi ini..."
                  }
                  value={moduleDesc}
                  onChange={(e) => setModuleDesc(e.target.value)}
                  className="min-h-[70px] resize-none rounded-lg border-slate-200 text-xs"
                />
              </div>
            </div>
            <DialogFooter>
              <Button
                type="button"
                variant="ghost"
                onClick={() => setIsModuleDialogOpen(false)}
                className="h-9 rounded-lg text-xs font-bold text-slate-400 hover:bg-slate-50"
              >
                Batal
              </Button>
              <Button
                type="submit"
                className="h-9 rounded-lg bg-[#1061D6] text-xs font-bold text-white hover:bg-blue-700"
              >
                {editingModuleId
                  ? "Simpan Perubahan"
                  : form.watch("type") === "webinar"
                    ? "Simpan Jadwal"
                    : "Simpan Materi"}
              </Button>
            </DialogFooter>
          </form>
        </DialogContent>
      </Dialog>

      {/* ─── Modal Dialog: Tambah Materi Pembelajaran ───────────────────────── */}
      <Dialog
        open={isMaterialDialogOpen}
        onOpenChange={setIsMaterialDialogOpen}
      >
        <DialogContent className="font-['Poppins'] sm:max-w-[500px]">
          <form onSubmit={handleMaterialSubmit}>
            <DialogHeader>
              <DialogTitle className="text-sm font-bold text-[#1E293B]">
                {editingMaterialId
                  ? "Edit Modul"
                  : `Tambah Modul - Langkah ${materialModalStep} dari 2`}
              </DialogTitle>
              <DialogDescription className="mt-1 text-[10px] leading-normal text-[#64748B]">
                {materialModalStep === 1
                  ? "Pilih jenis konten modul pembelajaran yang ingin Anda buat."
                  : "Lengkapi judul, deskripsi, sifat modul, serta tautan berkas/media pendukung."}
              </DialogDescription>
            </DialogHeader>

            {materialModalStep === 1 ? (
              /* Step 1: Grid 6 Cards Pilihan Jenis Konten */
              <div className="space-y-4 py-4">
                <div className="grid grid-cols-2 gap-3">
                  {[
                    {
                      type: "video" as const,
                      label: "Video Pembelajaran",
                      desc: "Tautan YouTube/Vimeo atau berkas video",
                      icon: "🎥",
                      color: "border-[#3B82F6] bg-blue-50/40 text-blue-600",
                    },
                    {
                      type: "pdf" as const,
                      label: "Dokumen / PDF",
                      desc: "Berkas PDF modul, panduan, atau presentasi",
                      icon: "📄",
                      color: "border-[#EF4444] bg-red-50/40 text-red-600",
                    },
                    {
                      type: "document" as const,
                      label: "Artikel / Teks",
                      desc: "Materi bacaan langsung berupa teks",
                      icon: "📝",
                      color: "border-[#F97316] bg-orange-50/40 text-orange-600",
                    },
                    {
                      type: "link" as const,
                      label: "Quiz / Ujian",
                      desc: "Tautan kuis evaluasi pemahaman materi",
                      icon: "🏆",
                      color: "border-[#8B5CF6] bg-violet-50/40 text-violet-600",
                    },
                    {
                      type: "ppt" as const,
                      label: "File Download",
                      desc: "Slide presentasi PPT atau dokumen pelengkap",
                      icon: "📥",
                      color: "border-[#10B981] bg-green-50/40 text-green-600",
                    },
                    {
                      type: "link" as const,
                      label: "Link Eksternal",
                      desc: "Rujukan website luar sebagai media belajar",
                      icon: "🔗",
                      color: "border-[#6366F1] bg-indigo-50/40 text-indigo-600",
                    },
                  ].map((opt, i) => {
                    const isSelected = materialType === opt.type;
                    return (
                      <button
                        key={i}
                        type="button"
                        onClick={() => setMaterialType(opt.type)}
                        className={cn(
                          "flex flex-col gap-1.5 rounded-xl border p-3.5 text-left transition-all hover:bg-slate-50",
                          isSelected
                            ? "border-[#1061D6] bg-blue-50/30 ring-1 ring-[#1061D6]"
                            : "border-slate-200 bg-white",
                        )}
                        style={{ height: "106px" }}
                      >
                        <div className="flex items-center gap-2">
                          <span
                            className={cn(
                              "flex h-7 w-7 items-center justify-center rounded-lg border text-sm font-bold",
                              opt.color,
                            )}
                          >
                            {opt.icon}
                          </span>
                          <span className="text-xs leading-none font-bold text-slate-800">
                            {opt.label}
                          </span>
                        </div>
                        <p className="mt-1 text-[9px] leading-relaxed font-medium text-slate-400">
                          {opt.desc}
                        </p>
                      </button>
                    );
                  })}
                </div>

                <DialogFooter className="mt-4">
                  <Button
                    type="button"
                    variant="ghost"
                    onClick={() => setIsMaterialDialogOpen(false)}
                    className="h-9 rounded-lg text-xs font-bold text-slate-400 hover:bg-slate-50"
                  >
                    Batal
                  </Button>
                  <Button
                    type="button"
                    onClick={() => setMaterialModalStep(2)}
                    className="h-9 rounded-lg bg-[#1061D6] text-xs font-bold text-white hover:bg-blue-700"
                  >
                    Selanjutnya
                  </Button>
                </DialogFooter>
              </div>
            ) : (
              /* Step 2: Form Input Detail */
              <div className="grid gap-3.5 py-4">
                {/* Module placement indicator */}
                <div className="grid gap-1">
                  <Label className="text-[10px] font-bold text-[#475569]">
                    {form.watch("type") === "webinar"
                      ? "Ditempatkan Di Jadwal Sesi"
                      : "Ditempatkan Di Materi"}
                  </Label>
                  <Select
                    value={activeModuleIdForMaterial}
                    onValueChange={setActiveModuleIdForMaterial}
                  >
                    <SelectTrigger className="h-10 rounded-lg border-slate-200 text-xs">
                      <SelectValue
                        placeholder={
                          form.watch("type") === "webinar"
                            ? "Pilih Sesi"
                            : "Pilih Materi"
                        }
                      />
                    </SelectTrigger>
                    <SelectContent>
                      <SelectItem value="none" className="text-xs">
                        {form.watch("type") === "webinar"
                          ? "Tanpa Sesi"
                          : "Tanpa Materi (Modul Lepas)"}
                      </SelectItem>
                      {localModules.map((m) => (
                        <SelectItem key={m.id} value={m.id} className="text-xs">
                          {m.title}
                        </SelectItem>
                      ))}
                    </SelectContent>
                  </Select>
                </div>

                {/* Title */}
                <div className="grid gap-1">
                  <Label
                    htmlFor="mat-title"
                    className="text-[10px] font-bold text-[#475569]"
                  >
                    Judul Modul <span className="text-red-500">*</span>
                  </Label>
                  <Input
                    id="mat-title"
                    placeholder="Contoh: Modul 1.1 - Identifikasi Bahaya Kerja"
                    value={materialTitle}
                    onChange={(e) => setMaterialTitle(e.target.value)}
                    className="h-10 rounded-lg border-slate-200 text-xs"
                    required
                  />
                </div>

                {/* URL Input conditional to type */}
                {materialType !== "document" && (
                  <div className="grid gap-1">
                    <Label
                      htmlFor="mat-url"
                      className="text-[10px] font-bold text-[#475569]"
                    >
                      {materialType === "video"
                        ? "Tautan URL Video (YouTube / Vimeo)"
                        : materialType === "pdf"
                          ? "Tautan URL Berkas PDF"
                          : materialType === "ppt"
                            ? "Tautan URL File Download"
                            : "Tautan URL Website Eksternal"}{" "}
                      <span className="text-red-500">*</span>
                    </Label>
                    <Input
                      id="mat-url"
                      placeholder="Contoh: https://youtube.com/watch?v=... atau https://example.com/file.pdf"
                      value={materialUrl}
                      onChange={(e) => setMaterialUrl(e.target.value)}
                      className="h-10 rounded-lg border-slate-200 text-xs"
                      required
                    />
                  </div>
                )}

                {/* Duration & Size */}
                <div className="grid grid-cols-2 gap-3.5">
                  <div className="grid gap-1">
                    <Label
                      htmlFor="mat-dur"
                      className="text-[10px] font-bold text-[#475569]"
                    >
                      Estimasi Durasi (Menit)
                    </Label>
                    <Input
                      id="mat-dur"
                      type="number"
                      min={1}
                      placeholder="15"
                      value={materialDuration}
                      onChange={(e) => setMaterialDuration(e.target.value)}
                      className="h-10 rounded-lg border-slate-200 text-xs"
                    />
                  </div>
                  <div className="grid gap-1">
                    <Label
                      htmlFor="mat-size"
                      className="text-[10px] font-bold text-[#475569]"
                    >
                      Ukuran Berkas (Bytes)
                    </Label>
                    <Input
                      id="mat-size"
                      type="number"
                      placeholder="1048576"
                      value={materialSize}
                      onChange={(e) => setMaterialSize(e.target.value)}
                      className="h-10 rounded-lg border-slate-200 text-xs"
                    />
                  </div>
                </div>

                {/* Preview Toggle */}
                <div className="mt-1 flex items-center justify-between rounded-xl border border-slate-100 bg-slate-50/50 p-3">
                  <div className="flex flex-col gap-0.5">
                    <span className="text-[10px] font-bold text-[#334155]">
                      Pratinjau Bebas (Preview)
                    </span>
                    <span className="text-[8px] font-semibold text-slate-400">
                      Izinkan modul diakses secara gratis tanpa mendaftar
                    </span>
                  </div>
                  <Switch
                    checked={materialIsPreview}
                    onCheckedChange={setMaterialIsPreview}
                  />
                </div>

                {/* Description */}
                <div className="mt-1 grid gap-1">
                  <Label
                    htmlFor="mat-desc"
                    className="text-[10px] font-bold text-[#475569]"
                  >
                    Deskripsi Ringkas Modul{" "}
                    <span className="text-red-500">*</span>
                  </Label>
                  <Textarea
                    id="mat-desc"
                    placeholder="Ringkasan penjelasan isi modul..."
                    value={materialDesc}
                    onChange={(e) => setMaterialDesc(e.target.value)}
                    className="min-h-[50px] resize-none rounded-lg border-slate-200 text-xs"
                    required
                  />
                </div>

                <DialogFooter className="mt-2">
                  {!editingMaterialId && (
                    <Button
                      type="button"
                      variant="outline"
                      onClick={() => setMaterialModalStep(1)}
                      className="h-9 rounded-lg border-slate-200 text-xs font-bold text-slate-500 hover:bg-slate-50"
                    >
                      Kembali
                    </Button>
                  )}
                  <Button
                    type="submit"
                    className="h-9 rounded-lg bg-[#1061D6] text-xs font-bold text-white hover:bg-blue-700"
                  >
                    {editingMaterialId ? "Simpan Perubahan" : "Simpan Modul"}
                  </Button>
                </DialogFooter>
              </div>
            )}
          </form>
        </DialogContent>
      </Dialog>

      {/* ─── Modal Dialog: Konfirmasi Simpan Kursus (Figma Screen 2) ────────── */}
      <Dialog open={isConfirmDialogOpen} onOpenChange={setIsConfirmDialogOpen}>
        <DialogContent className="flex flex-col items-center gap-5 rounded-2xl p-8 text-center font-['Poppins'] sm:max-w-[360px]">
          <div className="flex h-20 w-20 items-center justify-center rounded-full bg-blue-50 text-[#1061D6] shadow-inner">
            <svg
              width="40"
              height="40"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              strokeWidth="3"
              strokeLinecap="round"
              strokeLinejoin="round"
              className="animate-pulse"
            >
              <polyline points="20 6 9 17 4 12" />
            </svg>
          </div>
          <div className="space-y-1">
            <DialogTitle className="text-lg font-bold text-[#1E293B]">
              Simpan Kursus?
            </DialogTitle>
            <DialogDescription className="text-xs font-medium text-[#64748B]">
              Pastikan informasi sudah benar.
            </DialogDescription>
          </div>
          <DialogFooter className="mt-2 flex w-full items-center gap-3 sm:justify-center">
            <Button
              type="button"
              variant="outline"
              onClick={() => setIsConfirmDialogOpen(false)}
              className="h-11 flex-1 rounded-xl border-slate-200 text-xs font-bold text-slate-500 hover:bg-slate-50"
            >
              Batal
            </Button>
            <Button
              type="button"
              onClick={executeSave}
              className="h-11 flex-1 rounded-xl bg-[#1061D6] text-xs font-bold text-white shadow-md hover:bg-blue-700"
            >
              Simpan
            </Button>
          </DialogFooter>
        </DialogContent>
      </Dialog>

      {/* ─── Modal Dialog: Sukses Simpan Kursus (Figma Screen 3) ────────────── */}
      <Dialog open={isSuccessDialogOpen} onOpenChange={setIsSuccessDialogOpen}>
        <DialogContent className="flex flex-col items-center gap-4 rounded-2xl border-none p-8 text-center font-['Poppins'] shadow-2xl sm:max-w-[320px]">
          <div className="flex h-20 w-20 animate-bounce items-center justify-center rounded-full bg-[#22C55E] text-white shadow-lg shadow-green-100">
            <svg
              width="44"
              height="44"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              strokeWidth="3.5"
              strokeLinecap="round"
              strokeLinejoin="round"
            >
              <polyline points="20 6 9 17 4 12" />
            </svg>
          </div>
          <DialogTitle className="text-base font-bold tracking-tight text-[#1E293B]">
            Kursus Berhasil Disimpan
          </DialogTitle>
        </DialogContent>
      </Dialog>
    </div>
  );
}

// ##################
// end authored
// ##################
