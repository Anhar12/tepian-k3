// ##################
// authored (generated by claude, Jun 12 2026 19:40 WITA)
// ##################

import { createFileRoute, redirect } from "@tanstack/react-router";

export const Route = createFileRoute("/(core)/back-office/ppid/")({
  beforeLoad: async () => {
    throw redirect({
      to: "/back-office/landing-settings",
      search: { tab: "ppid" },
    });
  },
  component: () => null,
});

// ##################
// end authored
// ##################
