// ##################
// authored (generated by gemini, Jun 12 2026 17:50 WITA)
// ##################

import LandingNavbar from "@/components/navbar";
import Footer from "@/components/footer";
import GridBackground from "@/components/grid-background";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { pageHead } from "@/utils/page-head";
import { trpc } from "@/utils/trpc";
import { useQuery } from "@tanstack/react-query";
import { format } from "date-fns";
import { id as idLocale } from "date-fns/locale";
import {
  ArrowLeft,
  Download,
  FileText,
  Loader2,
  Calendar,
  User,
  BookOpen,
} from "lucide-react";
import { getPublicUrl } from "@/utils/url";

export const Route = createFileRoute("/ppid/setiap-saat/$documentId")({
  component: PPIDDocumentDetailPage,
  head: () => pageHead("Detail Regulasi K3 - PPID"),
});

function PPIDDocumentDetailPage() {
  const navigate = useNavigate();
  const { documentId } = Route.useParams();

  // Fetch document details using tRPC
  const { data: doc, isLoading } = useQuery(
    trpc.platform.ppid.getPublicDocumentById.queryOptions({
      id: documentId,
    }),
  );

  if (isLoading) {
    return (
      <div className="flex min-h-screen w-full flex-col bg-white dark:bg-neutral-950">
        <LandingNavbar />
        <div className="flex flex-1 flex-col items-center justify-center text-muted-foreground">
          <Loader2 className="mb-2 size-8 animate-spin text-primary" />
          <span>Memuat detail dokumen...</span>
        </div>
      </div>
    );
  }

  if (!doc) {
    return (
      <div className="flex min-h-screen w-full flex-col bg-white dark:bg-neutral-950">
        <LandingNavbar />
        <div className="flex flex-1 flex-col items-center justify-center text-muted-foreground">
          <FileText className="mb-3 size-12 text-slate-300" />
          <p className="text-lg">Dokumen tidak ditemukan atau tidak aktif</p>
          <Button
            className="mt-4"
            onClick={() => navigate({ to: "/ppid/setiap-saat/regulasi" })}
          >
            Kembali ke Daftar Regulasi
          </Button>
        </div>
      </div>
    );
  }

  return (
    <div className="flex min-h-screen w-full flex-col overflow-x-hidden overflow-y-auto bg-white dark:bg-neutral-950">
      <LandingNavbar />

      {/* Hero Section */}
      <section className="relative flex h-40 flex-col items-center justify-center bg-primary px-4 text-center">
        <div className="pointer-events-none absolute inset-0 overflow-hidden opacity-20">
          <GridBackground />
        </div>
        <div className="relative z-10 max-w-4xl px-4">
          <h1 className="line-clamp-2 text-2xl font-bold text-primary-foreground md:text-3xl">
            {doc.title}
          </h1>
        </div>
      </section>

      {/* Detail Content */}
      <main className="container mx-auto max-w-6xl flex-1 space-y-6 px-4 py-8 sm:px-6 md:px-10">
        <Button
          variant="ghost"
          className="gap-2 text-muted-foreground hover:text-foreground"
          onClick={() => navigate({ to: "/ppid/setiap-saat/regulasi" })}
        >
          <ArrowLeft className="size-4" />
          Kembali ke Daftar Regulasi
        </Button>

        <div className="grid grid-cols-1 gap-6 lg:grid-cols-3">
          {/* Metadata Card (1 Column) */}
          <div className="space-y-6 lg:col-span-1">
            <Card className="rounded-2xl border border-slate-100 shadow-xs dark:border-neutral-800">
              <CardHeader>
                <CardTitle className="text-lg">Detail Berkas</CardTitle>
              </CardHeader>
              <CardContent className="space-y-4 text-sm">
                <div>
                  <span className="block text-xs font-semibold text-slate-400 uppercase">
                    Nomor Regulasi
                  </span>
                  <span className="font-semibold text-slate-700 dark:text-neutral-300">
                    {doc.documentNumber || "-"}
                  </span>
                </div>
                <div className="flex items-center gap-2">
                  <BookOpen className="size-4 shrink-0 text-slate-400" />
                  <div>
                    <span className="block text-[10px] font-semibold text-slate-400 uppercase">
                      Jenis
                    </span>
                    <span className="font-medium text-slate-700 dark:text-neutral-300">
                      {doc.type}
                    </span>
                  </div>
                </div>
                <div className="flex items-center gap-2">
                  <User className="size-4 shrink-0 text-slate-400" />
                  <div>
                    <span className="block text-[10px] font-semibold text-slate-400 uppercase">
                      Penerbit
                    </span>
                    <span className="font-medium text-slate-700 dark:text-neutral-300">
                      {doc.publisher}
                    </span>
                  </div>
                </div>
                <div className="flex items-center gap-2">
                  <Calendar className="size-4 shrink-0 text-slate-400" />
                  <div>
                    <span className="block text-[10px] font-semibold text-slate-400 uppercase">
                      Tanggal Rilis
                    </span>
                    <span className="font-medium text-slate-700 dark:text-neutral-300">
                      {doc.publishedAt
                        ? format(new Date(doc.publishedAt), "dd MMMM yyyy", {
                            locale: idLocale,
                          })
                        : "-"}
                    </span>
                  </div>
                </div>
                {doc.description && (
                  <div className="border-t pt-2">
                    <span className="mb-1 block text-xs font-semibold text-slate-400 uppercase">
                      Deskripsi / Abstraksi
                    </span>
                    <p className="text-xs leading-relaxed text-slate-600 dark:text-neutral-400">
                      {doc.description}
                    </p>
                  </div>
                )}
              </CardContent>
            </Card>

            {doc.fileUrl && (
              <Button
                asChild
                className="w-full gap-2 rounded-xl py-6 text-base font-semibold"
              >
                <a
                  href={getPublicUrl(doc.fileUrl)}
                  download={doc.fileName || "regulasi.pdf"}
                  target="_blank"
                  rel="noreferrer"
                >
                  <Download className="size-5" />
                  Download Berkas (PDF)
                </a>
              </Button>
            )}
          </div>

          {/* PDF Viewer (2 Columns) */}
          <div className="lg:col-span-2">
            <Card className="flex h-[600px] flex-col overflow-hidden rounded-2xl border border-slate-100 shadow-xs dark:border-neutral-800">
              <CardHeader className="flex flex-row items-center justify-between border-b bg-slate-50 px-6 py-4 dark:bg-neutral-900">
                <CardTitle className="flex items-center gap-2 text-base font-bold">
                  <FileText className="size-4 text-primary" />
                  Pratinjau Dokumen
                </CardTitle>
              </CardHeader>
              <CardContent className="flex-1 bg-slate-100 p-0 dark:bg-neutral-900">
                {doc.fileUrl ? (
                  <iframe
                    src={`${getPublicUrl(doc.fileUrl)}#toolbar=0`}
                    className="h-full w-full border-none"
                    title={doc.title}
                  />
                ) : (
                  <div className="flex h-full w-full flex-col items-center justify-center p-6 text-center text-muted-foreground">
                    <FileText className="mb-2 size-16 text-slate-300" />
                    <p>Pratinjau berkas tidak tersedia untuk dokumen ini.</p>
                  </div>
                )}
              </CardContent>
            </Card>
          </div>
        </div>
      </main>

      <Footer />
    </div>
  );
}

// ##################
// end authored
// ##################
