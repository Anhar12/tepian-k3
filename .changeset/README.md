# Changesets

Hello and welcome! This folder has been automatically generated by `@changesets/cli`, a build tool that works
with multi-package repos, or single-package repos to help you version and publish your code. You can
find the full documentation for it [in the changesets repository](https://github.com/changesets/changesets)

**Each time you make a change that needs to be released**, run:

```bash
pnpm changeset
```

This will prompt you to:

1. Select which packages have changed
2. Choose the bump type (major/minor/patch)
3. Write a summary of the changes

**To apply changesets and bump versions**, run:

```bash
pnpm changeset:version
```

**To check pending changesets**, run:

```bash
pnpm changeset:status
```
