// ##################
// authored (generated by claude, Jun 01 2026 19:43 WITA)
// ##################

import { settings } from "@tepian-k3/db/schema";
import { createInsertSchema } from "drizzle-zod";
import z from "zod";

const setSettingSchema = createInsertSchema(settings, {
  key: z.string().min(1, "Key setting harus diisi").max(250),
  value: z.string().min(1, "Value setting harus diisi"),
  description: z.string().optional(),
});

const getSettingByKeySchema = z.object({
  key: z.string().min(1, "Key setting harus diisi"),
});

export const settingSchema = {
  setSettingSchema,
  getSettingByKeySchema,
};

export default settingSchema;

// ##################
// end authored
// ##################
